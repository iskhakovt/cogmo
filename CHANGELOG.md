# Changelog

| Date | Change |
|-|-|
| 2026-04-30 | Phase 3 P3.3 first slice — dynamic skill creation end-to-end. `SkillRunner.register({branch})` reads `SKILL.md` + `skill.py` from the branch tip in the bare skills repo, parses the manifest, runs the stub classifier (`classifyManifest` in `src/skills/classifier.ts`, pinned to `notify` until the AST-lint pass lands), and atomically merges into `main` + writes the DB rows under a `pg_advisory_xact_lock(hashtext("skill_register:" + name)::bigint)`. Filesystem `git update-ref refs/heads/main` runs *inside* the transaction so a ref-update failure throws out cleanly; the small remaining hole (DB commit fails after `update-ref` succeeds) is documented and acceptable at personal scale. New `src/skills/git-ops.ts` wraps `revParse` / `gitShow` / `isAncestor` / `updateRef` (with CAS) / `deleteRef` / `getMainSha` against the bare repo and surfaces typed `GitOpsError` codes (`ref_not_found` / `file_not_found` / `non_fast_forward` / `ref_changed` / `exec_failed`). New atomic store ops `executeRegister` / `executeApprove` / `denyPendingDeploy` / `executeRollback` plus `getDeployById`. Companion RPCs: `approveDeploy({pendingId})` advances `main` for an approve-tier deploy and re-checks fast-forward at approve time; `denyDeploy({pendingId, reason})` is idempotent on missing/already-resolved ids; `rollback({name, toGitSha})` rewinds `main` + re-classifies the target SHA so the audit trail records what we rolled back to; `deregister({name})` soft-disables (audit history retained). The `cogmo skills` CLI exposes all five (`register` / `approve` / `deny` / `rollback` / `deregister`); `runner.register` removes `not_implemented_in_p3_1` from the API. New `register_skill` agent tool (`src/skills/skills-tool.ts` + `SKILLS_PROMPT_GUIDANCE`) + `Service.skills` namespace (`src/skills/skills-service.ts`) wrap the RPC for the LLM — agent authors a skill via `delegate_coding` against the skills repo, then calls `register_skill` with the branch name; the deploy is live immediately under the stub classifier. Dynamic per-turn tool registration in `handle-message`: `runner.listToolDefs()` reads each live skill's `SKILL.md` from git (cache keyed by `(name, gitSha)` so re-deploys invalidate automatically), `buildSkillTools` converts each into a `ToolSpec`, and the orchestrator constructs a fresh `ToolRegistry` per turn merging built-ins + skill tools — registered skills appear from the next turn onwards, disabled/rolled-back ones disappear. New `ToolRegistry.snapshot()` exposes registered specs for the per-turn merge. `runner.invoke` now also validates outputs against `manifest.outputs` JSON Schema via ajv (TODO marker in the file removed); a skill returning the wrong shape is recorded as `status=error` with the schema-failure detail rather than silently storing garbage on `skill_runs.output`. Source loading from git is the universal path now — `__registerForTests` stays as a fast in-memory test seed but production reads `git show <sha>:SKILL.md` via the `(name, gitSha)`-keyed cache. New unit tests: `git-ops.test.ts` (real bare repo + clones, all six wrappers exercised), `runner.register.test.ts` (16 tests covering register / re-register no-op / non-fast-forward rejection / missing branch / missing SKILL.md / invalid manifest / invoke after register / fresh-runner cross-instance source load / outputs validation / `listToolDefs` shape / source-cache invalidation across SHAs / rollback / deny idempotency / deregister soft-disable), `skill-tool-builder.test.ts` (handler success/error/throw, fault-tolerant `buildSkillTools`), `skills-tool.test.ts` (`register_skill` happy path + rejected verbatim + missing service); CLI tests extended to cover the five new subcommands. 1760 unit tests pass + 88 integration tests pass. |
| 2026-04-30 | Testing retrospective gap-fills: JSONB read-side Zod parsers reject corrupted rows (raw-SQL bypass); `ClaudeCodeBackend` stream-json parser permissive-input contract pinned; `CodingStreamingRegistry` concurrency invariants — listener-set snapshot, self-unsubscribe, re-entrant publish; reaper TTL boundary (strict `<`) + stale-DB-pass skipped on `docker.listContainers` failure; `/repo add` FSM dialog surfaces `repo_local_path_exists` and transport-side `repo_invalid_input` and clears state. |
| 2026-04-28 | Phase 6 P1 slice 4 (P1.5) — verify + push + draft PR. New `coding-task-verify` Inngest function triggered by `coding/task/cli-done` (emitted by execute orchestrator on the durable `pending_verify` transition) drives `runVerifyStreaming` → `runCommitAndPush` → `runOpenDraftPr` with durable `step.run` boundaries; status flows `pending_verify → verifying → pushed → pr_open`, four new events (`coding/task/cli-done`, `coding/task/verify-complete`, `coding/task/pushed`, `coding/task/pr-opened`). Schema: `coding_repos.identity_name TEXT NOT NULL DEFAULT 'default'` (selects which `github_identity:<name>` row in the secrets table this repo uses) + `verify_timeout_seconds INT NOT NULL DEFAULT 600` (wall-clock cap separate from the CLI's `task_wall_time_seconds`); `coding_tasks.pr_url TEXT` replaced with `coding_tasks.pr_metadata JSONB` carrying `{url, number, branchSha, openedAt}` validated by `PrMetadataSchema` — atomic-by-Zod, no half-recorded state. New `src/secrets/github.ts`: `GitHubIdentitySchema` validates `{pat, sshPrivateKey, sshPublicKey}` JSON bundles stored under `github_identity:<name>`; PAT and SSH key for a single bot account are inseparable so they live in one JSONB blob (CLAUDE.md atomic-cluster rule); `resolveGitHubIdentity` returns a `Result` discriminating `missing` / `malformed_json` / `schema_mismatch`. Per-repo override via `coding_repos.identity_name` does NOT fall back to `default` — typos fail loudly rather than authoring PRs under the wrong account. Setup wizard's GitHub step (interactive + non-interactive paths) prompts for the PAT, validates against `GET https://api.github.com/user`, generates an Ed25519 SSH signing keypair via `micro-key-producer/ssh.js` (Paul Miller, MIT, same noble family as `@noble/ciphers`/`@noble/hashes`) which returns OpenSSH-armored `privateKey`/`publicKey`/SHA256 `fingerprint` ready for `git config gpg.format=ssh -c user.signingkey=<path>`; prints the public key + `https://github.com/settings/ssh/new` link telling the operator to install it as a *signing key* (not authentication). Re-runnable: existing identity offers Keep / Replace PAT (re-uses key) / Regenerate. Non-interactive accepts `COGMO_GITHUB_PAT` (with `_FILE` variant) and optional `COGMO_GITHUB_SSH_PRIVATE_KEY` (currently warns and regenerates — extracting the public key from an arbitrary OpenSSH blob without ssh-keygen on the host needs a parser worth a follow-up). New `Transport.repos.cloneAndAdd({name, remoteUrl})` + Telegram `/repo add` FSM dialog (3 steps — name → remoteUrl → confirm) clones the remote into `${COGMO_REPOS_DIR}/${name}` using the default identity's PAT through a one-shot host-side `GIT_ASKPASS` helper; positional `/repo add <name> <path> <url>` form stays for scripting / pre-cloned repos. New `src/secrets/git-askpass.ts → withGitAskpass(pat, fn)` materialises a per-invocation tempdir with a 0700 helper script + 0600 secret file, runs `fn`, removes the tempdir unconditionally on return — even if `fn` throws; `runGit(args, env, opts)` spawns git with `GIT_TERMINAL_PROMPT=0` so missing auth fails fast. New `src/sandbox/askpass.ts → provisionAskpass({baseDir, rootTaskId, identity})` writes `helper`/`pat`/`signing-key`/`signing-key.pub` under `${SANDBOX_ASKPASS_DIR}/<rootTaskId>/` (default `/run/cogmo/askpass`), bind-mounted read-only at `/.cogmo-askpass/` inside the task container; `LocalInProcessSandbox.stopTask` calls `cleanupAskpass` in its `try/finally` so partial provisioning is recovered even when the cascade kill panics. Commit + push (`src/agent/coding/commit-push.ts`): `git status --porcelain` decides whether to commit (clean tree = idempotent retry-skip), commit via per-invocation `git -c gpg.format=ssh -c user.signingkey=<path> -c user.email=cogmo-bot@noreply -c user.name="Cogmo Bot" commit -S -m <goal>` (no global config), `git push origin cogmo/<idShort>`. Discriminated outcomes: `pushed` (with commit SHA via `rev-parse HEAD`), `nothing_to_commit`, `branch_conflict` (rejected / non-fast-forward), `auth_failed` (PAT/ssh deny), `failed`. Draft PR (`src/agent/coding/draft-pr.ts`): `octokit.pulls.create({draft:true})` from `@octokit/rest` v22; title = `task.goal` collapsed to one line + truncated 70 chars with ellipsis; body = `## Plan\n\n<plan>\n\n## Verify output\n\n` + fenced verify output + `_Generated by Cogmo_` (empty plan/output get `_(no plan recorded)_` / `_(no output)_` placeholders); discriminated by `RequestError.status` — 401/403 → `auth_failed`, 422 → `validation_failed` (PR already exists / head sha missing), other → `failed`. `parseRemoteUrl` accepts `git@host:owner/repo(.git)?`, `https://host/owner/repo(.git)?`, `ssh://...` and returns null for anything else. New runtime deps: `@octokit/rest@22` + `@octokit/request-error@7` + `micro-key-producer@^0.8.6` (drops the planned `gh` CLI from devbase image — one less binary, one less auth surface). Test coverage: 358 unit tests across the coding module covering each runner's discriminated outcomes + the orchestrator's status transitions + identity resolution + the `cloneAndAdd` clone-then-register path against a real bare git remote. Integration test (`verify-orchestrator.integration.test.ts`) chosen over real-GitHub-account testing per octokit team's recommendation: Gitea container via testcontainers exercises the real `git push` wire protocol; scoped fetch interceptor for octokit (mirroring `src/test/fal-mock.ts` — no `globalThis.fetch` patching, avoids the same SDK conflicts that ruled out MSW for fal); host-spawning fake sandbox runs `git`/`bash` on the host with askpass paths rewritten in-place; happy path asserts `pr_open` + branch in Gitea + correct octokit POST payload; failure path asserts `status=failed` + no branch pushed when verify exit≠0. 1330 unit + 2 integration tests pass. Design markers upgraded in `coding-delegation.md`: Flow / Git Identity / Repo Registry / Merge gate all `[confirmed]`. WIP-ref push on verify-fail deferred (p3 in todo) — orchestrator leaves a feature-flag hook (default off). |
| 2026-04-27 | Phase 6 P1 slice 3 — tool gate + Docker proxy + reaper + cgroup parent. Three new tables in migration 0012: `networks`, `volumes` (same lineage shape as `containers` — `parent_id` → containers, `root_task_id`, `depth`, `instance_id`, labels JSONB, ttl), and `coding_tool_decisions` (per-task permission decision log: `tool`, `pattern`, `decision`, `scope`). Pure in-process policy table (`src/agent/coding/policy.ts`) classifies tool calls under a default-broad-allow stance — file ops anywhere, read-only Bash, test/build/lint/format, package installs, local docker, in-container `rm` all auto-allow; narrow prompt set covers external state changes only (`git push`, `gh pr create/merge/review/close/edit/comment/ready/reopen`, `gh issue` mutations, `gh release/repo create/delete/edit`, `npm/pnpm/yarn publish | unpublish`, `cargo publish | yank`, `uv publish`, `twine upload | publish`, HTTP writes via `curl`/`wget` to non-localhost). Compound commands prompt if ANY sub-command is in the prompt set (worst-case wins). 90 unit tests cover happy paths, IPv6 localhost, quoted curl flags, weird input. `ClaudeCodeBackend.execute` drops `--permission-mode acceptEdits` and routes every tool call through Claude Code's stream-json control protocol — `control_request` events with `subtype: can_use_tool` arrive on stdout, the orchestrator writes `control_response` back on stdin. New `CodingExecuteHandle` returned by `execute(ctx, sessionId)` exposes `events: AsyncIterable<CodingEvent>` + `respondPermission(requestId, response): Promise<void>`. Handle is idempotent on duplicate respondPermission calls. New Docker socket proxy in `src/sandbox/proxy/` — `CogmoSocketProxy` listens on per-task Unix sockets at `/run/cogmo/sockets/<task-id>.sock`, classifies endpoints via pure `router.classify(method, path)` (deny `/swarm/*` `/plugins/*` `/nodes/*` wholesale, policy on `POST /containers/create`, hijack `/exec/{id}/start` `/containers/{id}/attach` `/events` `/build` `/session` `/containers/{id}/logs`, forward everything else), policy mutates the create body (deny `Privileged=true` / `NetworkMode=host` / host-path binds / dangerous caps SYS_ADMIN/NET_ADMIN/SYS_PTRACE/SYS_MODULE/SYS_BOOT/SYS_RAWIO/MAC_ADMIN/MAC_OVERRIDE; inject `Runtime` + `CgroupParent` + Cogmo labels). Single shared `http.Server`; one tiny `net.Server` per task socket tags accepted connections via WeakMap and hands them to the HTTP server. Hijacked / upgraded requests open a fresh upstream `net` connection, replay the request preamble, and pipe both directions raw (covers buildx `Upgrade: tcp` → HTTP/2 without speaking HTTP/2). 30 unit tests via two real Unix sockets (proxy task socket → fake upstream daemon socket). Supervisor wires the proxy into `LocalInProcessSandbox.createTaskContainer` — allocates a per-task socket BEFORE Docker createContainer (so the bind-mount path exists when Docker validates the spec), registers the scope with a placeholder `parentDockerId=""` first, then upserts with the real docker id once `createContainer` returns. `registerTask` is now an upsert that doesn't re-bind the socket on the second call, so connections in flight aren't disrupted. Bind appended to `HostConfig.Binds`. `stopTask` calls `proxy.unregisterTask`; `shutdown` calls `proxy.close`. Roll-back on Docker create failure or container.start failure unregisters the placeholder. Cgroup parent — every container in a task tree gets pinned under a shared `cogmo-task-<dashless-uuid>.slice` (systemd) cgroup parent. Slice 3.0h ships the slice naming + assignment infrastructure; aggregate-budget enforcement at the slice level (CPU/memory/pids quotas) is deferred until the spawn-many-children case becomes routine. Per-leaf limits via `NanoCpus`/`Memory`/`PidsLimit` already cap the task container directly. Tool gate orchestrator wiring: `runExecuteStreaming` now drives a real decision pipeline — build canonical pattern (`Bash(git push *)` etc.), replay task-scoped decision log (newest-first match wins), run `policy.evaluate` (allow short-circuits + persists scope=once for audit; deny short-circuits with reason), prompt path emits `coding/task/permission-requested` and blocks on `step.waitForEvent("coding/task/permission-decision")` filtered by `taskId + requestId`. New events: `coding/task/permission-requested`, `coding/task/permission-decision`. New `StepWaitForEvent` type derived from the SDK like `StepRun`. `transport.coding.respondPermission(params, tapperHandle)` — identity-checked Inngest event emit (no DB transition; the orchestrator's wait resumes on the event). New Telegram inline keyboard (Once / Task / Deny) with single-char wire codes (`o`/`t`/`d`) to fit Telegram's 64-byte callback_data limit alongside a full UUID taskId + 16-char requestId prefix; codes translated back on parse. New per-channel Inngest function listening on `coding/task/permission-requested` posts the keyboard message into the task's active Telegram session. Block-indefinitely on Telegram outage per slice3-plan #4 — wait timeout is 7 days (abandoned-task safety net), not deny-on-timeout. Reaper cron runs every minute and walks three passes per tick: TTL pass (current-instance containers with expired TTL still active, killed depth-DESC, marked `reaped`), orphan pass (Docker-side discovery via `listContainers({label=cogmo.managed=true})` — anything tagged with a dead/unknown `cogmo.instance` or with no DB row gets reaped), stale-DB pass (rows we still consider live but the daemon doesn't know about, marked `exited`). After containers, networks + volumes get a TTL pass. 1m cron interval (Inngest cron's minimum); slice3-plan + design/sandbox.md target 30s — close enough at personal scale. 16 reaper unit tests cover every pass (positive + negative), depth-DESC kill order, idempotency on re-tick, network/volume TTL, `docker.listContainers` failure mode. Sysbox + proxy-socket bind-mount compatibility verified on Fedora 43 via the karellen COPR sysbox-ce 0.7.0 build (no `/var/run` shadowing — sysbox's `sysbox-fs` only virtualises `/proc` and `/sys`); confirmed with `docker run --runtime=sysbox-runc -v /var/run/docker.sock:/var/run/docker.sock alpine sh -c "curl -s --unix-socket /var/run/docker.sock http://localhost/_ping"` returning `OK`. New env vars: `SANDBOX_PROXY_SOCKET_DIR` (default `/run/cogmo/sockets`), `SANDBOX_HOST_DOCKER_SOCKET` (default `/var/run/docker.sock`). 1155 unit tests pass (+~120 vs slice 2). Design markers upgraded: `sandbox.md` → Data Model / Proxy Design / Reaper / Lifecycle / Crash Recovery / Networks-Volumes-Images all `[confirmed]`; Resource Accounting → Hierarchical enforcement split (slice naming + assignment `[confirmed]`, aggregate-budget enforcement `[proposed]`); `coding-delegation.md` → Tool gate `[confirmed]`. |
| 2026-04-27 | Phase 6 P1 slice 2 — plan approval + execute (`awaiting_approval` → `pending_verify`). New `pending_verify` enum value (migration 0011) between `executing` and `verifying` — naming convention recorded: `awaiting_X` = human gate, `pending_X` = automatic transient. `ClaudeCodeBackend.execute(ctx, sessionId)` wraps `claude -p --resume <sid> --permission-mode acceptEdits` with the same JSONL parser + new `tool_use` / `tool_result` extraction from consolidated assistant/user messages (deduped against repeated blocks). New `CodingStreamingRegistry` — per-task in-process EventEmitter pub/sub bridging the durable orchestrator and turn-bound delivery; matches industry split (durable bus for state transitions, side-channel for token streams; Inngest's own Realtime is Redis-backed for the same reason). `Service.coding.delegate` becomes the **submit** path (insert task, emit `coding/task/start`, return `{taskId, status: "queued"}` immediately) — real LLM-facing API change vs slice 1; `DELEGATE_CODING_GUIDANCE` rewritten to tell the model to ack briefly and not speculate. Admission check (`repo.maxConcurrentTasks` + `countActiveTasksForRepo`) lands at the boundary now that multiple conversations could trigger concurrent tasks on the same repo. Plan-approval inline keyboard (Approve / Revise / Cancel) on the plan message, Telegram callback handler with **strict identity check** (resolves `callback.from.id` via `transportStore.resolveUser` and rejects strangers in the same chat with `identity_rejected`); atomic `approvePlanIfPending` / `cancelTaskIfActive` store methods make double-tap idempotent. Conversational revise (matches Cursor / Devin / Claude Code): cancels the task and posts a follow-up asking what should change; agent's next turn re-delegates. New `coding-task-execute` Inngest function triggered by `coding/task/plan-approved` reuses-or-recreates the task container (`sandbox.listContainersForTask` + `inspectContainer`; `--resume <sid>` rehydrates from the per-task home volume if the reaper got it during a long approval). Per-task progress message in Telegram (`CodingProgressSubscriber`): single message edited in place across plan + execute, header (phase emoji + truncated goal), status line (token counter, failure reason on terminal phases), last activity (executing only — `Read…`, `Edit ✓`), throttled at 500 ms; `plan_finalized` always force-edits to attach the keyboard without a stale interim post racing it; per-channel `telegram-coding-progress-<channelId>` Inngest function on `coding/task/start` resolves the chat via `transportStore.getActiveSessionsForConversation`. Token counts surface at `execute_complete` via the result event; `resource_usage` schema gains `cost_usd`; orchestrator translates camelCase `BackendUsage` → snake_case `ResourceUsage` at the persistence boundary. New events: `coding/task/plan-approved`. New `Transport.coding.{approvePlan, cancelTask}` namespace; new `TransportError` codes (`task_not_found`, `task_already_approved`, `task_not_pending_approval`, `task_already_terminal`). Decisions recorded: in-process EventEmitter beats Redis/Inngest for token streams (single-node deployment); retries=0 stays on both orchestrator functions (file edits + CLI session non-replayable mid-stream); `pending_verify` chosen over `cli_done`/`executed`/`awaiting_verify` for naming-convention symmetry with `awaiting_approval`. Design markers upgraded: `coding-delegation.md` → Backend Interface (plan + execute) / Container Lifecycle / Resume procedure / Plan gate all `[confirmed]`. |
| 2026-04-26 | Phase 6 P1 slice 1 — autonomous coding plan-only end-to-end. New module `src/sandbox/` with the supervisor/runtime/store split: `containers` + `cogmo_instances` tables (schema 0009), `LocalInProcessSandbox` driving the host Docker daemon via `dockerode@5`, sysbox-runc by default with `SANDBOX_RUNTIME` env enforcing explicit selection (no silent fallback to runc), Docker-label lineage (`cogmo.{managed,instance,root_task,parent,depth}`), boot-time crash recovery that reaps containers from prior `cogmo.instance` ids, and `getTaskContainer(dockerId)` factory for re-deriving handles across `step.run` boundaries. New module `src/agent/coding/` with `coding_repos` + `coding_tasks` tables (schema 0010 + 0011 adding `conversation_id`), `ClaudeCodeBackend.plan()` wrapping `claude -p --permission-mode plan` with stream-json parsing (Zod-narrowed `system/init` for session_id, `stream_event content_block_delta` for text deltas, `result` for usage + plan_ready emission), idempotent `git worktree add` reconcile, an orchestrator with durable `step.run` boundaries (`set-status-planning`, `allocate-worktree`, `create-container`, `persist-plan`, `set-status-failed`, `teardown`) using `GetStepTools<typeof inngest>["run"]` derived from the SDK so the Jsonify<T> shape is preserved without re-typing, and a `delegate_coding` agent tool surfaced via the new `Service.coding` namespace that runs the orchestrator inline (the durable Inngest function will be wired in slice 2 when approval needs `step.waitForEvent`). New `cogmo/devbase:slice1` image based on `mcr.microsoft.com/devcontainers/base:dev-ubuntu24.04` with Node 24 + pnpm + claude-code 2.1.119 pinned, published from `images/devbase/Dockerfile` via `.github/workflows/devbase.yml`. New `.github/workflows/sysbox-e2e.yml` installs sysbox 0.7.0 via `.deb` on `ubuntu-24.04` and runs the supervisor's sysbox-flavoured tests (asserts `Runtime: sysbox-runc` on inspect and that `/proc/self/uid_map` shows host-uid remapping for inner uid 0 — proves userns isolation is actually in effect). Bug fixes caught during integration testing: `exec.wait()` originally attached `'end'` listeners on demand which deadlocked any caller that read stdout to completion before calling wait — fixed by capturing the exit signal eagerly when the exec starts; `containerCreated` flag was set after the DB write so a failed `setTaskContainerId` skipped cleanup — now stamped before the write so the orchestrator's catch always tears the container down. Bumped unit `hookTimeout` to 30s — pushSchema contention with 6 parallel PGlite test files flaked the default 10s. Slice 1 deferred to later slices: Docker socket proxy + sibling container support + reaper cron + cgroup parent (slice 3 when execute-mode tool calls can spawn children), plan approval Telegram keyboard (slice 2), tool gate via stream-json (slice 3), verify + push + draft PR (slice 4), `/repo` control commands (slice 1.0j alongside `/repo add` FSM), GitHub PAT secret kind + GIT_ASKPASS host helper (slice 1.0i). Design markers upgraded: `sandbox.md` → Runtime Selection / Data Model (slice-1 tables) / Label Schema / Crash Recovery / Module Structure all `[confirmed]`; `coding-delegation.md` → Backend Interface (plan only) / Task Model `[confirmed]`. |
| 2026-04-17 | Design drafted: autonomous coding + container sandbox. Two new design docs — [design/sandbox.md](design/sandbox.md) (container sandbox infrastructure — `sysbox-runc` as default runtime on Ubuntu 22.04+, Docker socket proxy pattern for lineage + policy at the Docker API level, `containers` + `cogmo_instances` + `networks` + `volumes` tables with `parent_id` / `root_task_id` / `depth` denormalized at insert, per-task systemd slice as `CgroupParent` for kernel-enforced hierarchical resource ceiling, reaper cron at 30s for TTL + orphan + stale-row passes, raw bidirectional socket pipe for hijacked endpoints + buildx `/session` so transparent buildx works in P1 without the moby/buildkit SDK, cache volume scoping split by integrity story — global for npm/cargo/go/apt, per-repo for pip + build caches; P2 sidecar extraction via single-image / two-subcommand topology with tRPC `SandboxRouter` control plane, transport deployer-configurable between unix socket and TCP) and [design/coding-delegation.md](design/coding-delegation.md) (Claude Code / Codex CLI subprocess delegation using the user's Max/Pro subscription — no API keys, no Agent SDK; `coding_tasks` + `coding_repos` tables; one container per task with worktree bind-mounted at `/workspace`, per-task home volume `cogmo-task-home-<task-id>` for CLI session files so resume works after idle teardown; worktree persistence via git remote — clean remove on success, `refs/cogmo-wip/<task-id>` push for dirty teardown, no S3 / tarballs default; three autonomy gates — plan (user-only, automated triggers skip), tool (stream-json bidirectional, not PreToolUse hooks — hook timeouts can't cover user-away gaps), merge (draft PR only, never merges); Prompt Construction section with self-verify clause retiring the Cogmo-side retry loop in favour of CLI-internal iteration + budget caps + single post-hoc verify; Concurrency section per-repo-serial default with install-lock relaxation as P3; Admission & Rate Limiting section for automated `trigger_source` — global cap, per-source daily quota, failure backoff with exponential pause, user-priority FIFO scheduler on Inngest cron; Inngest step boundaries table mirroring `handle-message` durable-around-streaming pattern; P1 disk-based credential delivery via `GIT_ASKPASS` with short-lived PATs on a bot account, SSH commit signing via `git config gpg.format ssh`, vault socket + GitHub App installation tokens deferred to P2; three-tier Implementation Phases P1/P2/P3). CLAUDE.md design-doc index updated with both entries. No code changes — design only. |
| 2026-04-16 | Telegram session/profile/model management. Six new Telegram commands: `/sessions` (inline keyboard ≤10 convs, numbered text >10), `/resume <alias>`, `/name <alias>` (`-` clears), `/end`, `/profile [list\|switch\|new\|edit\|delete]`, `/model [<model>]`, plus `/cancel` for mid-dialog abort. Interactive multi-turn FSM for `/profile new|edit` (prompt → model → confirm; `skip` keeps current in edit mode; in-memory per chat — restart-mid-dialog loses draft, deliberate v0 tradeoff). `bot.on("message:text")` intercepts dialog input at the top so free-form replies during the FSM never reach the agent (regression-tested). Schema: `profiles.user_id UUID NULL FK → users` + `UNIQUE(user_id, name) NULLS NOT DISTINCT` (existing rows → org profiles visible to all, read-only via Transport); `model_providers.user_selectable BOOLEAN NOT NULL` (gates `/model` picker and `profiles.update({model})` validation); `messages.profile_id UUID NOT NULL FK → profiles` (backfilled from `conversations.profile_id`); `messages.model TEXT NOT NULL` with `'<legacy>'` sentinel backfill (no DEFAULT — every insert stamps explicitly); new `aliases` table with `UNIQUE(user_id, alias)`. Per-turn snapshot stamping in `handle-message.ts`: new `load-turn-snapshot` step reads `conv.profile_id` + `profile.model` once at turn-start, stamps every message produced by the turn (user batch + intermediate + final assistant) — mid-turn `/profile switch` or `profile.model` edits take effect on the *next* turn. `TransportError` extended with 7 codes (`profile_not_found` / `profile_in_use` / `profile_name_taken` / `model_unavailable` / `alias_taken` / `operation_not_permitted` / `access_denied`). New Transport methods: top-level `resumeConversation(address, handle, {alias\|conversationId})`; namespaced `conversations.{list, getCurrent, setAlias, setProfile}`, `profiles.{list, create, update, delete}`, `models.list`. All profile mutations enforce caller-owns ACL — org profiles (`user_id IS NULL`) always reject with `access_denied`; org-level CRUD happens out-of-band via psql/wizard. New `UniqueViolationError` + `translateUniqueViolation` helper walks Drizzle's `DrizzleQueryError.cause` to surface Postgres 23505 as typed errors, which Transport maps to `alias_taken` / `profile_name_taken`. `AgentStore.getProfile` broadened to return full `Profile` (incl. `userId`, `name`). New files: `src/transport/adapters/telegram/{commands,sessions-ux,profile-dialog}.ts` + tests; `src/agent/store/errors.ts`. Wizard + non-interactive seed + e2e-setup all pass `user_selectable: true` on `model_providers` inserts. Migration `0007_odd_true_believers.sql` with 3-phase ADD→backfill→SET NOT NULL. 72 new unit tests (schema, admin ACL, session UX, command handlers, FSM). Docs already updated in prior iteration (`design/transport/{adapters,telegram,sessions,overview}.md`, `design/providers.md`, `design/data-model.md`). |
| 2026-04-16 | Context fast path — account for output tokens in `shouldSkipCounting`. The prior estimator summed only `lastInputTokens + newContentEstimate`, but the starting input for turn `N+1` is `turn N input + turn N output + new user content` (the assistant's reply is persisted into history). One response worth of tokens was unaccounted for — enough to slip past the 50% threshold and skip counting when the conversation was actually close to the limit. Fix: new `messages.output_tokens` column (`NOT NULL`, no default), backfilled with `-1` ("unknown, force count") for pre-migration rows via `migrations/0008_needy_ultimo.sql` (add nullable → UPDATE → SET NOT NULL pattern, safe on tables with existing data). `insertMessages` now requires `lastMessageOutputTokens` — written to the final row only; intermediate tool turns and the singular `insertMessage` user-row path store the `-1` sentinel (harmless — the fast path only reads the last assistant row). `getLastInputTokens` renamed to `getLastTokens`, returns `{ inputTokens, outputTokens } \| null`. Estimator: `lastIn + lastOut + ceil(newChars/4) < budget * 0.5`, with any negative/null on either field forcing a real count. 0.5 threshold unchanged — prior ambiguity was accidental lenience, not a design margin; adding the output term tightens the estimate correctly. New tests cover: both counts round-trip through the store, fast path returns false when `N+M+newChars/4` clears 50% but `N+newChars/4` alone wouldn't, boundary at exactly `budget * 0.5` (strict `<`), sentinel `-1` on either value forces count. |
| 2026-04-16 | OpenTelemetry export — opt-in via standard `OTEL_EXPORTER_OTLP_ENDPOINT`. SDK is loaded as a Node `--import` hook (`dist/otel.js`) so instrumentation patches before any module resolves; with the env var unset the hook early-returns and ~10 MB of SDK + instrumentations never load. Hand-picked instrumentations only: `instrumentation-http`, `-undici` (covers Anthropic/OpenAI SDK fetches and Hindsight/Tavily/Telegram), `-pino` (auto-injects `trace_id`/`span_id` into log records). Skips the meta `auto-instrumentations-node` package and skips pg tracing (no maintained `postgres.js` integration; query overhead would dwarf span cost at our point-read pattern). Inngest's engine opens `inngest.execution` per function run via the global tracer — our domain spans parent under it via standard OTel context, no Inngest OTel middleware required. (`extendedTracesMiddleware` would also install an `InngestSpanProcessor`, but that one only activates when the function run is started with a W3C `traceparent` on the request — which would require propagating traceparent through event payloads, deferred.) Domain spans: `chat` with GenAI semantic conventions (`gen_ai.provider.name`, `request.model`, `usage.input_tokens`/`output_tokens`/`cache_read.input_tokens`/`cache_creation.input_tokens`, `response.finish_reasons`) emitted from both `AnthropicProvider` and `OpenAICompatibleProvider`, with try/finally lifecycle that handles consumer-abandoned streams; `tool.execute` with `cogmo.tool.name`/`cogmo.tool.error`; `memory.recall`/`memory.retain` with `memory.hit`/`memory.count`. Metrics surface kept narrow — `cogmo.llm.tokens` counter (labels `type`/`model`/`provider`), `cogmo.agent.iterations` histogram, `cogmo.debounce.wait_ms` histogram. Span-derivable metrics (LLM/tool latency, error rate, recall hit rate) intentionally not duplicated as counters. OTLP/protobuf over HTTP — works with Grafana LGTM all-in-one (default local recommendation), SigNoz, and Grafana Cloud without a Collector. New test harness (`src/test/otel-harness.ts`) registers in-memory trace + metric exporters before bootstrap, with a metrics-cache reset hook (`__resetMetricsForTests`) to work around `metrics.getMeter()` lacking a proxy. 4 unit tests (gen_ai attrs, cache tokens, counter labels, error span) + 1 integration test asserting span shape (`inngest.execution` parent + `chat` children) and metric labels through the live pipeline. DEPLOYMENT.md gains an Observability section with LGTM, SigNoz, and Grafana Cloud examples + sampling guidance, and a "Cross-function-run correlation" note explaining the current one-trace-per-run boundary. Testcontainers e2e image build opts into BuildKit via `.withBuildkit()` so `pnpm test:e2e` works locally without requiring a pre-built image. |
| 2026-04-16 | Dependency cleanup — removed `@testcontainers/ollama` (all test/dev setups moved to llmock via `hindsightSlim()`; `ollama()`/`pullModel()`/`OLLAMA_MODEL` factories and the `"ollama"` branch of `hindsight()` were dead code). `hindsight()` signature narrowed: drops `llmProvider` arg, now takes `{ apiKey, baseUrl? }` directly. Also dropped five never-imported packages from `package.json`: `superjson`, `ts-pattern`, `p-limit`, `msw`, `telegram-test-api` — all were aspirational entries that never got wired up (`design/tooling.md` still documents them as the approved pick when the need arises). |
| 2026-04-15 | Test + dev infrastructure switched to `hindsight-api` / `hindsight-api-slim` (API-only variants). `hindsightSlim()` pinned to `ghcr.io/vectorize-io/hindsight-api:0.5.1-slim` (closest-patch replacement for the retired `hindsight:0.5.0-slim`); `hindsight()` uses `ghcr.io/vectorize-io/hindsight-api:latest`. Drops the Control Plane web UI that Cogmo never talks to (~100 MB saving on slim, one fewer service per test run). Verified against Hindsight's `docker/standalone` Dockerfile + `start-all.sh`: API and Control Plane are independent processes, Alembic migrations run in the API, so CP absence is safe. Production guidance in DEPLOYMENT.md is intentionally unchanged — end users may still want CP for memory inspection. |
| 2026-04-15 | Non-interactive setup + post-setup verification hints. `cogmo setup --non-interactive` now reads `COGMO_LLM_PROVIDER_TYPE` / `COGMO_LLM_API_KEY` / `COGMO_LLM_BASE_URL` / `COGMO_TELEGRAM_BOT_TOKEN` / `COGMO_TELEGRAM_ALLOWED_USERS` / `COGMO_TAVILY_API_KEY` / `COGMO_FAL_API_KEY` (all secret-bearing vars support the `_FILE` Docker convention). Validation matches the interactive wizard (`/v1/models`, `getMe()`, Tavily search ping) — every credential is validated *before* any DB write, so a failure leaves the DB untouched. Idempotent re-runs replace existing same-name providers and Telegram channels. New `src/setup/non-interactive.ts` (env parsing via Zod + injectable validators → stores) and `src/setup/reset.ts` (extracted from `runSetup` for direct testing). Interactive wizard gains a concrete "How do I know it's working?" post-setup block with the configured bot username. 23 new unit tests — env parsing, Zod refinement edges, `_FILE` round-trip, every validation-failure path asserts zero DB rows, reset scopes. Validators injected at the boundary (no `vi.mock` of fetch). Design doc upgraded to `[confirmed]`. |
| 2026-04-15 | Health endpoint + docs tidy. `GET /health` on port 9090 returning `application/health+json` per the IETF draft (`status`, `version`, `releaseId`, `description`, `notes`). Liveness-only — no dependency checks, a Postgres blip must not restart the container. `createHealthServer()` / `startHealthServer()` split so tests bind to ephemeral ports. Binds to `0.0.0.0:9090` unconditionally: exposure and port remapping are deployment concerns (Docker `-p`, k8s container spec, host firewall) — the app just listens. `EXPOSE 9090` in Dockerfile for metadata. Version stamped from the existing `VERSION` build arg; optional `GIT_SHA` for release ID. Dropped `v0.md` — the single `[ ]` item (`generate_image`) shipped in v1.16.0, so the tracker served its purpose. PROGRESS.md + todo.md synced with current state. |
| 2026-04-15 | Image generation — integration test + fal-mock scaffolding. Scoped `createFalFetch()` (record/replay) passed via `BootstrapOptions.falFetchOverride` → `createFal({ fetch })`, not MSW. Scoped fetch injection avoids MSW's `onUnhandledRequest: "bypass"` mangling Anthropic streaming auth headers through llmock. Fixture key = `{model-slug}-{sha256(model:prompt:size:seed):12}`, mock CDN URL `fal.media-mock.test/{key}.{ext}` rewritten in record-time response so replay is deterministic. Hand-written llmock fixture uses `match.sequenceIndex` to disambiguate iterations (llmock's Anthropic conversion keeps the original user message as "last user" through tool turns — otherwise fixtures match forever). `directOutbound` captured via a test-local Inngest function. Integration + record workflow documented in `design/image-generation.md`. |
| 2026-04-14 | Image generation — `generate_image` tool via Vercel AI SDK (`ai` 6.x + `@ai-sdk/fal`), fal.ai as first provider. Curated `MODEL_CATALOG` enum (Flux Dev/Schnell/Pro/Pro-Ultra/Kontext), LLM picks per-call from tool description guidance. Graceful degradation when `FAL_API_KEY` missing. 4xx classification via `APICallError.isRetryable` (SDK-structured, not substring). `AttachmentStore.upload()` gains optional `prefix` param — generated images stored under `generated/`. Mid-stream delivery: Telegram stream handle parses `tool_result` for `generate_image`, downloads bytes, `sendPhoto` during the loop (dedup on `runId + path`, marked only after successful send so transient failures don't block retries); placeholder stripped after delivery. Batch path for non-streaming channels: `RenderedMessage.images`, `DeliveryHandle.deliverBatch(content, images?)` + `hasBatchTargets()` gate to skip S3 downloads on streaming-only setups, pure `extractGeneratedImages()` helper scoped by `toolUseId → toolName` to avoid false positives, wrapped in `step.run("batch-delivery")` for exactly-once on retry + Inngest observability (small `{delivered, failed}` return; image bytes flow through body in memory only). `Promise.allSettled` per-image resilience. `AdapterDeps` gains `attachments`. `isRenderedMessage` type guard shared by Telegram + Direct adapters. Direct adapter `directOutbound` event extended with base64 images. Wizard prompts for `fal_api_key` (no live validation — no cheap ping endpoint). `generate_image` added to default `tool_set`. Design doc: [design/image-generation.md](design/image-generation.md) with rationale for AI SDK (images) vs raw SDK (text LLMs, blocked on `countTokens` PR #12176 + thinking signature bug #11602). `design/crash-recovery.md` durability map updated. 34 new unit tests across extract-images, image-tools (incl. abort classification), telegram stream handle (incl. failed-sendPhoto retry + placeholder stripping), delivery-router (incl. `hasBatchTargets`), orchestrator. |
| 2026-04-14 | Telegram response formatting + channel-scoped steering rules. `marked` v18 + custom HTML post-processor (`renderTelegramHtml`) converts agent markdown to Telegram's HTML subset. OpenClaw-informed edge cases: table→`<pre>` wrapping, emoji+bold preprocessing, native `<blockquote>`. `renderOutput` hook on `AdapterModule` — adapter-level pure function, DeliveryRouter calls per-session. Streaming: plain text during stream, HTML on final edit, 400 fallback. `steering_rules.channel_type` nullable column for channel-scoped behavioral rules. `PromptSource.assemble` takes context object `{ profileId, channelTypes }`. `getActiveChannelTypes` on TransportStore (joins sessions→channels). Default Telegram rules seeded at channel setup. `RenderedMessage` type, `AdapterEntry` in DeliveryRouter. Telegram adapter restructured from single file to directory module. |
| 2026-04-13 | Memory networks + auto-recall gate. Four memory networks (world, bank, opinion, observation) implemented as Hindsight tags (`network:<type>`). Observer now extracts facts from conversation transcripts via `chatTyped()` with Zod schema, classifies into networks, and retains to Hindsight with `observation_scopes: "per_tag"` for per-network consolidation. New `retainBatch` on `MemoryProvider` interface. `memory_retain` tool defaults to `network:world`. Auto-recall gated by profile-level `auto_recall` column (`off/always/heuristic/llm`, default `heuristic`). Heuristic gate skips greetings, acks, and continuations — pure function, zero latency. LLM gate stubbed for future use. `tagsMatch` added to `RecallOptions`/`ReflectOptions` and passed through Hindsight adapter. Migration: `profiles.auto_recall` column. |
| 2026-04-12 | Guided setup + encrypted secrets + multi-provider LLM. New `src/secrets/` module: AES-256-GCM encryption via `@noble/ciphers` (Cure53-audited), HKDF-SHA256 key derivation via `@noble/hashes`, `secrets` table for encrypted credential storage, `ConfigResolver` (DB-first env-fallback), `_FILE` Docker secrets convention, `gen-key` CLI subcommand. New `src/setup/` module: interactive wizard (`cogmo setup`) via `@clack/prompts` — configures LLM provider (Anthropic/OpenRouter/OpenAI/custom with validation), Telegram channel + allowlist, optional web tool keys. Re-runnable (Keep/Modify/Skip per step), `--reset` scopes, `--non-interactive` mode. Multi-provider LLM: `llm_providers` table + `model_providers` routing table (position-based priority, `UNIQUE(model, position)` prevents ties). Provider dispatch in bootstrap resolves model → best provider → decrypts key → constructs adapter. `ANTHROPIC_API_KEY` removed from env (DB-only, no backward compat). Identity resolution wired in transport layer: `createConversation` calls `resolveUser`, Telegram adapter rejects unauthorized users via `user_identities` rows. Channel CRUD: `createIdentity`, `updateChannelCredentials`, `removeChannel` (cascading FK delete). Seed refactored to `src/setup/seed.ts` with named exports. `.dockerignore` switched to deny-all/allow-list. `profiles.summarization_model` replaces `SUMMARIZATION_MODEL` env var. Design docs: `design/providers.md`, `design/setup.md`, expanded `design/infrastructure.md` Secrets section. |
| 2026-04-12 | Stage 1 evolution — Observer Inngest function (`src/agent/evolution/observer.ts`) triggered by `conversation/idle`. Extracts behavioral corrections from conversation transcripts via `chatTyped()` with Zod-validated structured output. Corrections persisted to `steeringRules` table with graduation model: `observationCount >= 2` promotes from learning (inactive) to rule (active, injected into system prompt). LLM-based semantic dedup compares new corrections against existing rules (new/reinforce/contradiction). Consolidation at 30+ active rules merges similar entries. Formatted readable transcript (tool calls as `[Tool: name(input)] → result`, images/thinking stripped). Global scope (industry standard). Safety: contradictions logged but not applied, safety-category excluded from extraction. New store methods: `getCorrections`, `upsertCorrection`, `countActiveRules`, `replaceRules`. 33 new unit tests across 4 test files. |
| 2026-04-12 | Persist full tool invocation history — `messages.content` now stores `ContentBlock[]` (text, tool_use, tool_result, image, thinking) instead of collapsing to final text. `AgentLoopResult.newMessages` exposes all messages produced per invocation (intermediate tool turns + final assistant). Orchestrator persists all of them in a single Inngest step. Pair-aware compaction (`snapToPairBoundary`) ensures summarize/truncate never orphan a tool_use/tool_result pair. Store interface tightened: `insertMessage` accepts `string \| ContentBlock[]`, `getHistory` returns `ReadonlyArray<Message>`. Prerequisite for Stage 1 correction extraction (needs to see tool choices, not just text). |
| 2026-04-11 | Crash recovery contract — summarization LLM call inside `compactMessages` wrapped in `step.run("summarize-prefix", ...)` so it's exactly-once on Inngest retry. The compaction orchestration (token counting, threshold decisions) deliberately stays non-durable: caching it would freeze the decision against stale `memory.recall` / profile inputs and force base64 image payloads into Inngest state. Replay regression test (`handle-message.replay.test.ts`) using `@inngest/test`'s `steps:` mechanism — proves cached durable steps don't re-execute their bodies (user/assistant inserts, summarization), and that the streaming section intentionally re-runs (canary for the non-durable contract). New `design/crash-recovery.md` documents the full durability map of `handle-message`, per-tool re-execution table, streaming dedup, and process-death edge case. `agents.md` Crash Recovery section now points to it. |
| 2026-04-11 | Project renamed `ai-assistant` → `cogmo`. `package.json` name, Inngest app id, default Postgres user/db (`cogmo`), default S3 bucket (`cogmo-files`), CI image tag, GHCR repo (auto via `${{ github.repository }}`), README/CLAUDE titles + badges, log messages, Telegram welcome. LLM message role `"assistant"` (Anthropic/OpenAI protocol literal) and the default seed profile name `"assistant"` (role descriptor, not brand) untouched. |
| 2026-04-10 | Hindsight upgrade to 0.5.0 — pinned `ghcr.io/vectorize-io/hindsight:0.5.0-slim`. Bumped `@vectorize-io/hindsight-client` to 0.5.0. New 3-phase retain pipeline (PR #722) is sync by default; `HindsightMemoryProvider.retain()` now passes `async: true` so the agent doesn't block on extraction. Bank creation moved from POST to PUT (handled by client). Test fixtures re-recorded against v0.5.0 with `gpt-4o-mini` (gpt-5-nano blocked on Hindsight emitting `max_completion_tokens` — see `design/memory.md` → Known Gaps). `requestTransform` extended to normalize test bank IDs. |
| 2026-04-09 | LLM capability — `ThinkingBlock` in ContentBlock union with `thinking` config on `ChatParams`. Anthropic: native extended thinking, streaming accumulation, history replay. `clearOldThinking()` pre-pass in agent loop replaces old thinking content with empty (preserves block structure, frees tokens). `ResponseFormat` on `ChatParams` for structured JSON output — Anthropic via tool_use trick (synthetic tool + `tool_choice`), OpenAI via native `response_format`. Mutually exclusive with `tools`. `chatTyped<T>()` utility in `src/llm/typed.ts` — Zod schema → `responseFormat`, parse + validate response, retry with error feedback injection (default 2 retries). 284 unit tests. |
| 2026-04-09 | Response source routing — DeliveryRouter resolves targets via inbound message cursor chain instead of broadcasting to all sessions. `getSourceSessions()` + `getReceiveAllSessions()` on TransportStore. Private conversations merge receive-all observers; non-private forced to source only. Design doc upgraded to `[confirmed]`. |
| 2026-04-09 | Context window management — `countTokens()` on LlmProvider (Anthropic native API, js-tiktoken for OpenAI-compatible). Model registry with budget computation. Three-layer compaction pipeline: clear old tool results (60%), summarize prefix (80%), truncate (95%). Fast-path skips counting when under 50% budget. `inputTokens` persisted on assistant messages. Status events for user feedback during summarization. |
| 2026-04-08 | Debounce wiring — `debounce-router`, `debounce-idle`, `debounce-maxwait` Inngest functions. Entry guards (staleness, await_input). Resume policy (flush/debounce/await_input). Per-conversation concurrency lock. |
| 2026-04-08 | Session lifecycle — Inngest idle timer closes sessions after configurable timeout. `resolveSession` staleness safety net. `/new` command closes current session and creates fresh conversation. |
| 2026-04-07 | Prompt caching — Anthropic `cache_control` on system blocks and tool definitions. OpenRouter passthrough via `cache_control` extension. Reduces input token costs on multi-turn conversations. |
| 2026-04-07 | Switch from llmock fork to official `@copilotkit/aimock` 1.8.0 — all upstream PRs merged (#57, #58, #63, #64). |
| 2026-04-05 | AttachmentStore — separate binary storage from text workspace (Service.files). S3-backed upload/download with proper MIME types. Telegram photo handler stores raw bytes. |
| 2026-04-05 | ImageBlock — canonical type in ContentBlock union. Both Anthropic (base64/URL source) and OpenAI (image_url/data URI) adapters. Telegram `message:photo` handler downloads, stores in S3, orchestrator resolves before LLM. `contentToBlocks()` parser with ImageRef pattern. |
| 2026-04-05 | OpenAI-compatible LLM adapter — `OpenAICompatibleProvider` using official OpenAI SDK. Covers OpenAI, xAI (Grok), OpenRouter, DeepSeek, Groq, Together. Chat + streaming + tool calling. |
| 2026-04-05 | Core memory blocks — `core_memory_blocks` DB table (user_id + key, upsert). `core_memory_update`/`core_memory_read` tools via Service.coreMemory. `getUserContext` callback reads blocks into system prompt. Conditional onboarding (shown only when empty). |
| 2026-04-05 | Auto-recall — before each agent turn, embed user message via Hindsight recall, inject top results as `# Recalled Context` in system prompt. Agent still has explicit `memory_recall` for deeper searches. |
| 2026-04-05 | System prompt overhaul — auto-generated Tools section from tool registry (adding a tool updates the prompt). Service guidance per namespace (MEMORY, FILES, CORE_MEMORY). Conditional onboarding. Identity prompt inspired by OpenClaw SOUL.md. |
| 2026-04-05 | V0 tools — `web_search` (Tavily), `web_answer` (Perplexity Sonar via OpenRouter), `fetch_url` (@mozilla/readability + SSRF protection), `read_file`/`write_file`/`list_files` (S3/MinIO). Enhanced `get_current_time` (structured JSON, timezone, day of week). |
| 2026-04-05 | MinIO container in dev-infra and test setups — S3-compatible object storage for file tools and attachments. |
| 2026-04-01 | Streaming design doc (`design/transport/streaming.md`) — DeliveryRouter, StreamingAdapter, StreamHandle, agent loop streaming, Telegram throttled edits, Inngest Realtime research. |
| 2026-04-01 | Streaming implementation — unified DeliveryRouter replaces per-channel respond functions. `chatStream()` on LlmProvider with ChatStreamResult. `runStreamingAgentLoop()` with onEvent callback. TelegramStreamHandle with 500ms throttled edits. LLM call outside step.run() for real-time streaming. `response/ready` demoted to notification. |
| 2026-04-01 | V0 milestone tracking — `v0.md` created with Infrastructure, LLM Provider, Tools, Memory, Prompts sections. Research: OpenClaw, Letta, memU, Nanobot, ZeroClaw, Grip AI. |
| 2026-04-01 | llmock recording enabled for e2e tests — pass real API keys through, add `test:record:e2e` script. |
| 2026-03-31 | CI: Codecov coverage reporting, JUnit test reports via `dorny/test-reporter`, `checks:write` permissions. `@vitest/coverage-v8` for v8 coverage provider. |
| 2026-03-31 | Docker-based e2e — builds from Dockerfile, runs app as container (tests real production artifact). Pre-built image in CI via `docker/build-push-action` with GHA cache. `E2E_IMAGE` env var for pre-built images. |
| 2026-03-31 | Rename `cli.ts` → `main.ts`, add to tsup entry points. Fix distroless ENTRYPOINT (`/nodejs/bin/node`). |
| 2026-03-31 | llmock `requestTransform` — strips timestamps/UUIDs for deterministic fixture matching. Exact match (`===`) when transform set. Shared setup in `test/llmock-setup.ts`. Record mode via `LLMOCK_RECORD=1` + `pnpm test:record`. |
| 2026-03-31 | Slim Hindsight image (`latest-slim`, ~500MB) — external LLM + embeddings via llmock, RRF reranker. No PyTorch, no model downloads. `hindsightSlim()` container factory with zerank support (patched config for base URL). |
| 2026-03-31 | Hindsight provider config documented in `design/memory.md` — production (OpenRouter gpt-5-nano + qwen3-embedding + zerank-2, ~$6/mo) vs test (slim + llmock replay). |
| 2026-03-31 | llmock installed from patched fork (`iskhakovt/llmock#patched`) — 4 fixes: URL path prefix, header forwarding, requestTransform, base64 embedding decoding. PRs: CopilotKit/llmock#57, #58, #63, #64. |
| 2026-03-29 | llmock replaces mock-anthropic + Ollama — `@copilotkit/llmock` 1.6.0 serves Anthropic API + OpenAI-compatible endpoints from fixtures. Single in-process instance backs both our app and Hindsight. Deleted `test/mock-anthropic/`. E2e drops from ~5min to ~1min. |
| 2026-03-29 | Integration tests (in-process) — `bootstrap()` extracted from `src/index.ts`, called directly in test workers. Env injection via `process.env` in globalSetup (propagates to Vitest workers). Pipeline + memory tests moved from e2e. E2e slimmed to boot smoke test. |
| 2026-03-29 | Three-tier test structure — unit (PGlite, mocks), integration (Docker + in-process), e2e (Docker + subprocess). Renamed `*.integration.test.ts` → `*.e2e.test.ts`. Moved `test/containers.ts` → `dev/containers.ts`. |
| 2026-03-29 | Store unit tests with PGlite — `@electric-sql/pglite` 0.4.2 (PG17 in WASM). `pushSchema` from `drizzle-kit/api` applies schema without migration files. `pg_uuidv7` extension + `uuidv7()` alias. 31 tests across 2 store files. Driver-agnostic `Database` type (`PgDatabase<PgQueryResultHKT>`) eliminates `as any` casts. |
| 2026-03-29 | Replace `pg` driver with `postgres.js` — pure JS, faster, modern. Swap `drizzle-orm/node-postgres` → `drizzle-orm/postgres-js` across 5 files. Zero API changes needed — Drizzle's connection pattern and `$client.end()` work identically. |
| 2026-03-29 | CLI entrypoint (`src/cli.ts`) — `serve` and `seed` commands. Seed script (`src/seed.ts`) extracted from app startup, runs independently with only `DATABASE_URL`. Dockerfile uses `ENTRYPOINT ["node", "dist/cli.js"]`. App fails fast if seed hasn't run. |
| 2026-03-29 | Channel registry — table-driven adapter discovery. `AdapterModule` contract (`channelType` + `setup()`) with `satisfies` barrel enforcement. Registry reads `channels` table, starts matching adapters, creates generic respond functions. No per-adapter respond files. |
| 2026-03-29 | Direct channel adapter — replaces old CLI adapter. Event-driven via Inngest (`adapter/direct/inbound`, `adapter/direct/outbound`). Console script (`scripts/console.ts`) for dev interaction. No in-process stdin/stdout. |
| 2026-03-29 | Code quality — shared test factories (`src/test/factories.ts`), `createRespond()` shared factory, `contentToText()` helper, neverthrow `Result<T, TransportError>` at Transport boundary, `#private` fields on all classes, `private constructor` + `static async create()`. 84 unit tests, 3 integration tests. |
| 2026-03-29 | Structural alignment — code matches design docs. Renamed `src/channels/` → `src/transport/`. Store pattern (`agent/store/`, `transport/store/`) with interfaces + Drizzle implementations. New schemas: channels, channel_sessions, inbound_messages, user_identities. Deleted: chats, deliveries, old enums. New events: `inbound/arrived`, `response/ready`. Handle-message receives IDs not raw content. 9 tables, fresh migration. |
| 2026-03-28 | Design doc cleanup — fixed stale refs across 8+ docs, added platform_ts to inbound_messages, updated infrastructure (testcontainers, no Docker Compose), fixed Drizzle example (UUIDv7), removed TELEGRAM_BOT_TOKEN from secrets (channels.credentials). |
| 2026-03-26 | Telegram adapter — grammY long polling, auth allowlist (TELEGRAM_ALLOWED_USERS), control commands (/start, /new) via CommandService, typing indicator. Opt-in via TELEGRAM_BOT_TOKEN. 74 tests across 14 files. |
| 2026-03-26 | Module restructure — handle-message to src/agent/, respond functions to src/channels/, src/inngest/ is core setup only. Module boundaries documented in CLAUDE.md. |
| 2026-03-26 | Channel architecture decisions — agent returns markdown (no rich intermediate repr), control commands intercepted by adapters, grammY over Telegraf. Documented in agents.md + decisions.md. |
| 2026-03-25 | Fix pipeline e2e test — switched from serve mode (HTTP) to connect mode (WebSocket). Connect mode eliminates function discovery timing issues: app initiates outbound connection to Inngest dev server, `connect()` blocks until handshake succeeds. Also fixed test userId (was string, needed UUID from default user). All 3 integration tests passing. |
| 2026-03-25 | Hindsight retain/recall integration test passing — Ollama qwen2.5:3b via @testcontainers/ollama. Migrated from DockerComposeEnvironment to individual testcontainers. Fixed PG18 volume path, Ollama healthcheck, Hindsight /health endpoint, Ollama base URL /v1 suffix. |
| 2026-03-24 | Tool system refactor — ToolCapabilities interface (ACL boundary), Zod input validation via defineTool(), memory_recall + memory_retain tools, Hindsight wired into orchestrator. 66 tests across 11 files. |
| 2026-03-24 | Design: tool architecture (capability interface, typed dispatch, plugin-ready), bank strategy (per-user + tags), memory ACL via tags (compartments + trust tiers), plugin trust tiers, Hindsight operations clarification (reflect ≠ consolidation) |
| 2026-03-23 | End-to-end message pipeline — LLM abstraction (provider-agnostic), agentic loop, CLI adapter, Inngest orchestration with DI, 37 unit tests |
| 2026-03-23 | Initial Drizzle migration generated and verified against PostgreSQL |
| 2026-03-23 | CLAUDE.md — design philosophy (early abstractions, event decoupling, thin infra), testing rules, DI stance, code style |
| 2026-03-23 | Replace BullMQ with Inngest — event-driven durable execution, added to Docker Compose, updated all design docs |
| 2026-03-23 | Add todo.md + `/next` command — priority-based task tracking |
| 2026-03-23 | Add confidence markers to all design docs (`[confirmed]`/`[proposed]`/`[research]`) |
| 2026-03-23 | Remove nucleus/NixOS-specific details — app is now deployment-agnostic |
| 2026-03-23 | Rewrite data-model.md — Drizzle as source of truth, lean Phase 1 schema (3 tables), deferred tables documented for later |
| 2026-03-23 | Add Docker Compose (PostgreSQL + pgvector, Redis), Drizzle schema + config, db connection module |
| 2026-03-23 | Scaffold Phase 0 — package.json, tsconfig, tsup, Biome 2.x, Vitest, Pino logger, env parsing (@t3-oss/env-core + Zod v4), directory structure |
| 2026-03-22 | Add tooling.md — dev stack research (pnpm, tsx, tsup, Fastify, Drizzle, Vitest, Pino, Biome, Remeda, neverthrow) |
| 2026-03-22 | Add data-model.md (unified PostgreSQL schema), testing.md (local dev, mocking, evaluation) |
| 2026-03-22 | Add session lifecycle, context window mgmt, message batching, Telegram auth to agents.md |
| 2026-03-22 | Add Hindsight deployment clarification, `claude -p` integration pattern to architecture.md |
| 2026-03-22 | PROGRESS.md: add missing Phase 0/1/2 tasks (schema, session lifecycle, auth, `claude -p`) |
| 2026-03-22 | Fix design doc gaps: stateless-per-invocation model, GroupQueue priority, signal capture schema, embedding model, Stage 4 graduation features |
| 2026-03-22 | Add PROGRESS.md — phased delivery plan (Phase 0-5) |
| 2026-03-22 | Initial design docs — 9 files distilled from 22 research docs |
