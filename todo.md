# Todo

## Next

- [ ] `p3` Warn on retired/unknown model id at startup — scan `profiles.{model,summarization_model,extraction_model}` and `model_providers.model` against `MODEL_REGISTRY` on boot and log a structured warning naming the row + suggesting `/profile edit` / `/model`. Migration 0019 healed Sonnet 4 / Opus 4 in-place to their recommended successors, but the retired Anthropic ids (Haiku 3.5, Sonnet 3.5/3.7, Opus 3) and any non-Anthropic deprecations stay as-is — picking a "replacement" for them is a guess. Surfacing dead config to the operator is more honest than silently changing their billing target. The setup wizard already filters `MODEL_REGISTRY` in the picker so no new bad rows can be introduced via the UI; this guard is for legacy state.
- [ ] `p3` Replace top-level `process.exit(await dispatch(command))` in `src/main.ts` with `process.exitCode = await dispatch(command)` so buffered stdout/stderr drains naturally before the process exits. Blocked on per-dispatch graceful shutdown: `serve` already cleans up via the `finally` after `connection.closed`, but `skills`/`setup`/`seed` call paths that open DB pools, LLM SDK clients, and (for `skills`) the sandbox supervisor — those keep the event loop alive and would hang the process if `process.exit` is removed without explicit teardown. Fix needs a `shutdown()` hook on each entrypoint that closes its handles, then exitCode-driven exit becomes safe.
- [ ] `p3` `pnpm console` exits before the assistant reply lands when stdin is piped — readline's `close` event fires the moment EOF is seen, racing the in-flight `waitForResponse` poll loop and printing `Bye.` mid-wait. Fine for interactive use but breaks one-shot CLI usage like `echo "msg" | pnpm console`. Fix: defer the `close` handler until any pending `waitForResponse` settles (or delivery times out). Workaround today: `(sleep 1 && echo msg && sleep 35) | pnpm console`.
- [ ] `p3` Shrink fal fixture image — current replay fixture is 125 KB (recorded at fal's default 1024x768). A smaller size parameter or post-record downscaling would keep the repo lean. Not urgent — Git LFS is overkill, 125 KB is acceptable.
- [ ] `p3` Migrate binary test fixtures to Git LFS — only when any of: (a) a single fixture exceeds 1 MB, (b) fixtures churn in most PRs (would happen with visual regression testing on FE screenshots), (c) total binary growth > ~50 MB/year, (d) clone time becomes annoying (>1 min), or (e) GitHub shows the large-files warning. Today all fixtures are <200 KB with low churn (re-recorded only when prompts/models change), so direct commit is correct. One-shot migration when symptoms appear: `git lfs track "test/fixtures/fal/*.jpg"` + `git lfs migrate import --include="test/fixtures/fal/*.jpg"` rewrites history. See `.gitattributes` for the current binary markers.
- [ ] `p2` Durable tool execution — wrap expensive tool calls (notably `generate_image` at $0.02-0.04/call, also `web_answer` via Perplexity Sonar) in `step.run` so Inngest retries replay from cache instead of re-billing. Requires threading `step` into `runStreamingAgentLoop` and per-tool invocation — real architectural change. Decide: opt-in per tool (`ToolSpec.durable: true`?) vs wrap-all-by-default. Return value must be the tool's `string` result (already small — JSON metadata for generate_image, summarized answer for web_answer); bytes never leave the step body. Tool side effect is exactly-once on retry. Today, retries re-execute tool calls — see `design/crash-recovery.md` "Tool side effects that re-execute" table.
- [ ] `p2` Tool result compression — cut tokens on verbose tool output (biggest sources: `file.read`, `web.fetch`, `memory.recall`). Two layers: (a) `ToolSpec.compress?(output) => output` hook invoked in the agent loop before appending the `tool_result` block — per-tool strategies: head+tail truncation with elision marker, dedupe repeated lines with counts, markdown-not-HTML for `web.fetch`, top-K cutoff with scores for `memory.recall`. (b) Historical tool-result compaction in `context.ts` — once a `tool_result` is N turns back, replace it with a one-line placeholder (`[web.fetch https://... — 12KB truncated, turn 7]`); current turn keeps full output. Complements existing summarisation compaction but is deterministic and lossless for the current turn. Prompt caching already covers the cached prefix; this targets the uncached tail. Inspired by rtk-ai/rtk but operates at the tool-result boundary inside the runtime, not shell output.
- [ ] `p2` Background compaction — run summarization after response (while user reads) instead of before next turn, store pre-computed summary to eliminate compaction latency
- [ ] `p3` Reconsider internal tag stripping — removed from Phase 1 (thinking blocks + tool calls cover current use cases). Revisit if a concrete need for inline orchestrator-only metadata emerges that tools can't express
- [ ] `p3` Consider moving `IDENTITY`/`ONBOARDING` constants (`src/agent/prompt.ts`) out of code — they're defaults that `profile.basePrompt` can override, but if evolution ever wants to tune the baseline identity/onboarding tone we'd need them in a table. Not urgent — profile override path already exists.
- [ ] `p2` Batch API support for async evolution tasks — 50% cost reduction for reflection, extraction, optimization
- [ ] `p2` CLI channel management commands — `main.ts channel add telegram --token=...`, `channel list`, `channel remove` (store layer covered by `p1` Channel CRUD; this is the CLI surface)
- [ ] `p3` LLM-suggested conversation auto-naming — generate alias from first turn or on demand. User can accept/edit/reject. Aliases remain user-controlled; this is a suggestion path, not auto-apply.
- [ ] `p3` Per-adapter Transport ACL — wrap `Transport` per adapter with a permission policy (e.g., third-party adapters cannot mutate profiles, cannot create `receive: all` sessions). Returns `operation_not_permitted` (already in `TransportError` union). Deferred until first non-trusted adapter ships.
- [ ] `p3` Profile-switch tool-removal regression test — when `/profile switch` removes a tool whose `tool_use` blocks live in conversation history, verify the next turn still loads cleanly (Anthropic API accepts historical tool blocks even if not in current `tools`, but a regression test pins the contract).
- [ ] `p2` Pass transaction function to stores instead of full Database — makes transactions inescapable, narrows the interface
- [ ] `p3` Observer unit test — mock `step.run` to cover min-message guard, conditional consolidation, return shape
- [ ] `p3` Idempotent correction inserts — dedup key (rule text hash + category + source) to prevent duplicate rules on step retry
- [ ] `p3` Consider dropping Inngest serve mode — only connect mode is used (tests, production, local dev)
- [ ] `p3` Switch Hindsight LLM to gpt-5-nano — blocked on Hindsight emitting `max_completion_tokens` for GPT-5 models. Currently on gpt-4o-mini (~$10/mo vs ~$6/mo target). See `design/memory.md` → Known Gaps.
- [ ] `p3` Hindsight retain failure monitoring — `async: true` means background pipeline errors are invisible to caller. Poll `operation_id` status or wire webhooks; surface failures to logs/metrics.
- [ ] `p3` Adopt `recallResponseToPromptString()` — richer memory injection with temporal fields, entity summaries
- [ ] `p3` Use native OpenRouter provider for prod Hindsight — replaces `openai` + custom base URL workaround
- [ ] `p3` Stale llmock fixture cleanup — detect unused fixtures after test run, auto-delete
- [ ] `p2` Observer channel-scoped extraction — query conversation's active channels via `channel_sessions → channels`, pass to extraction prompt, extend Zod schema with optional `channelType`, let LLM decide if correction is channel-specific or general. Also: add `channelType` to `getCorrections()` return shape so dedup key distinguishes channel-scoped from global corrections.
- [ ] `p3` Composite index on `steering_rules(profile_id, channel_type)` — premature at personal scale (~30 rules), add when rule count or query latency grows
- [ ] `p3` grammY native test primitives — use `bot.handleUpdate()` + `bot.api.config.use(transformer)` instead of `vi.mock("grammy")`
- [ ] `p3` Rename `DEPLOYMENT.md` → `deploy.md` and `PROGRESS.md` → `progress.md` for consistency with the project's lowercase-hyphenated naming convention. Update inbound links across the repo (`README.md`, `CLAUDE.md`, design docs, CI workflows, `todo.md`). Keep `README.md`/`CHANGELOG.md`/`CONTRIBUTING.md`/`CLAUDE.md` as-is — universal or tool-enforced conventions.
- [ ] `p3` Opt-in client-side encryption for R2 attachments — Cloudflare R2 encrypts at rest with provider-managed keys (no SSE-C, no SSE-KMS), so Cloudflare staff (and anyone they're compelled to disclose to) can technically read uploaded/generated images. Add an opt-in flag (`S3_CLIENT_ENCRYPT=true` or per-bucket config) that wraps S3 `PutObject`/`GetObject` with AES-256-GCM streaming, key derived from `COGMO_MASTER_KEY` via HKDF (same pattern as DB credentials). Runtime overhead is negligible (AES-NI gives ~3-5 GB/s, 28 bytes of nonce+tag per object). Real costs are operational: master-key rotation now requires re-encrypting every R2 object; objects become opaque in the R2 dashboard / `aws s3 cp` / `rclone`; forecloses direct-browser-serve (a `<img src="r2-url">` would show ciphertext). Worth shipping behind a flag for users who want Cloudflare-blind storage; off by default at personal scale where the threat model is "someone gets my Cloudflare creds" not "Cloudflare staff snoop". ~30-50 lines in the S3 wrapper.
- [ ] `p3` Telegram e2e via Test DC + tgintegration — real user on Telegram test servers, TypeScript/mtcute client
- [ ] `p3` Consider web interface adapter for e2e testing
- [ ] `p3` Evaluate DI library (ditox/awilix) when adapter count exceeds 5
- [ ] `p3` Consider Renovate over Dependabot — Renovate has a built-in `node-lts` preset that genuinely tracks the Node LTS schedule, which Dependabot lacks. Currently using a workaround in `.github/dependabot.yml` (ignore all Node major bumps, manual yearly upgrade). Switch only if the manual cadence becomes annoying or if other LTS-tracking gaps appear.
- [ ] `p3` Hindsight error classification — wrap `HindsightClient` calls in `HindsightMemoryProvider` with `AbortError` logic for known-permanent failures (4xx auth/validation). Currently retries indiscriminately, wasting up to ~10s on every misconfigured request. Requires investigating `HindsightClient`'s error shape (is it `e.status`? `e.response.status`? `e.code`?) before classifying safely.
- [ ] `p3` Consider ISO 8601 PT durations for time-valued config — instead of `minTimeoutMs: 1000`, `SESSION_IDLE_TIMEOUT_MINUTES=60`, `DEBOUNCE_IDLE_SECONDS=3` etc, use `"PT1S"`, `"PT1H"`, `"PT3S"` parsed by `date-fns` or a tiny helper. Eliminates the unit-suffix bikeshed and prevents seconds-vs-milliseconds bugs at API boundaries. Would touch `RetryOptions`, `env.ts`, and any caller passing durations. Defer until we add a third unit (anything other than ms/s/min) or until the unit-suffix maintenance becomes annoying.
- [ ] `p3` fetch_url SSRF hardening — dns.resolve() + IP check before fetch (current check is string-level only)
- [ ] `p3` Verify Perplexity Sonar citations structure via real OpenRouter API response — may need to adjust parsing
- [ ] `p3` Streaming retry-dedup test — `TelegramAdapter` `#activeStreams` map is documented in `design/crash-recovery.md` ("Streaming dedup across the same process") and `design/transport/streaming.md` but no test enforces that a retry with the same Inngest `runId` reuses the existing `TelegramStreamHandle` instead of opening a second Telegram message. Regression would ship silently until a user sees duplicate bubbles. Needs either a real adapter instance or a new test seam.
- [ ] `p3` Replace the `as any` cast on the `cache_control` text part inside `buildMessages` in `src/llm/openai-compat.ts` with a local `CachedTextPart extends OpenAI.Chat.ChatCompletionContentPartText` interface + `satisfies` cast. OpenRouter's `cache_control` extension isn't in OpenAI's types; the ecosystem winner (Qwen, wave-agent, et al.) is local interface extension — no double-cast needed because of width subtyping. Include `ttl?: "5m" | "1h"` for forward compat with OpenRouter's 1h cache variant.
- [ ] `p3` Replace `(b as { text: string }).text` casts with type-guard predicate filters. Current pattern is `.filter((b) => b.type === "text").map((b) => (b as { text: string }).text)` — TypeScript doesn't narrow through `.filter()` because the predicate signature is `(value: T) => boolean`, not `(value: T) => value is U`. Switching to `.filter((b): b is TextBlock => b.type === "text")` lets `.map((b) => b.text)` work without the cast. Grep for `as { text: string }` to find call sites (handle-message summarize callback, llm provider adapters likely). Cosmetic, pre-existing pattern.
- [ ] `p3` Operator-supplied SSH signing key import (`COGMO_GITHUB_SSH_PRIVATE_KEY`) — non-interactive setup currently rejects the var with "importing pre-generated OpenSSH keys isn't supported yet" and falls back to generating a fresh keypair. To support, parse the OpenSSH private blob (Ed25519-only is fine for slice 4 — the wizard generates Ed25519, and ssh-keygen format conversions across types are unnecessary scope), derive the public key, validate the keypair self-consistency (public matches what we'd derive), and persist into the bundle instead of regenerating. Useful for operators who want a stable signing key that survives setup re-runs (CI, GitOps, repeatable deploys); without import, every `cogmo setup` rotates the signing key and the operator has to install the new public key on github.com. Non-blocking for personal use — the wizard's print-the-public-key flow works fine when re-runs are rare. `micro-key-producer/ssh.js` doesn't expose a parse helper today but the OpenSSH wire format is documented; alternative is `sshpk` (~30 KB).
- [ ] `p3` Sysbox-real-Claude subprocess e2e — slice 4.0i ships an integration test using a Gitea testcontainer + scoped octokit fetch + host-spawning fake sandbox, which exercises the orchestration but not the sandbox's bind-mount machinery or a real Claude session inside a sysbox container. A true `.e2e.test.ts` would: (a) launch the cogmo binary as a subprocess, (b) spawn a real sysbox container via the supervisor, (c) run claude inside it on a fixture repo with a trivial change, (d) assert the verify orchestrator opens a draft PR upstream. Blocked on either a self-hosted GHA runner with sysbox installed, or a runc-on-GHA workaround that swaps `SANDBOX_RUNTIME=runc` for the test (lose userns isolation in CI but keep it in production). The existing sysbox-e2e GHA job (`.github/workflows/sysbox-e2e.yml`) already installs sysbox 0.7.0 on `ubuntu-24.04` for the supervisor's sysbox-flavoured tests; this would extend it to drive the full task pipeline.
- [ ] `p2` Cron pruner for `refs/cogmo-wip/<task-id>` refs — `teardownWorktree` (`src/agent/coding/teardown.ts`) force-pushes failed/cancelled worktrees to this namespace so users can fetch + inspect Claude's in-progress code (`git fetch origin refs/cogmo-wip/<id>:wip-<id>` → `git worktree add <path> wip-<id>`). Refs are append-only and accumulate; design/coding-delegation.md targets a weekly cron that prunes any WIP ref whose `coding_tasks` row is terminal AND >30 days old. Personal scale tolerates manual cleanup for now; promote when ref count crosses ~50 or pushes start hitting GitHub's per-repo ref limit.
- [ ] `p3` Bootstrap-tar fallback for no-remote worktree teardown — design's row 3: when a task fails BEFORE its first commit on a brand-new repo without a remote yet, the WIP push has nowhere to go. Fall back to `~/.cogmo/archives/<task-id>.tar.zst` with 14-day TTL. Doesn't apply to `/repo add`-registered repos (always have a remote). Land if the no-remote bootstrap path is ever supported.
- [ ] `p3` Apply the JSONB-blob pattern to multi-field clusters on `coding_tasks` as later slices add them, instead of growing more nullable columns. Today's nullable lifecycle columns (`session_id`, `container_id`, `plan`, `plan_approved_at`, `pr_url`, `failure_reason`) are all single fields where blob = text-with-overhead — don't convert. Slice 2+ likely candidates: a `pr` blob (`pr_url` + `pr_number` + `branch_sha` + `opened_at`) when slice 4 ships verify+push+PR; a `failure` blob (`failure_reason` + `failure_step` + `failed_at`) for richer error reporting; possibly a `cli_session` blob (`session_id` + `cli_version` + `model`) if per-turn CLI metadata becomes useful. The `worktree_assignment` JSONB pattern (atomicity by Zod schema, single null check, all-or-none invariant enforced at the store boundary) is the model to copy.
- [ ] `p3` Bump GHA sysbox e2e job from `ubuntu-24.04` to `ubuntu-26.04` once both ecosystems catch up. Ubuntu 26.04 LTS released 2026-04-23 but as of 2026-04-26 (a) `actions/runner-images` doesn't ship the `ubuntu-26.04` label yet — issue [#13855](https://github.com/actions/runner-images/issues/13855) is blocked on Azure Marketplace publishing the base image; [#13964](https://github.com/actions/runner-images/issues/13964) is the open feature request — and (b) sysbox 0.7.0 (Mar 2026) `distro-compat.md` still tops out at Noble (24.04, kernel 6.8+); no `.deb` for 26.04. Watch the sysbox release notes for a `0.7.x` or `0.8.0` adding 26.04 support, then bump runs-on. Lower-priority because 24.04 is GHA's `ubuntu-latest` until the migration completes.
- [ ] `p3` Adopt `docker-bake.hcl` for multi-image builds — currently two separate workflows (`publish.yml` for the `cogmo` app image, `devbase.yml` for `cogmo-devbase`). Bake earns its keep at 3+ images or when we want platform matrices (amd64+arm64 for Mac dev). Likely trigger: slice 4 picks up Codex CLI + Python toolchain in devbase, possibly with arm64 variants; or we add per-language devbase variants. Refactor: ~30 lines, define `app` / `devbase` targets, workflows replaced with `docker buildx bake --push <target>`. Functionally identical to today, but local `docker buildx bake devbase` reproduces CI exactly.
- [ ] `p3` Group all-or-none coupled deps structurally in `createTransport` / `RegistryDeps` — both `skillRunner`+`skillStore` and `codingStore`+`codingStreamingRegistry` are independently optional in the type but must be passed together (Transport / per-channel functions check `if (!a || !b)` at runtime). One PR refactor: collapse each pair into `skills?: { runner, store }` / `coding?: { store, streamingRegistry }` so the invariant is structural and TypeScript rejects half-set states at the call site. Anti-pattern will keep biting whenever a third coupled-pair gets added. Touches `transport.ts`, `transport/registry.ts`, `transport/adapter-module.ts`, `index.ts` bootstrap, and adapter `setup()` functions.
- [ ] `p3` Free-text deny reason via reply prompt — `transport.skills.denyDeploy` accepts an optional `reason` and `runner.denyDeploy` logs it, but the Telegram callback always passes `undefined` (one-tap Deny is faster). If you find yourself wondering "why did I deny that one?" looking at logs, add a follow-up reply prompt (mirror `/repo add`'s FSM dialog) or a `denied_reason TEXT` column on `skill_deploys`. Defer until the audit value is felt — single-tap Deny is good UX otherwise.
- [ ] `p3` Managed sandbox API backend (Daytona / E2B / Fly Machines) — second impl of `Sandbox` in `src/sandbox/supervisor.ts`, alongside the local dockerode+sysbox impl. Picked at deploy time via config; users without Docker+sysbox available point at a managed provider. Daytona is the leading target — sysbox-native, preserves slice 3's sibling-container design. Mirror the OpenAI Agents SDK's `SandboxClient` shape so further providers are per-provider deltas, not rewrites.

## Later

- [ ] `p2` Morning briefing — Inngest cron function (Phase 2)
- [ ] `p2` Post-conversation extraction — Inngest delayed function (Phase 2)
- [ ] `p2` Memory consolidation — daily `reflect()` via Inngest cron (Phase 2)
- [ ] `p2` Agent self-scheduling tools (Phase 2)
- [ ] `p2` Claude Agent SDK integration — background tasks via subscription (Phase 2)
- [ ] `p2` MCP client Phase A — implement `src/mcp/` per [design/integrations/mcp.md](design/integrations/mcp.md): `mcp_servers` + `mcp_server_tools` tables, `McpRegistry` facade, stdio transport, host runner, schema-hash pinning, profile glob filtering, 25-tool budget, `SecretRef` injection from secrets table, `/mcp` admin commands (add/list/remove/approve), 30s per-call timeout + one-shot reconnect, Inngest `step.run` durability for tool calls. Non-goals for A: HTTP/SSE transport, sysbox sandboxing, OAuth, resources/prompts. Validation: one real third-party server (GitHub) usable end-to-end. Three PRs: store + migrations; client + pool + adapter; orchestrator wiring + admin commands.
- [ ] `p2` MCP client Phase B — sysbox runner for `untrusted` servers (read-only rootfs, no host mounts, per-server egress allowlist), trust allowlist in code (`SHIPPED_TRUSTED_SERVERS`), full approval UX. Gates: every third-party server runs sandboxed.
- [ ] `p2` MCP client Phase C — HTTP transport (`StreamableHTTPClientTransport`), manual-token auth via `SecretRef` headers. Validation: one HTTP-only server (e.g. Linear).
- [ ] `p3` MCP client Phase D — OAuth 2.1 with PKCE + Dynamic Client Registration, encrypted refresh-token persistence, proactive refresh before tool dispatch (gemini-cli #23296 cautionary tale). Unblocks Google Workspace servers (Gmail, Calendar) — replaces `First ingestion agent: Gmail via MCP` and `First ingestion agent: Google Calendar via MCP` entries below.
- [ ] `p2` First ingestion agent: Gmail via MCP (Phase 2) — depends on MCP client Phase D
- [ ] `p2` First ingestion agent: Google Calendar via MCP (Phase 2) — depends on MCP client Phase D
- [ ] `p3` Artifact renderer — standalone web server for rich content (charts, tables, interactive views)
- [ ] `p3` All-in-one Docker image — single container bundling Postgres+pgvector, Redis, Inngest, Hindsight, Cogmo, supervised by systemd (or s6-overlay) inside. Base on `ubi9-init` or `jrei/systemd-debian`; one named volume for all persistent state; first-boot oneshot unit runs setup wizard. Targets the "simple user" one-liner path (`docker run -v cogmo-data:/var/lib/cogmo ghcr.io/.../cogmo-aio`). Decide vs. a `docker-compose.yml` at implementation time — compose is cheaper (~50 lines YAML) but keeps multi-container UX; AIO is ~1 week packaging work but yields true one-liner. Known hard problems: Postgres major-version upgrades across image tags (Home Assistant hits this; mitigate with `pg_upgrade` migration unit), backup story requires `pg_dump` via `docker exec` (not volume snapshot). Precedents: GitLab Omnibus (runit), Home Assistant OS.
- [ ] `p2` Skills P3.1 foundation — skills store schema (`skills`, `skill_deploys`, `skill_runs`, `skill_context_calls`), `SkillManifestSchema`, local bare skills repo bootstrap at `$COGMO_SKILLS_PATH` with pre-receive hook (see [design/skills.md](design/skills.md) and PROGRESS.md P3.1)
- [ ] `p2` Skills P3.2 runtime — JSON-RPC worker protocol, tier 1 (Pyodide in Node worker thread) + tier 2 (Python subprocess in sysbox container via `Sandbox`) workers, pool/dispatcher, Python `ctx` SDK (PROGRESS.md P3.2). Depends on Phase 6 P1.1 sandbox primitives for tier 2.
- [ ] `p2` Skills P3.3 deployment pipeline (remaining) — real risk classifier with AST lint (today's declaration-only classifier in `src/skills/classifier.ts` honours declared `effects` / `tier` / `secrets`-count but doesn't *detect* undeclared effects; replace once an AST pass over `skill.py` is worth the maintenance); `/disable` / `/enable` channel commands (PROGRESS.md P3.3)
- [ ] `p3` Pending-approval skill branch cleanup — `runner.register` only calls `deleteRef(branch)` when the deploy goes live (auto / notify tier). Approve-tier deploys leave the original feature branch behind on the bare repo even after `approveDeploy` advances main; long-running deployments accumulate stale `refs/heads/skill/*` refs. Cleanup needs the original branch name on `skill_deploys` (extra TEXT column or JSONB `pending_metadata`) so `executeApprove`'s `applyFilesystem` can also `deleteRef` it. Personal-scale tolerates manual cleanup until the ref count starts mattering; promote when there are 20+ unmerged feature branches or a register starts failing on ref-name collisions.
- [ ] `p2` Skills P3.4 invocation (remaining) — Inngest cron per scheduled skill, failure handling (retry → notify → auto-disable after 3), tier-2 progressive-disclosure (swap full SKILL.md body into tool description on selection) (PROGRESS.md P3.4)
- [ ] `p2` Skills P3.5 telemetry & cost — `skill_runs` tracking, `skill_context_calls` audit, cost accounting via `ctx.llm` token metering + declared `cost_per_call_usd` + dispatcher budget enforcement (PROGRESS.md P3.5)
- [ ] `p3` Skills P3.6 deferred — egress-proxy secret substitution, `ctx.skills.invoke` composition, agent-led repair loop, search_skills retrieval layer. Move to Next individually when the trigger fires (trust boundary widens, skill count crosses ~50, tool-selection accuracy drops).
- [ ] `p3` Prompt optimization — evaluation rubrics, bootstrapped few-shot (Phase 4)
- [ ] `p3` HyDE for auto-recall — generate a short hypothetical answer with a small/fast LLM, embed that instead of the raw user message. Improves recall quality on long/multi-turn turns where a mean-pooled embedding of raw chatter dilutes intent. Adds an LLM call on the hot path, so gate behind a profile setting and only consider after bumping `HINDSIGHT_API_RECALL_MAX_QUERY_TOKENS` + truncation (issue #120) is in place and measured insufficient.
- [ ] `p3` Evaluate `@inngest/test` `InngestTestEngine` for our Inngest function tests. Today we mock `step` with `vi.fn()` and reach into `(fn as any).fn(...)` for handler invocation; the engine runs the real Inngest dispatcher (step memoization, replay, `step.waitForEvent`) and could catch a class of bugs the hand-rolled mocks miss. Trade-offs to weigh: execution-engine overhead vs hand-rolled speed, network side effects of real `step.sendEvent` calls, what (if anything) it offers for asserting function *config* (debounce/concurrency/triggers — currently `(fn as any).opts`). Candidates if we adopt: `handle-message.test.ts` (durable + non-durable boundary, resume policy), coding orchestrator tests (plan → execute → verify with `step.waitForEvent`), skills runner tests with the register/approve flow.

## Blocked

## Done

- [x] Migrate default model off `claude-sonnet-4-20250514` — Anthropic deprecated `claude-sonnet-4-20250514` and `claude-opus-4-20250514` with retirement date 2026-06-15. Migration `0019_replace_deprecated_models.sql` rewrites `profiles.{model,summarization_model,extraction_model}` and `model_providers.model` for both ids to their recommended successors (`claude-sonnet-4-6` / `claude-opus-4-7`); `model_providers` rewrite uses dedup-then-rename with a position-bump pre-pass to satisfy `unique(model, provider_id)` and `unique(model, position)` in operator setups that registered both old and new ids. `messages.model` deliberately untouched (audit trail). `MODEL_REGISTRY` (`src/llm/models.ts`) overhauled: dropped `claude-sonnet-4-20250514`, `claude-opus-4-20250514`, `claude-haiku-3-5-20241022` (the retired Haiku 3.5); added current-tier `claude-opus-4-7`, `claude-opus-4-5-20251101`, `claude-opus-4-1-20250805`, `claude-sonnet-4-5-20250929`; fixed `claude-sonnet-4-6` / `anthropic/claude-sonnet-4.6` `maxOutputTokens` from `128_000` → `64_000` (Anthropic docs confirm 64k synchronous output for Sonnet 4.6 — would 400 on a max-output request); added direct OpenAI flagships `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini` (limits per OpenAI docs); added new OpenRouter routes `x-ai/grok-4-fast` and `google/gemini-2.5-flash-lite` (limits from `openrouter.ai/api/v1/models`). `src/setup/seed.ts` default profile, `src/agent/evolution/observer.ts` `DEFAULT_EXTRACTION_MODEL`, and `src/transport/adapters/telegram/profile-dialog.ts` example string all switched to `claude-sonnet-4-6`. Test files (`src/test/factories.ts`, `src/llm/models.test.ts`, plus 9 `.test.ts` files using the deprecated id as opaque mock value) bulk-renamed via sed; `models.test.ts` assertions updated to match the new Sonnet 4.6 limits (1M / 64k / budget 926k). Skipped from this PR: Grok 4.3 (max output not exposed), Gemini 3.x previews (not stable), `gpt-5.4-nano` (limits unknown).
- [x] JSONB Zod validation at the driver boundary via `jsonbZod(name, Schema)` helper in `src/db/helpers.ts` (Drizzle `customType` wrapper, `Schema.parse()` on `toDriver` + `fromDriver`, JSON.stringify on write so PG sees a JSON literal not a raw text token). Migrated every JSONB column off `jsonb()` + scattered `.parse()` calls: `messages.content`, `llm_providers.attrs` (new `ProviderAttrsSchema`), `profiles.tool_set` (new `ToolSetSchema`), `inbound_messages.content` (new `InboundContentSchema` in `transport/content.ts` — also closes the standalone "Define InboundContent schema" todo), `containers.{labels,resource_limits}`, `networks.labels`, `volumes.labels`, `skills.{effects,inputs,outputs}`, `skill_deploys.classifier_log`, `skill_runs.{inputs,output}`, `coding_tasks.{devcontainer,worktree_assignment,pr_metadata,resource_usage}`. `channels.credentials` stays raw `jsonb()` with an OPAQUE marker comment per the CLAUDE.md exception (encrypted ciphertext the adapter hands back to the channel SDK). Drizzle row inference now produces the validated TS shape directly, so `JsonValue` casts and per-method `parseFooRow` wrappers are gone from sandbox/skills/coding stores; remaining `JsonValue` uses are `channels.credentials` (intentional) and outbound `Transport.deliver` (arbitrary content). CLAUDE.md JSONB rule updated to point at the helper. Tests rewritten for `contentToBlocks` to reflect the now-typed `string | InboundBlock[]` contract. 1814 unit tests pass.
- [x] Phase 6 P1 slice 4 (P1.5) — verify + push + draft PR. Verify orchestrator (`coding-task-verify` Inngest function) triggered by `coding/task/cli-done` (emitted by execute orchestrator on `pending_verify` transition). Drives `runVerifyStreaming` → `runCommitAndPush` → `runOpenDraftPr` with durable `step.run` boundaries; status flows `pending_verify → verifying → pushed → pr_open`. New events: `coding/task/cli-done`, `coding/task/verify-complete`, `coding/task/pushed`, `coding/task/pr-opened`. Schema: `coding_repos.identity_name TEXT NOT NULL DEFAULT 'default'` + `verify_timeout_seconds INT NOT NULL DEFAULT 600`; `coding_tasks.pr_url TEXT` replaced with `coding_tasks.pr_metadata JSONB` (`{url, number, branchSha, openedAt}`, atomic-by-Zod). New `secrets/github.ts` defines `GitHubIdentitySchema` (`{pat, sshPrivateKey, sshPublicKey}` bundle) + `resolveGitHubIdentity` returning a discriminated `Result`; secrets keyed `github_identity:<name>` with per-repo override via `coding_repos.identity_name`. Setup wizard's GitHub step generates an Ed25519 SSH signing keypair via `micro-key-producer/ssh.js` (Paul Miller, MIT, same noble family as `@noble/ciphers`/`@noble/hashes`) and prints the public key + `https://github.com/settings/ssh/new` link telling the operator to install it as a *signing key*. Validates the PAT against `GET https://api.github.com/user`. Non-interactive mode accepts `COGMO_GITHUB_PAT` (with `_FILE` variant). New `Transport.repos.cloneAndAdd({name, remoteUrl})` + Telegram `/repo add` FSM dialog (3 steps — name → remoteUrl → confirm) clones the remote into `${COGMO_REPOS_DIR}/${name}` using the default identity's PAT through a one-shot host-side `GIT_ASKPASS` helper (`src/secrets/git-askpass.ts`, ~100 LOC, mode 0700/0600, wiped on completion). New per-task askpass material in `src/sandbox/askpass.ts`: `provisionAskpass(...)` writes `helper`/`pat`/`signing-key`/`signing-key.pub` under `${SANDBOX_ASKPASS_DIR}/<rootTaskId>/`, bind-mounted read-only at `/.cogmo-askpass/`; `LocalInProcessSandbox.stopTask` calls `cleanupAskpass` in its `try/finally`. Commit signing via per-invocation `git -c gpg.format=ssh -c user.signingkey=<path> commit -S` (no global config). Push via `git push origin cogmo/<idShort>` with discriminated outcomes: `branch_conflict` (rejected), `auth_failed` (PAT/ssh deny), `failed`. Draft PR via `@octokit/rest` v22 (`pulls.create({draft:true})`); title=task.goal trunc 70 chars, body=`## Plan\n\n<plan>\n\n## Verify output\n\n<fenced output>\n\n_Generated by Cogmo_`; 401/403 → `auth_failed`, 422 → `validation_failed`, other → `failed`. New runtime deps: `@octokit/rest`, `@octokit/request-error`, `micro-key-producer`. Drops the planned `gh` CLI from devbase. Test coverage: 358 unit tests across coding module + 2 integration tests (`verify-orchestrator.integration.test.ts` — Gitea testcontainer for real `git push`, scoped fetch interceptor for octokit per the `fal-mock.ts` pattern, host-spawning fake sandbox; happy path → `pr_open` with branch in Gitea, failure path → `status=failed` no push). Integration test approach chosen over real-GitHub-account testing: octokit team's recommendation (REST mocked at fetch layer, git protocol against a separate git server). 1330 unit + 2 integration tests pass. Design markers upgraded in `coding-delegation.md`: Flow / Git Identity / Repo Registry / Merge gate all `[confirmed]`. WIP-ref push on verify-fail deferred (p3 in todo) — orchestrator leaves a feature-flag hook (default off).
- [x] Phase 6 P1 slice 3 — tool gate + Docker proxy + reaper + cgroup parent. Three new tables (migration 0012): `networks`, `volumes` (lineage parallel to `containers`), and `coding_tool_decisions` (per-task permission decision log). Pure in-process policy table (`src/agent/coding/policy.ts`) with default-broad allow + narrow prompt set on external state changes only — file ops, read-only Bash, test/build/lint/format, package installs, local docker, in-container `rm` all auto-allow; `git push`, `gh pr/issue/release/repo` mutations, package publishes, HTTP writes to non-localhost prompt; empty deny set (the proxy + sysbox runtime are the security boundary). `ClaudeCodeBackend.execute` drops `--permission-mode acceptEdits` and routes every tool call through stream-json `control_request`/`control_response` over stdin via the new `CodingExecuteHandle.respondPermission`. New Docker socket proxy in `src/sandbox/proxy/` listens on per-task Unix sockets at `/run/cogmo/sockets/<task-id>.sock`; pure router classifies endpoints (deny `/swarm/*` `/plugins/*` `/nodes/*` wholesale, policy on `POST /containers/create`, hijack `/exec`/`/attach`/`/events`/`/build`/`/session`/`/logs`, forward everything else); policy denies `Privileged=true` / `NetworkMode=host` / host-path binds / dangerous caps and injects `Runtime` + `CgroupParent` + Cogmo labels. Single shared `http.Server`; one tiny `net.Server` per task socket tags accepted connections via WeakMap. Hijacked / upgraded requests pipe both directions raw (covers buildx `Upgrade: tcp` → HTTP/2). Supervisor wires the proxy into `LocalInProcessSandbox.createTaskContainer` — pre-allocates the socket, two-phase `registerTask` upsert (placeholder pre-create, real `parentDockerId` post-create), bind appended to `HostConfig.Binds`, `stopTask` unregisters, `shutdown` closes the proxy. Cgroup parent (`cogmo-task-<dashless-uuid>.slice`, systemd) pins every container in the task tree to a shared subtree; aggregate-budget enforcement at the slice level deferred (per-leaf `NanoCpus`/`Memory`/`PidsLimit` already cap the task container). Tool gate orchestrator wiring: `runExecuteStreaming` builds canonical pattern → replays task-scoped decision log (newest-first, `Allow for task` wins) → runs `policy.evaluate` → on `prompt`, emits `coding/task/permission-requested` + blocks on `step.waitForEvent("coding/task/permission-decision")` filtered by `taskId + requestId` (7-day timeout as abandoned-task safety net per slice3-plan #4 "block indefinitely on Telegram outage"). New `StepWaitForEvent` type derived from the SDK. `transport.coding.respondPermission(params, tapperHandle)` — identity-checked Inngest event emit. Telegram inline keyboard (Once / Task / Deny) with single-char wire codes to fit the 64-byte callback_data limit. Per-channel Inngest function listens on `coding/task/permission-requested` and posts the keyboard. Reaper cron runs every minute (1m is Inngest's cron minimum; slice3-plan/design target 30s — close enough at personal scale): TTL pass (current-instance containers, depth-DESC kill order), orphan pass (Docker-side discovery, kills survivors of dead Cogmo instances), stale-DB pass (rows live in DB but missing from `docker ps` marked `exited`), then network/volume TTL pass. Sysbox + proxy-socket bind-mount compatibility verified on Fedora 43 via the karellen COPR (sysbox-fs only virtualises `/proc`+`/sys`, doesn't shadow `/var/run`). New env vars: `SANDBOX_PROXY_SOCKET_DIR`, `SANDBOX_HOST_DOCKER_SOCKET`. 1155 unit tests pass. Design markers upgraded: `sandbox.md` → Data Model / Proxy Design / Reaper / Lifecycle / Crash Recovery / Networks-Volumes-Images all `[confirmed]`; `coding-delegation.md` → Tool gate `[confirmed]`. Slice 4 ships verify + push + draft PR (P1.5).
- [x] Phase 6 P1 slice 2 — plan approval + execute. New `pending_verify` enum value (migration 0011) between `executing` and `verifying`; naming convention recorded (`awaiting_X` = human gate, `pending_X` = automatic transient). `ClaudeCodeBackend.execute(ctx, sessionId)` wraps `claude --resume <sid> --permission-mode acceptEdits` with shared JSONL parser + new tool_use/tool_result extraction (deduped). `CodingStreamingRegistry` — per-task in-process EventEmitter pub/sub, single-process by design (matches industry split: durable bus for state, side-channel for token streams). `Service.coding.delegate` becomes async submit (`{taskId, status: "queued"}`); admission check + Inngest emission at the boundary; `DELEGATE_CODING_GUIDANCE` rewritten for the LLM. Plan-approval inline keyboard with strict identity check via `transportStore.resolveUser`; atomic `approvePlanIfPending` / `cancelTaskIfActive` (idempotent on double-tap). New `Transport.coding.{approvePlan, cancelTask}` namespace + 4 new `TransportError` codes. Conversational revise (cancel + new delegate) — Cursor / Devin / Claude Code's pattern. `coding-task-execute` Inngest function on `coding/task/plan-approved` reuses-or-recreates the task container via `listContainersForTask` + `inspectContainer`; `--resume <sid>` rehydrates from per-task home volume. `CodingProgressSubscriber` posts a single Telegram message edited in place across plan + execute (header / status line / activity / token counter / 500ms throttle); per-channel Inngest function on `coding/task/start` resolves the chat via `getActiveSessionsForConversation`. Token counts surface via `execute_complete`; `resource_usage` schema gains `cost_usd`; orchestrator translates camelCase→snake_case at persistence boundary. retries=0 stays on both functions (CLI session + file edits non-replayable mid-stream). Design markers upgraded: `coding-delegation.md` → Backend Interface (plan + execute) / Container Lifecycle / Resume procedure / Plan gate all `[confirmed]`. Slice 3 adds the tool gate + Docker proxy + reaper (P1.4); slice 4 ships verify + push + PR (P1.5).
- [x] Phase 6 P1 slice 1 — autonomous coding plan-only end-to-end. New modules `src/sandbox/` (containers + cogmo_instances tables, `LocalInProcessSandbox` via dockerode 5, sysbox-runc default with explicit `SANDBOX_RUNTIME` env, label-based lineage, instance-id crash recovery) and `src/agent/coding/` (coding_repos + coding_tasks + conversation_id, `ClaudeCodeBackend.plan()` parsing claude's stream-json output, idempotent `git worktree add`, orchestrator with durable `step.run` boundaries using `GetStepTools<typeof inngest>["run"]` derived from the SDK, `delegate_coding` agent tool + `Service.coding` namespace running the orchestrator inline, `cogmo/devbase:slice1` image with claude-code 2.1.119 pinned). New GHA `sysbox-e2e` job installs sysbox 0.7.0 on `ubuntu-24.04` and asserts `/proc/self/uid_map` shows host-uid remapping. Bug fixes: `exec.wait()` deadlocked on consumed-stdout — captures the exit signal eagerly now; `containerCreated` flag was set after the DB write — now stamped before so failed `setTaskContainerId` still triggers cleanup. Slice 2 wires the durable Inngest function + plan-approval keyboard (P1.3); slice 3 adds the Docker proxy + sibling support (P1.4); slice 4 ships verify + push + PR (P1.5).
- [x] Telegram session/profile/model management — `/sessions` (inline keyboard ≤10, numbered text >10), `/resume <alias>`, `/name <alias>` (`-` clears), `/end`, `/profile [list|switch|new|edit|delete]` with interactive FSM for new/edit (prompt→model→confirm; `skip` keeps current in edit mode; `/cancel` aborts), `/model [<model>]` gated by `user_selectable`. Schema: `profiles.user_id` nullable (NULL=org, set=user), `model_providers.user_selectable BOOLEAN NOT NULL`, `messages.profile_id` + `messages.model` NOT NULL with `'<legacy>'` sentinel backfill, new `aliases` table. Transport: `TransportError` extended (`profile_not_found`/`profile_in_use`/`profile_name_taken`/`model_unavailable`/`alias_taken`/`operation_not_permitted`/`access_denied`); `resumeConversation(address, handle, {alias|conversationId})`; `conversations.{list, getCurrent, setAlias, setProfile}`; `profiles.{list, create, update, delete}` with caller-owns ACL (org profiles always reject mutations via `access_denied`); `models.list` filters `user_selectable`. Per-turn snapshot stamping in `handle-message.ts` via new `load-turn-snapshot` step — all rows in a turn share the same `(profile_id, model)`; mid-turn profile/model switches take effect next turn. `ProfileDialogs` FSM in-memory per chat; `bot.on("message:text")` intercepts dialog input before agent emit.
- [x] Context fast path — account for output tokens in `shouldSkipCounting`. New `messages.output_tokens` column (NOT NULL, sentinel `-1` for pre-migration + non-final batch rows), `insertMessages` requires `lastMessageOutputTokens`, `getLastInputTokens` → `getLastTokens` returning both terms, estimator becomes `lastIn + lastOut + ceil(newChars/4) < budget * 0.5` with either negative/null forcing a real count.
- [x] OpenTelemetry export — opt-in via `OTEL_EXPORTER_OTLP_ENDPOINT`, SDK loaded via `--import` hook (early-returns when unset), `inngest/experimental` `extendedTracesMiddleware` for one-trace-per-run, hand-picked instrumentations (http/undici/pino — skipped pg auto), gen_ai spans on both LLM providers, tool/memory/debounce spans, `cogmo.llm.tokens`+`agent.iterations`+`debounce.wait_ms` metrics, OTLP/proto over HTTP for LGTM/SigNoz/Grafana Cloud, in-memory test harness + 4 unit tests + 1 integration test, DEPLOYMENT.md observability section.
- [x] Non-interactive setup — `cogmo setup --non-interactive` reads `COGMO_LLM_*` / `COGMO_TELEGRAM_*` env vars (with `_FILE` variants), validates credentials live, writes `llm_providers` + `model_providers` + `channels` + `user_identities` + encrypted secrets. Idempotent on re-run. Interactive wizard also gets a "how do I know it's working?" post-setup block with the bot username.
- [x] Secrets infrastructure — `secrets` table, `@noble/ciphers` + `@noble/hashes`, AES-256-GCM + HKDF, `SecretsStore`, `_FILE` env support, `gen-key` subcommand, `ConfigResolver` (DB-first env-fallback), `.dockerignore` deny-all
- [x] Multi-provider LLM — `llm_providers` + `model_providers` routing table, provider dispatch via model → provider resolution, `ANTHROPIC_API_KEY` removed from env (DB-only)
- [x] Channel CRUD + allowlist enforcement — `createIdentity` / `updateChannelCredentials` / `removeChannel`, Telegram adapter enforces identity via `resolveUser`, `user_identities` rows replace `TELEGRAM_ALLOWED_USERS` env
- [x] Seed refactor — `src/setup/seed.ts` with named exports, `src/seed.ts` delegates
- [x] Setup wizard — `@clack/prompts`, `src/setup/` module, `main.ts setup`, re-runnable, `--reset`, `--non-interactive`, inline help
- [x] Stage 1 evolution — Observer extracts corrections from conversation/idle, persists to steeringRules with graduation (2+ observations → active), consolidation at 30+ rules
- [x] Typed LLM calls — `chatTyped()` with Zod schemas, `responseFormat` on ChatParams, `ThinkingBlock` + extended thinking, `clearOldThinking` pre-pass, retry with feedback injection
- [x] Response routing — source routing per `design/transport/response-routing.md`
- [x] Initialize Node.js project (package.json, tsconfig, Biome, Vitest)
- [x] Set up TypeScript build (tsx watch, tsup production)
- [x] Install core dependencies
- [x] Set up directory structure
- [x] Docker infra via testcontainers (`scripts/dev-infra.ts`, `test/containers.ts`)
- [x] Drizzle schema — module store pattern (`agent/store/`, `transport/store/`)
- [x] Design doc confidence markers (`[confirmed]`/`[proposed]`/`[research]`)
- [x] Decide orchestration: Inngest over BullMQ (event-driven durable execution)
- [x] End-to-end message pipeline — LLM abstraction, agentic loop, Inngest orchestration
- [x] Unit tests — 84 tests across 14 files
- [x] Initial Drizzle migration — 9 tables (users, profiles, conversations, messages, steering_rules, channels, channel_sessions, inbound_messages, user_identities)
- [x] CLAUDE.md — design philosophy, testing rules, DI stance, code style, encapsulation rules
- [x] Tool system refactor — Service interface (ACL boundary), Zod validation, defineTool helper
- [x] `memory_recall` and `memory_retain` tools with Service integration
- [x] Hindsight wired into orchestrator via Service
- [x] Hindsight retain/recall round-trip verified — integration test with Ollama via testcontainers
- [x] Telegram adapter — grammY, long polling, Transport interface, AdapterModule contract
- [x] Telegram auth — user ID allowlist via env var
- [x] Structural alignment — renamed `channels/` → `transport/`, store pattern, new events (`inbound/arrived`, `response/ready`), Adapter/Transport interfaces
- [x] Direct channel adapter — event-driven via Inngest, replaces old CLI adapter
- [x] Channel registry — table-driven adapter discovery via `AdapterModule` + `satisfies` barrel
- [x] Shared respond factory — `createRespond()`, generic per-channel respond in registry
- [x] Shared test factories — `src/test/factories.ts`, all test files use shared mocks
- [x] Transport tests — `transport.test.ts` covering resolveSession, createConversation, emit
- [x] neverthrow at Transport boundary — `emit()` and `createConversation()` return `Result<T, TransportError>`
- [x] `#private` fields on all classes — ES2022 runtime enforcement
- [x] `private constructor` + `static async create()` — TelegramAdapter
- [x] CLI entrypoint — `src/cli.ts` with `serve`/`seed` commands, Dockerfile ENTRYPOINT
- [x] Seed script — `src/seed.ts`, extracted from `ensureDefaults()`, idempotent
- [x] `contentToText()` helper — centralized JsonValue → string conversion
- [x] Console script — `scripts/console.ts`, standalone readline + DB polling
- [x] Replace `pg` driver with `postgres.js` — pure JS, faster, zero API changes
- [x] Store unit tests with PGlite — 31 tests, real SQL without Docker, driver-agnostic `Database` type
- [x] Move `test/containers.ts` → `dev/containers.ts` — shared with dev-infra
- [x] Three-tier test structure — unit (PGlite), integration (Docker + in-process), e2e (Docker + subprocess)
- [x] llmock replaces mock-anthropic + Ollama — fixture-based deterministic LLM responses
- [x] Integration tests — pipeline (in-process bootstrap + Inngest) + memory (Hindsight + llmock)
- [x] E2e smoke test — subprocess boot, migrations, one message end-to-end
- [x] `bootstrap()` extracted from `src/index.ts` — reusable wiring for tests
- [x] Test philosophy documented in CLAUDE.md — tiers, PGlite, llmock, env injection, Telegram strategy
- [x] Slim Hindsight image — external providers, no PyTorch, ~5s startup
- [x] llmock `requestTransform` — deterministic fixture matching with timestamp/UUID stripping
- [x] Docker-based e2e — builds from Dockerfile, tests real production artifact
- [x] Rename `cli.ts` → `main.ts`, fix Dockerfile entrypoint
- [x] CI: Codecov coverage, JUnit test reports, `dorny/test-reporter` job summaries
- [x] Hindsight provider config — production (OpenRouter + zerank-2) vs test (slim + llmock)
- [x] Streaming — unified DeliveryRouter, StreamingAdapter/StreamHandle, TelegramStreamHandle, chatStream() on LlmProvider
- [x] Web tools — web_search (Tavily), web_answer (Perplexity Sonar), fetch_url (readability + SSRF)
- [x] File tools — read_file/write_file/list_files via S3 (MinIO), Service.files namespace
- [x] Enhanced get_current_time — structured JSON, timezone, day of week, UTC offset
- [x] MinIO container in dev-infra and test setups
- [x] System prompt overhaul — auto-generated tools section, per-service guidance, conditional onboarding
- [x] Core memory blocks — DB table, core_memory_update/read tools, auto-recall, getUserContext callback
- [x] OpenAI-compatible LLM adapter — OpenAICompatibleProvider via official SDK
- [x] ImageBlock — canonical type, both adapters, Telegram photo handler, AttachmentStore (separate binary storage)
- [x] Steering rules table — already existed, injected into system prompt per invocation
- [x] Context window management — `countTokens()` on LlmProvider, model registry, three-layer compaction pipeline, usage tracking
- [x] Full tool invocation history — `messages.content` stores `ContentBlock[]`, `AgentLoopResult.newMessages`, pair-aware compaction (`snapToPairBoundary`)
- [x] Debounce wiring — debounce-router, debounce-idle, debounce-maxwait, entry guards, resume policy
- [x] Prompt caching — Anthropic cache_control, OpenRouter passthrough
- [x] Session lifecycle — Inngest idle timer, resolveSession staleness, `/new` command
