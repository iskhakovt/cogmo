# Todo

## Next

### Coding delegation & sandbox

- [ ] `p3` Revisit the two host-side fetches that write the skill branch's ref only if it actually bites. For the skills repo `coding_repos.local_path` IS `COGMO_SKILLS_PATH`, and both `fetchFeatureBranch` (bare → `refs/heads/<branch>`) and `auto-register-skill` write that one ref in that one repo, ordered only by the `coding/task/pr-opened` emit that precedes the fetch-back step. Measured on git 2.54 in the production shape — local repo lacking both the ref and its objects, 5000 packed `cogmo/*` refs, two fetches at 0/5/25/100/250ms stagger against a 163-283ms fetch: **0 failures in 125 trials**. Both fetches target the same value, so the loser either creates the ref or finds it already there; no update, no lock, no error. Contention IS reachable when the two fetches observe *different* old values (20/20 failures with `cannot lock ref … is at X but expected Y`), which needs the branch tip to move on origin mid-flight — a re-push from a retried task is the only path there, and `retries: 0` on both functions makes those separate runs. `core.filesRefLockTimeout` (default 100ms, git-internal so `RETRY_DISABLED` does not bypass it) covers the lock-file variant but not the old-value one; raising it would also make a stale `.lock` from a crashed process fail slowly instead of fast. Impact if it ever does fire is worse than the odds suggest, which is why this stays on the list at all: `coding-auto-register-skill` is absent from `CODING_ORCHESTRATOR_FUNCTION_IDS`, so a throw there is reconciled by nobody — no `coding/task/failed`, no user-facing event, `retries: 0` — leaving an open PR beside a skill that silently never registered, visible only as an Inngest function failure in the logs. Suspect this if `skill-authoring.integration.test.ts` flakes on `expect(run.status).toBe("success")` with a git error naming the ref, or if a skill PR opens and no `skills` row appears.

- [ ] `p3` Transactional outbox for `coding/task/start`, to remove the ambiguity a thrown `inngest.send` leaves behind rather than choosing a side of it. Today `CodingService.delegate` inserts the row, sends, and on a throw terminalizes the row if it is still `queued` — chosen so the task's fate matches the `is_error` the caller is about to see, since the alternatives are an accepted event planning work the user was told had failed, or a `queued` row holding the repo's only admission slot forever. An outbox removes the choice: write `coding_tasks` and an outbox row in one transaction, have a relay publish and mark it sent, and the send stops being a dual write. Cost is a relay process (or a cron sweep of unsent rows) plus a table — real infrastructure for a failure mode that needs the bus to half-fail, which at single-user scale has never been observed. Same shape would cover `plan-approved` and `cli-done`. See `design/coding-delegation.md` → Flow, and the reasoning at the `startOrRelease` catch.

- [ ] `p3` Decide whether `runAgentLoop` (the non-streaming agent loop in `src/agent/loop.ts`) stays. It has no production caller — only tests reference it — and it now carries the full `turnKey` / `ToolCallContext` / `streamed` plumbing the streaming loop needs, so every change to the loop contract is paid for twice. Either delete it (CLAUDE.md → Code Style: "no code just in case") or name the caller that justifies it. Not a mechanical call: it is the only non-streaming path, so deleting it forecloses a batch/eval harness that doesn't want a delivery surface.

- [ ] `p2` `cleanup-orphan-run-branches.ts` sweep-repo: the staleness verdict (`ageMs >= RETENTION_MS` on a per-invocation `new Date()`) gates whether `delete-${taskId}` steps exist, so a task near the 7-day boundary can insert a new step id into an established graph mid-run → `Could not find step` failure. Move the verdict into the memoized `load-tasks` step (compute `stale: boolean` per task there) so the step graph derives from durable state. Also: `resolveGitHubIdentity` (DB + PAT decrypt) re-runs on every one of N+3 boundaries — acceptable cost, but worth a step if N grows.
- [ ] `p3` Decide whether the Claude CLI exec should carry git's maintenance suppression too. `commit-push.ts` and `teardown.ts` now disable `maintenance.auto`/`gc.auto` on the git commands Cogmo itself runs against the task tree, because a detached `git maintenance run --auto` writing into `/workspace` reaches the host tree through the bind mount and races `removeWorktree`. The CLI subprocess (`agent/coding/claude.ts`, the `execStreaming` that runs `claude`) passes no `env` at all, so any `git commit` the *model* runs during execute spawns maintenance in that same tree with nothing suppressing it. The same reasoning that settled the `runGit` question applies — suppression is correct exactly where the tree is about to be deleted, and this one is — but this call site is different in kind: it shapes the environment the model works in rather than a command we issue, and the CLI does its own env scrubbing, so check what actually survives into the subprocess before assuming an added variable arrives.
- [ ] `p3` Claude-CLI deny-list via mounted `settings.json` for non-negotiables that hold even under `--permission-mode bypassPermissions`. `permissions.deny` rules are enforced regardless of permission mode — the right place to encode invariants the sandbox can't catch on its own. Concrete deny entries: `Bash(rm -rf /:*)`, anything writing outside `/workspace`, anything reading `/.cogmo-askpass/*` or `/root/.ssh/*`, and any `Bash(git remote ...)` that sets a remote other than `origin` (today the orchestrator's PAT auth scopes blast radius, but a deny-rule pins it structurally). Today nothing protects against, e.g., claude `rm -rf /workspace/.git` mid-execute and pushing an empty branch. Implementation: bake `settings.json` into the `cogmo-devbase` image under `/etc/claude/settings.json`, or mount per-task from the host alongside askpass. Pairs with the denial-circuit-breaker entry below — defense in depth.
- [ ] `p3` Denial-rate circuit breaker on claude-cli execute. When the model is denial-spiraling (perms denied, tool errors, refusals), it's usually wedged and continuing burns money. Execute should monitor stream-json events for `tool_result.is_error === true` patterns; on N consecutive errors or M total in a single run, terminate the CLI and set the task to `failed` with `failure_reason = "denial-spiral circuit breaker: <N> consecutive tool errors"`. Would have surfaced the `bypassPermissions` bug on the first record attempt instead of waiting ~30s for claude to give up after 15 turns. Concrete counter shape: track `consecutiveErrors` + `totalErrors` in the stream-json reader, reset `consecutive` on any successful tool_result, fire teardown at (3, 20).
- [ ] `p3` `result.session_id` from the CLI's terminal stream-json event is the source of truth, not the `<sid>` we passed to `--resume`. Today execute stores the plan-phase sessionId in `coding_tasks.session_id` and replays it on retry; the CLI's terminal `result` carries the *real* session id, which can differ if `--resume` created a fork. Reader should overwrite `coding_tasks.session_id` from the final `result` event and fall back to scraping it from intermediate stream events if `result` is empty. Today a session-id divergence would surface as "next retry's --resume rehydrates the wrong conversation" — only visible if execute fails mid-stream and gets retried with the stale id. Low priority because retries:0 on execute means it doesn't retry; promote when the retry budget loosens (paired with the loadIdentity-pin item already captured).
- [ ] `p3` Self-disabling streamer pattern for `CodingProgressSubscriber` (and adjacent UI fan-out). A broken Telegram socket should never cancel an in-flight claude run. Today the subscriber surfaces send errors via `log.warn` but doesn't formalize "stop streaming, keep working" — the next message-edit's retry storm hits the same broken socket. Concrete shape: wrap the stream-edit calls in a small `disablingStreamer({ send, onDisabled })` helper that tracks a per-runId enabled flag, flips false on first error, calls `onDisabled` once for logging, and routes subsequent updates through normal (non-streaming) delivery.
- [ ] `p3` Collapse `failTaskIfNonTerminal` to a single SQL statement using a CTE + RETURNING — current shape is `UPDATE ... WHERE id=$1 AND status NOT IN (...) RETURNING id` followed by a disambiguating `SELECT status WHERE id=$1` when the update returned zero rows. Two round-trips per non-matching call. Under REPEATABLE READ the row can't disappear mid-tx so the gap is theoretical, but the single-statement form is simpler to reason about: `WITH prior AS (SELECT id, status FROM coding_tasks WHERE id=$1 FOR UPDATE) UPDATE coding_tasks t SET status='failed', failure_reason=$2 FROM prior WHERE t.id=prior.id AND t.status NOT IN ('pr_open','failed','cancelled') RETURNING prior.status AS prior_status`. Saves one round-trip and removes a subtle invariant question. Flagged on PR #267 review as "not blocking." Pairs with the existing `transitionTaskStatus` shape — fold both into the same pattern when one lands.
- [ ] `p3` Replace `await new Promise(r => setTimeout(r, 4_000))` in `reconcile-on-failure.integration.test.ts` (asserting negatives) with a still-not-fired tripwire function — register a small Inngest function that pushes to a buffer on `coding/task/failed` for a specific synthetic task id, then assert the buffer stays empty within a short window. Today the two negative tests add ~8s wall-clock to the integration suite. Acceptable cost; flagged on PR #267 review as low-hanging fruit if the suite grows.
- [ ] `p3` Telemetry: surface `coding-task-reconcile` fire rate so a steady drip implies a deeper transport regression that the in-worker `try/catch` isn't catching. The reconcile logs `warn` on every flip + `info` on every `already_terminal` no-op, but no metric/alert. Two shapes: (a) Pino counter exporter → Prometheus / OTel — emit `coding.reconcile.flipped{function_id}` and `coding.reconcile.skipped{reason}`; threshold-alert when `flipped > 0` over any 24h window (current production baseline should be zero — every fire indicates a worker-disconnect class we haven't fixed); (b) opportunistic — surface "task was reconciled, not gracefully failed" in the Telegram `/status` message for the affected conversation so the user sees the diagnostic provenance. Wire whenever a metrics sink exists; today the in-process log line is the only signal. Flagged on PR #267 review as observability backlog.
- [ ] `p3` Cron reconcile sweeper for non-terminal `coding_tasks` rows — strictly redundant with the `coding-task-reconcile` subscriber on `inngest/function.failed` (which is the primary defence against the 4-day wedge incident), but the report's finding 3 suggested a daily belt-and-braces cron that joins `coding_tasks` against Inngest's `trace_runs` / `function_finishes` tables (or the REST API) and reconciles any non-terminal row whose latest run is `FunctionFailed` / `Cancelled` and older than 30 min. Skip if the subscriber proves reliable; revisit if telemetry shows a wedge it missed. Care needed: must not race the 7-day permission-prompt `step.waitForEvent` (that path is parked durably, not failed). See design/coding-delegation.md → Worker-death reconciliation, design/sandbox.md → Wall-clock and idle timeouts.
- [ ] `p3` Add idle timeout to verify orchestrator's `runVerifyStreaming` — today verify uses its own `Promise.race(setTimeout)` for the total cap (returns `exitCode = TIMEOUT_EXIT_CODE = 124`, distinct from `ExecTimeoutError`). The wedge-resilience PR added `timeoutMs`/`idleTimeoutMs` to every other exec callsite, but verify was left alone because mapping `ExecTimeoutError` back to the `124` user-visible exit code is non-trivial. Wire when a transport wedge during verify actually surfaces; today the outer sandbox teardown bounds the leak.
- [ ] `p2` Automated-trigger plan-approved emit — evolution / signal_pipeline tasks finish the plan orchestrator at status `executing` but no one emits `coding/task/plan-approved`, which is the sole trigger of `createCodingExecuteOrchestrator`. So those tasks stall after planning: row says `executing`, no CLI ever resumes. Pre-existing since slice 1 ("Slice 1 only handles the user trigger path" comment); surfaced during PR-313 review. Two ways to fix: (a) extend the autoapprove block in `runCodingTask` to also call `approvePlanIfPending` + emit for non-user triggers (no Telegram side effect because they have `conversation_id IS NULL`, the progress subscriber bails) — but the status flip would happen twice (plan→executing in the orchestrator, then approve→awaiting→executing via the execute path); requires choosing one transition source. (b) Refactor the execute orchestrator to also trigger on a separate `coding/task/auto-execute-ready` event for automated triggers, keeping `coding/task/plan-approved` user-only. (b) is cleaner because it stops conflating "human approved" with "automated path." Block until evolution-driven coding tasks are actually exercised; today the author doesn't trigger them.
- [ ] `p2` Guard the in-container git ownership path (CVE-2022-24765 / `safe.directory`). In-container git over the bind-mounted standalone clone assumes the container CLI user's uid equals the host worktree-owner uid — otherwise git's dubious-ownership check fires on the *first* in-container command (the `git status --porcelain` in `runCommitAndPush`), failing every coding/skill task. Today the invariant holds via sysbox id-mapped mounts plus the `vscode(1000) == cogmo(1000)` contract (`src/sandbox/askpass.ts` modes comment), so nothing sets `safe.directory` in production. But it's undefended: a `runc` deployment (`allowPrivilegedRunc`, or any non-sysbox host — `src/sandbox/supervisor.ts:189`) with mismatched uids hits "detected dubious ownership" with no test catching it. `worktree-in-container.integration.test.ts` only proves *gitdir resolution* — it sets `safe.directory=/workspace` via `GIT_CONFIG_*` precisely because it runs `alpine/git` as root over a host-uid-owned mount, so it explicitly does NOT exercise the ownership path. Fix options: (a) make the ownership path robust regardless of runtime — bake `git config --system --add safe.directory /workspace` into the `cogmo-devbase` image, or have the orchestrator run it in the container before the CLI starts (cheap, deployment-behavior change → discuss first); (b) `chown` the bind-mounted clone to the container CLI uid at `create()` time; (c) add an integration test that runs in-container git as a non-matching uid WITHOUT `safe.directory`, asserting both the failure mode today and the fix. Default sysbox path is safe; land before recommending/supporting runc deployments. Flagged on PR #396 review (the standalone-clone change); see `design/coding-delegation.md` → Task Model ("In-container ownership assumption").
- [ ] `p2` Cron pruner for `refs/cogmo-wip/<task-id>` refs — `teardownWorktree` (`src/agent/coding/teardown.ts`) force-pushes failed/cancelled worktrees to this namespace so users can fetch + inspect Claude's in-progress code (`git fetch origin refs/cogmo-wip/<id>:wip-<id>` → `git worktree add <path> wip-<id>`). Refs are append-only and accumulate; design/coding-delegation.md targets a weekly cron that prunes any WIP ref whose `coding_tasks` row is terminal AND >30 days old. Personal scale tolerates manual cleanup for now; promote when ref count crosses ~50 or pushes start hitting GitHub's per-repo ref limit.
- [ ] `p3` Phase 3b.2 follow-up: `cogmo/devbase` reachable from Daytona — verify Daytona's builder pulls `ghcr.io/iskhakovt/cogmo-devbase:<version>` cleanly on first `create()`. Folds naturally into the Phase 3c.6 real-Daytona conformance suite below. (Sibling integration-test-arm follow-up shipped in Phase 3c.1 via `FakeDaytonaSandboxClient`.)
- [ ] `p3` Phase 3c — Daytona observability + production hardening. Deferred follow-ups from PR #173 review (the P3 items): (a) **real-Daytona integration test** gated behind `DAYTONA_API_KEY` env, mirrors `runner.sysbox.integration.test.ts` shape — exercises one full skills tier-2 invocation against a live Daytona Cloud sandbox so our SDK mocks can't lie about the WS demuxing or session-cleanup behaviour; (d) **Daytona reaper / orphan reconciliation** — `reconcileCrashedInstances` is a no-op today (Daytona auto-persists/auto-archives are the provider's job), but a Cogmo-side audit pass that lists sandboxes labelled `cogmo.instance != current` and either resumes or deletes them would catch stale state from crashed retries; (e) **per-sandbox cumulative usage** — when Daytona SDK adds per-sandbox CPU-seconds / memory-seconds / cost USD (today the SDK only exposes org/region-level instantaneous quotas), wire it into `resource_usage`. 3c.5 shipped the raw-lifecycle subset that we can capture Cogmo-side; this is the upgrade once upstream lands. (f) **Cogmo-baked Daytona base image** — bake a slim image with `stdbuf` (for `claude` block-buffering defeat) and any other Cogmo-side deps so first-task latency drops from 2-5 min (Daytona builds + snapshots `cogmo/devbase`) to ~30s (snapshot already exists). Blocked on Daytona TS SDK exposing private-registry credential management — currently only via Daytona Dashboard. Each of (a)-(f) is independently scopable; ship in any order based on operational pain.
- [ ] `p3` Sysbox-real-Claude subprocess e2e — slice 4.0i ships an integration test using a Gitea testcontainer + scoped octokit fetch + host-spawning fake sandbox, which exercises the orchestration but not the sandbox's bind-mount machinery or a real Claude session inside a sysbox container. A true `.e2e.test.ts` would: (a) launch the cogmo binary as a subprocess, (b) spawn a real sysbox container via the supervisor, (c) run claude inside it on a fixture repo with a trivial change, (d) assert the verify orchestrator opens a draft PR upstream. Blocked on either a self-hosted GHA runner with sysbox installed, or a runc-on-GHA workaround that swaps `SANDBOX_RUNTIME=runc` for the test (lose userns isolation in CI but keep it in production). The existing sysbox-e2e GHA job (`.github/workflows/sysbox-e2e.yml`) already installs sysbox 0.7.1 on `ubuntu-24.04` for the supervisor's sysbox-flavoured tests; this would extend it to drive the full task pipeline.
- [ ] `p3` Bootstrap-tar fallback for no-remote worktree teardown — design's row 3: when a task fails BEFORE its first commit on a brand-new repo without a remote yet, the WIP push has nowhere to go. Fall back to `~/.cogmo/archives/<task-id>.tar.zst` with 14-day TTL. Doesn't apply to `/repo add`-registered repos (always have a remote). Land if the no-remote bootstrap path is ever supported.
- [ ] `p3` Managed sandbox API backend (Daytona / E2B / Fly Machines) — second impl of `Sandbox` in `src/sandbox/supervisor.ts`, alongside the local dockerode+sysbox impl. Picked at deploy time via config; users without Docker+sysbox available point at a managed provider. Daytona is the leading target — sysbox-native, preserves slice 3's sibling-container design. Mirror the OpenAI Agents SDK's `SandboxClient` shape so further providers are per-provider deltas, not rewrites.
- [ ] `p3` Surface Claude Code OAuth token expiry in `/status` ~30 days before the 1-year mark — secrets row already carries `created_at`. Today the only signal is "next coding task fails with `claude_auth_failed`". Wire a configuration-health section into the existing `/status` Telegram command (currently conversation-status only at `src/transport/adapters/telegram/sessions-ux.ts:113`); same row check could surface other rotation-driven secrets (GitHub PAT quarterly, Tavily/fal yearly) under one banner. Defer until a real near-expiry incident validates that "fail at next task" isn't enough notice. See `design/coding-delegation.md` → Subscription Auth → Token rotation.
- [ ] `p2` Daytona orphan sandbox sweep cron (PR #275 follow-up). The orchestrator's `deleteByTaskId(taskId)` in the failure catch reaps the common case via the `cogmo.task` label index, but loses the race when Daytona refuses delete on a sandbox still in `building_snapshot` state (observed during v1.66.0 retries — labelled sandbox sat building for 25+ min, billable). Implementation: daily Inngest cron mirroring `cleanup-orphan-run-branches.ts` — `daytona.list({})`, filter for `cogmo.task`-labelled sandboxes whose `coding_tasks` row is terminal (`failed` / `cancelled` / `pr_open`), `try { sdkSandbox.delete() } catch` per item so a state-transition-locked sandbox gets retried on the next tick. Skip non-terminal rows (mid-approval). Daytona's own 15-min idle reaper bounds the worst-case cost; promote when the residual leak surfaces in real billing. See `design/sandbox.md` → Deferred / Phase 3c → "Daytona-side orphan sandbox sweep".
- [ ] `p3` Daytona snapshot GC cron — snapshots accumulate per-version forever on the Daytona account (each `cogmo-devbase-<version>-<hash>` is created fresh on every cogmo bump; `DaytonaSandboxClient` never deletes them). Pairs naturally with the orphan-sandbox sweep above. The rename-on-rebuild path in `#ensureSnapshotActive` raises the upper bound: every rebuild leaves a `<base>-r-<hex>` orphan whose disposal depends on Daytona's own reaper. Implementation: `daytona.snapshot.list()`, derive the currently-deployed image set from `env.COGMO_DEVBASE_IMAGE` + `env.COGMO_SKILLS_IMAGE`, delete snapshots whose base name doesn't match (including all `-r-<hex>` rebuild variants — the suffix is a rebuild marker, not a different image). Marginal at single-user scale (3 active snapshots tops); ship when count crosses ~20 or the Daytona dashboard becomes a chore, or sooner if a chronically-stuck `REMOVING` state surfaces in the warm logs. See `design/sandbox.md` → Deferred / Phase 3c → "Snapshot GC".
- [ ] `p3` Sanitize git output before persisting in `failure_reason` / `coding/task/failed` events. `runCommitAndPush` returns `output: string` (combined stdout+stderr from `git status/add/commit/push`) and the orchestrators thread it into `coding_tasks.failure_reason` plus the Inngest event payload. Git redacts the PAT in URLs via its own URL parser today (`https://x-access-token:***@github.com/...`), but if a future error path ships the unredacted credential we'd persist it. Run the output through a regex sweep at the orchestrator-side composition site: strip `https?://[^/@]+:[^/@]+@` to `https://***:***@` and replace `gh[pousr]_*` / `github_pat_*` substrings with `<REDACTED>`. Same pattern as `KNOWN_API_KEY_PATTERNS` in `src/test/daytona-mock.ts`. Apply at `runCodingExecute`'s `reason = "execute push failed ..."` + the verify-orchestrator equivalents in `runCodingVerify`. Defensive — no known incident.

- [ ] `p3` Wrapper-level real-Daytona conformance scenario — the existing `daytona-conformance.integration.test.ts` calls `sandbox.process.executeSessionCommand` directly with raw command strings, bypassing `buildShellCommand` entirely. That's how the `exec`-builtin bug shipped: no test layer exercised the cogmo wrapper against real Daytona. Add a `RECORD=1`-gated scenario that routes through `DaytonaSandboxSession.execStreaming` (e.g. `session.exec(["echo", "hi"]).then(h => h.wait())` with a tight wall-clock bound), so a future regression that breaks Daytona's session-completion detection hangs at record time and surfaces to the operator before the fixture lands. Pairs with the existing `create-exec-delete` / `python-upload-fail` scenarios in `src/test/daytona-conformance.integration.test.ts`. The unit-level `buildShellCommand` regression already in `exec-streaming.test.ts` covers the immediate bug; this is belt-and-braces for the underlying contract.
- [ ] `p3` Replay-able Daytona claude-CLI integration test — `claude-cli-daytona.integration.test.ts` is still `describe.skip`-gated. The infrastructure landed with the skill-authoring scaffold PR (DaytonaMock PTY/binary-WS support, pipe-stdin in `exec-pty.ts`, SDK patch for bare `require('stream')`); remaining work is a one-shot recording: `RECORD=1 DAYTONA_API_KEY=... ANTHROPIC_API_KEY=... pnpm test:integration src/agent/coding/claude-cli-daytona.integration.test.ts`, verify replay is byte-stable, ungate. Note: that test only exercises `plan` mode against `cogmo-devbase`, so it's not blocked by the same git-transport gap that blocks the full skill-authoring test.
- [ ] `p3` "Building sandbox image…" pending state for tasks that race boot-time snapshot warm. Current behaviour: when a task arrives during a fresh-deploy snapshot warm, the per-task `step.run("ensure-image-present")` awaits the same memoised promise and the task transparently waits ~60s for the warm to finish. That's correct, but the user sees a silent ~minute of nothing before the plan stream opens. Surface a `pending: image-warming` event the conversation renderer can show as "Building sandbox image for v<version>, ~60 seconds…" so the user understands the delay. The orchestrator already has the warm-promise visibility; the missing piece is an event + a Telegram render path. Low priority — the visible symptom is "first task after every deploy feels slow" and the underlying cost (snapshot create) is bounded by Daytona's own build pipeline.

### Memory & evolution

- [ ] `p2` Authenticate the Hindsight API. `HindsightClientOptions` already takes `apiKey` ("sent as Bearer token in Authorization header") and `src/memory/hindsight.ts` constructs with `new HindsightClient({ baseUrl })`, passing nothing — so the memory server accepts any request that reaches it. Nothing in `design/memory.md`, `design/infrastructure.md` or `DEPLOYMENT.md` records a decision here; the secrets design scopes itself to "LLM provider API keys, channel bot tokens, web tool keys", and Hindsight is absent from that list, which reads as never-considered rather than considered-and-rejected. Its protection is positional: it listens on an internal address (`http://hindsight:8888`) that nothing outside the deployment routes to. That assumption is what `ctx.http` broke — a host-side request on a skill's behalf carries the host's routing — and the address guard now compensating for it is the weaker layer: it races DNS rebinding, and every future host-side fetch-for-a-skill has to remember to re-implement the same check, where a token would not care how the request arrived. Shape: `hindsight_api_key` as a `secrets` row (the established home for exactly this), threaded into the client constructor, plus `DEPLOYMENT.md` guidance since existing deployments need it set on both sides. **Confirm first that the self-hosted server enforces a key** — the SDK field implies support, but the server's behaviour with an unset key is unverified, and a token the server ignores is worse than none for being believed.

- [ ] `p2` Evolution failure-reflector — subscriber on `conversation/errored` (emitted by `handle-message`'s `onFailure` handler) that builds a failure-pattern corpus to feed the steering-rule auto-correction stage. Distinct from the Hindsight Observer (which sees `response/ready` and extracts user facts) — failed turns produced no user-facing assistant content, so they go straight to the evolution side. Highest signal-to-noise input the reflector will get; ship it as the first input source when the evolution subsystem comes online.
- [ ] `p3` Profile-class isolation: integration test for round-trip — `*.integration.test.ts` exercising Observer→Hindsight→recall with a class scope: stage retain under classed profile A, drain via Observer, recall through a service scoped to class A (visible) and class B (hidden). Unit coverage of leaf emission + tag stamping is strong, but the `any_strict` × `profile_class:*` interaction against real Hindsight is unverified end-to-end. Touches the existing Docker-based integration tier (PG, Redis, Inngest, Hindsight, llmock) — minimal new scaffolding.
- [ ] `p3` Profile-class isolation: Observer-level monitor for `compartment:misc` — log-based or metrics-based alert when the rate of `misc`-classified facts crosses a threshold (suggested 15-20% of a daily window). The compartment was added as an explicit "none of the above" escape hatch with prompt-only guardrails ("use sparingly … never as a tie-breaker"); LLMs ignore that instruction under load, so we need observability to know if `misc` is silently absorbing facts that should land in `personal`. Implementation: roll the `byCompartment` count Observer already tracks into a Prometheus counter or structured log-warn when the daily ratio crosses threshold. Cheap to add when a metrics surface exists.
- [ ] `p3` `memory_retain` accepts explicit `compartment` / `classes` — today the agent stages a retain with content only and the Observer drain classifies. Letting the agent self-tag (`memory_retain({ content, compartment: "intimate", classes: ["intimate"] })`) cuts a classifier round-trip and avoids LLM-classifier mistakes on edge cases the agent has clearer context for. Touch points: extend the `memory_retain` tool spec (Zod input schema), thread the explicit values through `service.memory.stageRetain`, write them into `pending_memories` so the drain stamps the chosen tags instead of asking the classifier. Mostly relevant if the misc-monitor flags real classification drift; until then the implicit-classify path is fine.
- [ ] `p3` Cross-class "see-also" recall — escape hatch for "intimate occasionally needs to know what assistant knows about my schedule". Today scopes are strict (`any_strict` excludes anything tagged with a non-allowed `profile_class:*`). Two shapes: (a) `memory.recall({ ..., classScope: "any" })` runtime override on a single recall — broader than the profile's standing scope, used when the agent decides the question is cross-context; (b) `profiles.memoryScope.profileClassesAlso: string[]` standing-scope addition that's always included regardless of strict matching. (a) is more LLM-controllable, (b) is more declarative. Defer until a real cross-class need surfaces in conversation; until then the strict boundary is the feature.
- [ ] `p3` Fold reflect `context` into the query string — Hindsight's SDK marks `ReflectRequest.context` as `@deprecated` ("concatenated with the query. Pass context directly in the query field instead"). Our `HindsightMemoryProvider.reflect` and `Service.memory.reflect` still forward `context` separately; `buildReflectBody` writes `body.context` when set. Migration: append `\n\nContext: ${context}` to the query inside `buildReflectBody` (or one layer up in `Service`), drop the `context` field from `ReflectOptions`. No-op user-visible behavior; quiets the upstream deprecation. Cheap follow-up — flagged in PR #148 review.
- [ ] `p3` Observer unit test — mock `step.run` to cover min-message guard, conditional consolidation, return shape
- [ ] `p3` Idempotent correction inserts — dedup key (rule text hash + category + source) to prevent duplicate rules on step retry
- [ ] `p3` Per-`profileId` rule consolidation — `consolidateRules` (`src/agent/evolution/consolidate-rules.ts`) groups rules by `channelType` so each merged row preserves its scope, but the same fix is missing for `profileId`. The function is called per-profile, yet `getCorrections` returns both profile-scoped (`profileId === $p`) AND global (`profileId IS NULL`) rules; `replaceRules` hardcodes `profileId: null` on the merged row, so a profile-scoped rule merged with a global rule silently loses its profile scope. Today the bug is dormant because rules are almost always global — `applyCorrection` in `extract-corrections.ts` writes `profileId: null` from extraction by default — so profile-scoped rules ~never exist in practice. Trigger to act: when profile-scoped corrections become routine, or if `/profile new`-style admin starts seeding profile-scoped rules. Fix shape: extend `R.groupBy` to key on a `(profileId, channelType)` tuple, run consolidation per group, preserve both fields on `replaceRules`.
- [ ] `p3` Fingerprint short-circuit for memory consolidation. Skip the daily `reflect()` sweep when the bank hasn't changed since the last successful run. The clean gate is a "was anything retained since the last sweep" signal, not a content hash: Hindsight owns the bank server-side and `recall()` returns only a query-scoped `Memory[]` (`{ content, type, metadata }` -- no stable `id` / `category` to key on), so Cogmo has no local full-bank snapshot to digest the way Odysseus does. Use a Hindsight change cursor / last-retain timestamp if one is exposed; otherwise track a per-bank dirty flag set on every `retain` / `stageRetain` drain and cleared after the sweep. cf. Odysseus `services/memory/memory_extractor.py` (`_fingerprint_entries` over `(id, text, category)` + a tidy-state sidecar) -- but that hashes a locally-owned `memory.json` we don't have. Caveat: `reflect()` is fire-and-forget (`async: true`) and dedup is Hindsight's job, so this gates the Phase 2 consolidation cron (under Later), not anything per-turn -- confirm that cadence exists before building.
- [ ] `p3` Hindsight retain failure monitoring — `async: true` means background pipeline errors are invisible to caller. Poll `operation_id` status or wire webhooks; surface failures to logs/metrics.
- [ ] `p3` Adopt `recallResponseToPromptString()` — richer memory injection with temporal fields, entity summaries
- [ ] `p3` Use native OpenRouter provider for prod Hindsight — replaces `openai` + custom base URL workaround
- [ ] `p3` Hindsight error classification — wrap `HindsightClient` calls in `HindsightMemoryProvider` with `AbortError` logic for known-permanent failures (4xx auth/validation). Currently retries indiscriminately, wasting up to ~10s on every misconfigured request. Requires investigating `HindsightClient`'s error shape (is it `e.status`? `e.response.status`? `e.code`?) before classifying safely.

### Agent loop, context & conversations

- [ ] `p2` Decide the delivery-push failure policy for chat streaming — today a push failure inside the `llm-iter<N>` step body (user blocked the Telegram bot mid-stream → grammY 403, no `status` field → classified retriable) fails the step and each per-step retry opens a fresh billed `chatStream` before the run fails; the same class in `degraded-reply` deliberately keeps plain retry semantics; and after the first failure the handle's rejected `#pending` makes the outer catch's `delivery.abort()` rethrow the original error before classification runs. Options: (a) self-disabling streamer for the chat path — the ledgered `onEvent` catches push errors, flips a per-run disabled flag, and the turn completes without streaming delivery (persist + batch/notify still run; mirrors the `CodingProgressSubscriber` entry above); (b) classify push failures distinctly (wrap in a marker error inside the ledgered onEvent → NonRetriable in the wrapper) so a dead socket fails the run in one attempt instead of three billed streams. (a) is better UX when only one of several sessions is dead. Same-family: teach `isRetriableProviderError` (or a sibling predicate) about grammY's `error_code` so Telegram 4xx classify deterministically wherever they surface.
- [ ] `p2` Per-run memo for inbound attachment resolution — `handle-message` re-downloads every inbound image/document from S3 and re-base64-encodes it in the bare body once per invocation. Promoted from `p3` after the durable-iteration work: the boundary count per turn went from ~4 to roughly `3×iterations + tools + 4`, so a 5-iteration turn carrying a 700 KB photo now does ~15 S3 round-trips and ~15 base64 encodes where it used to do ~4. Attachment paths are write-once, so a small in-process LRU keyed by path (same lifecycle thinking as `#activeStreams`) is trivially correct; cap it (~20 entries) so photo turns don't pin heap. Deliberately NOT step state (the doc's rejected alternative) — the payloads are exactly what must stay out of Inngest state. The same multiplier applies to `listProfileClasses` and the per-turn service/registry assembly, which are cheap enough to leave.
- [ ] `p3` Pin sampling (temperature/seed) on the chat path — with `llm-iter<N>` durable, provider-default sampling only matters on the mid-step-crash retry path (a re-run drain can mint different tool calls than the cached tool steps expect). Tightening shrinks that window. Cheap hardening, not a fix; see design/crash-recovery.md → Per-tool durability.
- [ ] `p3` Evaluate Inngest checkpointing against the self-hosted server — SDK 4.18 defaults to AsyncCheckpointing (multiple steps per request; falls back to per-boundary orchestration on parallel steps, step retries, lease migration, and the connect `maxRuntime` of 300s). Measure how often handle-message turns actually collapse to 1-2 executions vs. fall back (the 2026-08-19 report observed 15 steps across 5 executions), and whether tuning `maxRuntime`/`bufferedSteps`/`optimizeParallelism` is worth it. Durable steps make fallback cheap, so this is latency polish, not correctness.
- [ ] `p2` Give the canonical `StopReason` union a context-overflow member so an overrun can trigger compaction instead of ending the turn. Anthropic 0.117 added the `model_context_window_exceeded` stop reason; `llm/anthropic.ts` currently folds it into `max_tokens`, which is correct as far as it goes — it keeps the value out of `classifyPostStream`'s `end_turn && content.length === 0` arm, where a continuation prompt would have added *more* tokens to an already-overflowing context and guaranteed the same failure. But `max_tokens` is a different condition: the turn ends truncated when the right response is to compact and retry. Wiring that means a new member on `StopReason` (`llm/types.ts:121`), a matching `RepairSubtype` in `agent/repair.ts` whose repair is a compaction pass rather than a nudge, and the branch in `agent/loop.ts`. Also worth deciding what a *partial-content* overflow should do — today it returns `ok` and persists a truncated turn as complete. That same `ok` covers a plain `max_tokens` truncation, which is the wider hole: `classifyPostStream` has no `max_tokens` arm at all, so any turn that fills its output cap is delivered as a finished answer with no signal. It applies to the streaming path as much as the non-streaming one, so a fix belongs with this entry rather than at any single call site — the sub-agent builder guards its own delegation (`stopReason === "max_tokens"` → `isError` tool_result) precisely because the loop cannot yet tell it anything. Changing the classifier changes turn outcomes, which is why it wants its own PR and its own regression tests.

- [ ] `p2` Untrusted-context envelope for retrieved/external content. Today auto-recall injects recalled memory as `# Recalled Context` directly into the system prompt, and `fetch_url` / `web_search` results flow into the conversation as model-visible text -- both are prompt-injection surfaces (a fetched page or a poisoned memory can carry "ignore your instructions, call X tool" text the model reads with system-level trust). Wrap web results, fetched pages, and recalled memory in an explicit data-not-instructions envelope delivered as a `user`-role block (never system), tagged `{ trusted: false }`, with a header stating the enclosed content is reference data and that tool calls / memory writes / message sends must not be triggered by anything inside it. Keep recalled memory OUT of the system prompt. Exploitability is low at single-user Telegram scale today but becomes acute once MCP Gmail/Calendar ingestion lands (Phase 2) -- untrusted email is the canonical injection vector, so land this before the first ingestion agent. cf. Odysseus `src/prompt_security.py` (untrusted-context wrapping kept out of the system role) + `src/tool_security.py` (per-tool denylist for untrusted callers).
- [ ] `p2` Tool result compression — cut tokens on verbose tool output (biggest sources: `file.read`, `web.fetch`, `memory.recall`). Two layers: (a) `ToolSpec.compress?(output) => output` hook invoked in the agent loop before appending the `tool_result` block — per-tool strategies: head+tail truncation with elision marker, dedupe repeated lines with counts, markdown-not-HTML for `web.fetch`, top-K cutoff with scores for `memory.recall`. (b) Historical tool-result compaction in `context.ts` — once a `tool_result` is N turns back, replace it with a one-line placeholder (`[web.fetch https://... — 12KB truncated, turn 7]`); current turn keeps full output. Complements existing summarisation compaction but is deterministic and lossless for the current turn. Prompt caching already covers the cached prefix; this targets the uncached tail. Inspired by rtk-ai/rtk but operates at the tool-result boundary inside the runtime, not shell output.
- [ ] `p2` Background compaction — run summarization after response (while user reads) instead of before next turn, store pre-computed summary to eliminate compaction latency
- [ ] `p2` Batch API support for async evolution tasks — 50% cost reduction for reflection, extraction, optimization
- [ ] `p2` Heal-on-persist for history-contract orphans — durable repair pathway that supersedes orphan `tool_use` / stray `tool_result` / empty-content rows in `messages` so the DB converges to clean state. Pieces designed and prototyped in the closed PR #129 (referenceable for the structural shape): `messages.superseded_at` + `messages.superseded_by` columns gating `getHistory` visibility, `applyHeal({supersededIds, insertions, ...})` transactional store op, pure `computeHealPlan(originalsWithIds, validated)` that diffs original vs. validated and rewrites the tail, `HEAL_PERSISTABLE_KINDS` allowlist gating the persist on known-safe `RepairKind`s, `getHistoryWithIds` returning `{id, message, lastInboundMessageId}` so heal rows inherit the existing cursor (heal must NOT advance "we responded up to"), heal-history step in `handle-message` running between `load-history` and the agent loop, repair branch on `recover-conversation` that retries `inbound/ready` after applying the heal. Currently deferred because PR #125's in-memory `sanitizeHistory` already keeps every load clean for the LLM call — the only marginal value is DB convergence, which is handleable with a one-off psql `DELETE` at single-user scale. Ship trigger: a concrete second writer to `messages` (`/repair`, MCP tool-result ingestion, evolution-stage rewrites) that bypasses the loop's sanitizer, or a real recurrence proving the validator-at-load path isn't enough. ~3700 LOC including the snapshot JSON; design + tests already worked through. Pairs with the cross-tier orphan `tool_use` repair test in *Testing & CI* below — best landed together.
- [ ] `p3` Tag synthetic `tool_result` blocks structurally instead of by content-prefix string match — `isVolumeClusterNudge` currently filters by `content.startsWith("You have called \`${toolName}\` ")` because the synthetic and the real `tool_result` share the same Anthropic block shape. If a tool ever legitimately returns content beginning with that exact phrase (e.g. `memory_recall` surfacing a prior assistant turn that contained the nudge text), `summarizeToolHistory` would silently drop it from outcome counts. Risk is low at current tool surface but the coupling between `formatVolumeClusterContent` and `isVolumeClusterNudge` must stay byte-identical at the prefix. Two harden options: (a) sentinel marker the model can't produce (` VC_NUDGE` etc.) prefixed onto the synthetic content; (b) tag synthetic blocks with a distinguishing field — e.g. add an internal `synthetic?: true` field to the in-memory ContentBlock union (NOT sent to the API; stripped on serialize) and have the filter check that field instead. Option (b) is cleaner — single source of truth for "this is a synthetic" — but needs a sweep of the ContentBlock type and the streaming/persistence boundaries to ensure the flag never leaks to the provider. Two producers today: (1) the volume-cluster nudge (`formatVolumeClusterContent` → `[You have called \`T\` …]`), (2) Strategy 0 same-tool supersession summaries (`compactSameToolClusters` → `[Same-tool cluster: …]`). Today's architectural separation between them and their consumers keeps the two disjoint (Strategy 0 mutates the compacted prefix; `summarizeToolHistory` only scans post-`initialLength`), so no current helper has to filter both. But the *typed* option (b) above would discriminate by `synthetic?: { source: "volume_cluster" | "strategy_0" }` and serve any future consumer that needs a unified "is this synthetic" predicate. Ship trigger: a real false-positive (a tool returning a synthetic-prefixed string), or a downstream helper that needs to filter both producers simultaneously. Flagged by Gemini on PRs #278 + #287.
- [ ] `p3` Lift tool-call dispatch into a `Dispatch[]` plan — `executeToolCalls` and `runOne` currently take an optional `interceptions?: ReadonlyMap<string, ContentBlock>` parameter that only the volume-cluster trigger feeds (PR #278). Fine for one intercept source; would compound into multiple optional threading params if rate-limit caps, per-tool cost caps, side-effect-budget caps, or dispatch-time steering rules ever want to push synthetics into the same dispatch chain. The clean abstraction: compute one `Dispatch = { kind: "run", block, spec } | { kind: "intercept", block, synthetic }` per `tool_use` block upstream of `executeToolCalls`, with each intercept reason as a pure `(block, ctx) => ContentBlock | null` function composed via `??`. `executeToolCalls` reduces to "given a plan, run the `run` entries and return the `intercept` synthetics in order"; the optional param goes away. Trigger to ship: a second dispatch-time intercept source actually lands (the synthesis-on-degrade work *doesn't* count — it disables tools at the API level, not at dispatch). Until then, the threading is fine. Flagged by Gemini on PR #278.
- [ ] `p3` Reconsider internal tag stripping — removed from Phase 1 (thinking blocks + tool calls cover current use cases). Revisit if a concrete need for inline orchestrator-only metadata emerges that tools can't express
- [ ] `p3` Consider moving `IDENTITY`/`ONBOARDING` constants (`src/agent/prompt.ts`) out of code — they're defaults that `profile.basePrompt` can override, but if evolution ever wants to tune the baseline identity/onboarding tone we'd need them in a table. Not urgent — profile override path already exists.
- [ ] `p3` LLM-suggested conversation auto-naming — generate alias from first turn or on demand. User can accept/edit/reject. Aliases remain user-controlled; this is a suggestion path, not auto-apply.
- [ ] `p3` Profile-switch tool-removal regression test — when `/profile switch` removes a tool whose `tool_use` blocks live in conversation history, verify the next turn still loads cleanly (Anthropic API accepts historical tool blocks even if not in current `tools`, but a regression test pins the contract).

### Skills, voice & transport

- [ ] `p2` Extend the egress gate from credentials to read effects, and detect egress the classifier cannot see. Two halves of the same gap. (a) `computeRiskTier` pairs `network` with `secretsCount > 0` only, so `effects: [reads_memory]` plus a `network:` allowlist falls through to `if (all.size > 0) return "notify"` and deploys live — a skill can `ctx.memory.recall("")` the whole bank and POST it out with a one-line notification. The argument that made secrets-plus-network an `approve` trigger applies unchanged to reading the user's memory or data: the question is whether the skill can obtain something worth taking and also reach somewhere to send it. Candidate rule: any of `reads_memory` / `reads_user_data` / `reads_filesystem` alongside `network` gates to `approve`. (b) `ast-rules.ts` derives effects from *imports*, so a tier-1 skill that posts to Slack, SendGrid or Stripe through `ctx.http.post` imports nothing and produces no detected `sends_message` / `sends_email` / `financial` at all — the detection side of the classifier is blind in exactly the tier that just gained a network. Candidate: a `CALL_RULES` entry for `ctx.http.post` / `ctx.http.request` with a non-GET method, treated as `sends_*` when the destination is not also declared read-only, or simply as an undeclared-egress signal. Worth doing together, since (a) without (b) still leaves the write path unclassified. Deliberately not folded into the PR that added the secrets pairing — it widens the gate to a new class of effect and touches the detection table, which is its own change with its own test matrix.
- [ ] `p2` Close the remaining IPv6 forms `isBlockedAddress` waves through. Confirmed by reading `ctx-handler.ts`: there is no `ff00::/8` branch, so `ff02::1` / `ff05::1:3` / `ff0e::1` return `false` while the v4 arm blocks all of 224.0.0.0/4; no Teredo `2001::/32` branch, so `2001:0:53aa:64c:0:5bfe:7f00:1` carrying 127.0.0.1 passes though the sibling 6to4 and NAT64 forms are handled; and the IPv4-compatible branch's `!(bytes[12] === 0 && bytes[13] === 0)` guard — there so `::` and `::1` reach the loopback check — also leaks every `::0.0.x.y`, which `isBlockedV4` would refuse via `a === 0`. `::ffff:0:127.0.0.1` slips the `mapped` test too, since that requires `slice(0, 10)` all-zero and byte 8 is `0xff`. Multicast is the one worth having: it is reachable, and the v4 arm already refuses its equivalent, so the two arms disagree today. Fix is a handful of byte tests next to the existing ones plus table-driven cases in `ctx-handler.test.ts` mirroring the v4 list. `p2` rather than `p1` because reaching any of these needs a hostname whose resolver returns them, which is a step beyond a skill being careless.
- [ ] `p2` Stop `ctx.http` handing skills response headers that describe a body they never see. `#httpRequest` returns `Object.fromEntries(response.headers)`, so a gzip response arrives with `content-encoding: gzip` and upstream's `content-length` while `body` is the decoded string undici already inflated and `readCapped` decoded — a skill doing the documented thing (checking `content-length` before parsing, or inflating because the header says gzip) corrupts or rejects a good response. `Object.fromEntries` also collapses repeated headers to the last value, so two `set-cookie` lines arrive as one. Nearly every real API sends gzip, so this is the common path rather than an edge. Shape: drop `content-encoding` / `content-length` / `transfer-encoding` on the way out (the same body-bound set `daytona-mock` already filters, worth sharing a constant with), and either expose `set-cookie` as an array or document the collapse. Reported by review against Node 24 with a local gzip server; reproduce before fixing.
- [ ] `p2` Bound `ctx.http`'s use of the libuv threadpool. `lookup()` is blocking `getaddrinfo` dispatched to `UV_THREADPOOL` (4 threads by default), and `withDeadline` abandons the promise without cancelling the underlying call — its own docstring says as much. A skill issuing four or more concurrent requests to hostnames with a blackholed resolver occupies every thread for the OS resolver timeout (5-40s) regardless of the deadline the skill was given, and everything else in the process that uses the threadpool — attachment reads, zlib, crypto, `fs` — queues behind it. Nothing in `#httpRequest` caps per-run concurrency. Two levers, probably both: a small per-run semaphore around `http.request`, and `dns.resolve*` (which uses c-ares and does not touch the threadpool) instead of `lookup`, noting that swap changes behaviour — `resolve4`/`resolve6` skip `/etc/hosts` and the system resolver order, which is arguably correct for this guard but should be a deliberate decision. Single-user scale makes the collision unlikely today; it is filed because the blast radius is the whole process, not the skill.
- [ ] `p3` Revisit the 16 MiB NDJSON frame limit and what rides on it. `MAX_BUFFER_BYTES` in `worker-sysbox/transport.ts` went 4 → 16 MiB so a 5 MiB `ctx.http` response survives JSON escaping, but the limit is shared and three things follow. (a) It does not actually clear the 5 MiB cap: control bytes escape 6:1 as `\u0000`, so a body that is a quarter control characters already exceeds 16 MiB, and the overflow surfaces as a fatal transport error rather than the catchable `response_too_large` the API advertises — `HttpRequestArgsSchema.body` has no `.max()` either, so an oversized *request* takes the same path. (b) `skill-tool-builder.ts` stringifies a skill's whole `output` into the tool result with no cap, so a tier-2 skill returning 15 MiB now reaches the agent loop where 4 MiB used to stop it — against the 100 KB `MAX_READ_LENGTH` the repo applies to `ctx.files.read`. (c) `split2` accumulates to the limit before erroring and its per-chunk flatten is quadratic; review measured ~1.1s of blocked event loop and ~265 MB RSS at 16 MiB versus ~70ms at 4 MiB, and its `maxLength` counts UTF-16 code units, so a constant named `_BYTES` is really a character count and a multi-byte body hits the limit at roughly half the advertised size. Worth one pass that caps the tool result independently, maps transport overflow onto `response_too_large`, and either renames the constant or converts it. `p3` because a skill returning megabytes is not a shape anything produces today.
- [ ] `p3` Size `ctx.http` deadlines from the wall clock actually remaining. The ceiling comes from the manifest's whole `wall_clock_s`, so a skill that has already spent most of its budget can start a request the watchdog kills mid-flight instead of receiving the `timeout` error the method advertises. Computing what remains needs the moment the task reached a runtime, and nothing available where the handler is built is that moment: construction precedes the runner's own bookkeeping, and dispatch entry precedes a tier-2 pool acquisition that can be most of the budget on a cold Daytona pool (~30s per `design/skills.md`). Both were tried; both under-estimate, and under-estimating is the worse failure — with the deadline floored at 1ms, a cold-pool skill would see every request time out instantly, where over-estimating only degrades a late request to what any long operation does today. The real fix is a lifecycle hook: `pool.invoke` and `runOnWorker` already receive the `ctxHandler`, so whichever of them sends `task_invoke` can mark the start, and the ceiling becomes accurate for both tiers. That means widening the `CtxHandler` interface or threading an `onTaskStart` callback through the pool, which is why it is not bundled with the request-surface work.
- [ ] `p2` Pin `ctx.http` connections to the address the SSRF guard checked. `#httpRequest` resolves the hostname, refuses loopback / link-local / private / CGNAT ranges, then hands the original URL to `fetch`, which resolves it again — so the address vetted and the address connected to are not guaranteed to be the same one. A name whose authoritative server answers public first and private second defeats the guard on the second answer, which is DNS rebinding. Everything else about the guard holds; this is the gap between "refuses internal destinations" and "cannot reach internal destinations". Matters because that guard is the only thing between skill-authored code — LLM-written, plausibly prompt-injected — and services whose whole security model is being unroutable: Inngest can be posted to (`p1` under *Observability & operations*) and Hindsight takes no auth (`p2` under *Memory & evolution*). The fix is a dispatcher that resolves, checks and connects as one step: `fetch` accepts undici's `dispatcher`, and an `Agent` with a `connect.lookup` returning only the vetted address closes it. **`undici` is not importable today** (`ERR_MODULE_NOT_FOUND`) so this is a runtime-dependency decision under CLAUDE.md, not a code change — discuss before adding. The dependency-free alternative does not work: rewriting the URL to the IP literal with a `Host` header is fine over `http`, but on `https` the handshake takes the IP as SNI and certificate validation fails, and `fetch` exposes no `servername`. Exploiting this needs attacker-controlled DNS plus a timing win, which is a step up from a skill merely doing something careless — hence `p2` rather than `p1`.

- [ ] `p3` Give `ctx.http` a path for binary responses. `readCapped` decodes every body as UTF-8 and returns a string, so a skill fetching a PNG, PDF or gzip gets U+FFFD where the bytes were — silently mangled rather than a loud failure, and with no alternative call that returns bytes. Two shapes worth weighing: refuse non-text content types outright (clear, but guesses from a header the server controls), or return base64 with a flag on the response so the skill can decode. Base64 costs a third in size against the 5 MiB cap, which argues for making it opt-in per request rather than automatic. Low urgency while the documented use is REST APIs returning JSON; the trigger is the first skill that wants to fetch an image or a file.

- [ ] `p2` Make bound secrets invisible to skill code. Stage 2 of `design/skills.md` → *Secret exposure and egress control*. `SkillSecretSchema.binding` (`src/skills/types.ts:88-105`) already parses a `destination` plus an optional `url` / `header:` substitution and is ignored at runtime; honouring it means `ctx.secrets.get` returns `cogmo-secret://<uuid>` for a bound secret, `#httpRequest` matches the destination against the binding and substitutes the real value at the `fetch`, and each substitution is audited to `skill_context_calls`. Unbound secrets keep the pull model — the documented fallback for non-HTTP credentials — and keep classifying as sensitive. Both tiers reach `secrets.get` through the same host bridge (`images/skills/src/cogmo_skills_runtime/runner.py:108` calls it over the same RPC tier 1 uses), so there is one implementation site. Tier 2's raw `httpx` is not a hole to plug: a placeholder sent over a raw socket arrives as an opaque UUID, earns a 401, and discloses nothing, which makes `ctx.http` the path of least resistance rather than a rule to enforce. What this buys over stage 1 is defence against a compromised *allowed* destination, and against a secret escaping through logs, error strings, memory writes or a return value — real, but the residue after the allowlist rather than the main event, hence `p2`.
- [ ] `p2` CLI channel management commands — `main.ts channel add telegram --token=...`, `channel list`, `channel remove` (store layer covered by `p1` Channel CRUD; this is the CLI surface)
- [ ] `p3` Per-adapter Transport ACL — wrap `Transport` per adapter with a permission policy (e.g., third-party adapters cannot mutate profiles, cannot create `receive: all` sessions). Returns `operation_not_permitted` (already in `TransportError` union). Deferred until first non-trusted adapter ships.
- [ ] `p3` grammY native test primitives — use `bot.handleUpdate()` + `bot.api.config.use(transformer)` instead of `vi.mock("grammy")`
- [ ] `p3` Telegram e2e via Test DC + tgintegration — real user on Telegram test servers, TypeScript/mtcute client
- [ ] `p3` Consider web interface adapter for e2e testing
- [ ] `p3` Free-text deny reason via reply prompt — `transport.skills.denyDeploy` accepts an optional `reason` and `runner.denyDeploy` logs it, but the Telegram callback always passes `undefined` (one-tap Deny is faster). If you find yourself wondering "why did I deny that one?" looking at logs, add a follow-up reply prompt (mirror `/repo add`'s FSM dialog) or a `denied_reason TEXT` column on `skill_deploys`. Defer until the audit value is felt — single-tap Deny is good UX otherwise.
- [ ] `p3` Attachment-store compression layer (zstd-1, magic-byte dispatch). Wrap `AttachmentStore` so `upload` runs bytes through `zlib.zstdCompressSync` at level 1; `download` checks the four-byte zstd frame magic `0x28 0xb5 0x2f 0xfd` and decompresses iff present, otherwise returns bytes raw. Same convergence trick `wrapAttachmentStoreWithEncryption` uses — pre-wrap blobs stay readable, new uploads compress, the bucket converges. Compose order: `compress → encrypt → s3`, never the reverse (encrypted bytes don't compress, and reversing the order opens the CRIME-class side channel that per-blob streams currently avoid). Built into Node 22.15+'s `node:zlib`, no new dep. zstd-1 emits raw blocks for incompressible input at near-memcpy throughput with ~13-20 bytes of frame overhead, so applying uniformly is effectively free when the payload can't compress and 40-80% win on text. Trigger to ship: measure the actual attachment mix first — if it's dominated by already-compressed image formats (likely), the layer is dead weight until a text-shaped ingestion path (MCP Gmail/Calendar, RAG-over-PDFs, etc.) lands. Pair with whichever PR introduces that path. See `src/transport/attachment-store.ts` + `encrypted-attachment-store.ts` for the wrapper pattern.
- [ ] `p3` Per-capability notify (`delivery.notifyVoiceTargets(text)`) — `delivery.notifyConversation(conversationId, text)` fans out to every active session regardless of capability. Today's only consumer is the over-cap "(too long for voice — see above)" follow-up in `handle-message`'s voice-delivery step (PR #149); a Direct CLI session that never had a voice clip in flight still receives that note. Build alongside the existing `voiceTargets` partition the `DeliveryHandle` already maintains: a new method on the prepared handle that fans out only to voice-capable sessions, sharing the same per-session-error-resilience as `deliverVoice`. Low-impact UX nit — promote when a real mixed-channel deployment (Telegram + Direct + Slack) makes the spurious cross-channel note actually annoying.
- [ ] `p3` Investigate flaky Pyodide wall-clock-cap test — `src/skills/worker-wasm/host.test.ts > runOnWorker (Pyodide) > enforces wall-clock cap and falls back to worker.terminate on tight loops` times out at 15s when run under full-suite load (`pnpm test`) but passes in isolation (~10s). Symptom is the test exceeding its own `testTimeout: 15_000` rather than the wall-clock cap firing too late, so the bug is most likely worker-startup contention when ~120 other test files share the Vitest worker pool — Pyodide cold-start eats most of the budget before the tight-loop kill path even runs. First investigation step: add a `console.time` around the `runOnWorker` call vs. the worker import, run with `--pool=forks --maxWorkers=1` and full-suite parallel back-to-back, see whether import time alone blows the budget. Fixes likely fall into: (a) bump `testTimeout` to 30s with a tighter assertion on actual termination time, (b) warm a dedicated Pyodide worker for this file via Vitest `globalSetup`, or (c) split the wall-clock-cap subset into its own test file with `--no-isolate` and explicit pool config. Do NOT just bump the timeout without verifying — if cold-start really is variable enough to push past 15s under load, the production wall-clock cap (same code path) has the same exposure when the host machine is busy.
- [ ] `p3` Skills warm-pool replacement-spawn observability — `SysboxWorkerPool#postInvoke`'s lazy replacement spawn (`pool.ts` ~line 412) does `void this.#spawnOne().catch(log.warn)`. If a misconfig breaks every spawn (image deleted, sandbox unreachable), the pool sits below `min` between invokes with only a log line recording it. Not actually silent end-to-end — the next user invoke triggers another spawn and surfaces the same error to the user — but operators monitoring metrics see a gap. Fix shape when warranted: add `lastSpawnError?: { message: string; at: number }` to `pool.stats()`, set on every `#runSpawn` failure (ignoring `PoolDisposedDuringSpawnError`), cleared on next success. Optionally a circuit breaker that opens after N consecutive failures and rejects new invokes with `pool_unhealthy` instead of retrying forever. Triggers: P3.5 telemetry lands (somewhere to send it — `skill_runs.pool_health` field or a `pool_events` table), or a real incident shows operators surprised by silent degradation. Personal-scale today: log + next-invoke surfacing is enough. Reviewer-flagged on PR #181, deferred as personal-scale-acceptable.
- [ ] `p2` Schedule `uv cache prune` -- the wheel cache under `/skill-venvs/.uv-cache/` is content-addressed but unbounded. `design/skills.md` promises an "`uv cache prune` on a separate cadence"; today no cadence exists. The `skill-venvs-reaper` Inngest function (`src/skills/deps-reaper-function.ts`) explicitly protects `.uv-cache` from deletion (only sha256-hex dirs are candidates). On a long-running Daytona deployment with many distinct lockfile versions, growth is bounded by `(name, version, platform tag)` distinct combinations -- manageable at personal scale, unbounded long-term. Cheap fix: sibling Inngest function on a quarterly cron that mounts the deps volume and runs `uv cache prune --ci`. Quarterly cadence (vs daily reaper) and isolated failure budget keep the two reapers independent. Flagged on holistic deps-stack review.
- [ ] `p2` Document the author-side `uv` version contract. The register-time byte-compare requires the author's `uv pip compile` to match the runtime image's `uv` version byte-for-byte. `--no-header` (already documented in `design/skills.md` -> Lockfile) is necessary but not sufficient -- a uv minor bump can change the lockfile sort order, hash format, or whitespace, and every committed lockfile would then fail `requirements_lock_stale` until re-recorded. Pin the runtime image's `uv` version (today: whatever ships in `cogmo-skills:<version>`) and surface it in the Dependencies section so authors know which `uv` version to use locally. Pair with a CI check that compiles a fixture lockfile and asserts it matches a known-good byte sequence. Flagged on holistic deps-stack review.
- [ ] `p3` Sysbox negative-path e2e for the deps stack -- second sysbox test that commits a deliberately-broken `requirements.lock` (mismatched hash, yanked version, or pyodide-incompatible native dep) and asserts `result.status === "error"` with a specific `error` substring (`populate_failed`, `requirements_lock_*`, etc.). Today's e2e (`runner.sysbox.integration.test.ts > invokes a tier-2 skill with declared deps`) is happy-path only; failure surfaces are pinned by unit tests on `ensureVenvPopulated` but not re-exercised at the wire. Cost: ~30 s wall-clock + one fresh volume per failure mode. Defensible to defer (unit tier covers it cell-by-cell), but a single test would catch broader regressions cheaply -- populator script, activation path, AND runner error-mapping converge on one assertion. Flagged on PR #329 review.
- [ ] `p3` Exclude the live worker's own tmp dir from the opportunistic 10-min orphan sweep. `POPULATE_SCRIPT` in `src/skills/deps.ts` runs `find /skill-venvs -maxdepth 1 -name '*.tmp.*' -mmin +10 -exec rm -rf` at the top of every populate, intending to clean orphans from prior crashed populates. Healthy populates finish well under 10 min, so the corner case is narrow: a populate alive >10 min (huge dep set against slow PyPI, host timeout disabled, etc.). The `find` would clobber the live populate. One-line patch: `-not -name "$HASH-$PY_ABI.tmp.$WORKERID"` to exempt the current worker's tmp dir (tmp basename is `<hash>-<ABI>.tmp.<workerId>` after PR #333; `-name` matches the basename `find -maxdepth 1` returns; `-path "*/...tmp.../*"` looks like the right pattern but matches descendants only, leaving the dir itself selectable). Flagged on holistic deps-stack review.
- [ ] `p3` Per-skill UID / per-hash read-only mount -- concrete sketch for the multi-author skill story. The `[research]` Security note in `design/skills.md` calls out the trust precondition; this entry sketches the upgrade path. Touch points: (a) `images/skills/Dockerfile` provisions a pool of unprivileged UIDs (e.g. 2000-2999) with shared `/skill-venvs` read access; (b) `cogmo_skills_runtime/supervisor.py` `os.fork()` is followed by `os.setuid(skillUid)` before activation -- skill code runs under a UID distinct from the supervisor and from sibling skills; (c) populate writes `<hash>/` then `chmod -R a-w + chown -R skillUid:skillUid` to bind the venv to one skill; (d) reaper recovers UID -> skill-name mapping from a host-side table. Bigger lift than current scope; queue for when multi-author skills materialise. Flagged on holistic deps-stack review.
- [ ] `p3` Batch Pyodide compat lookups for multi-dep manifests. `checkPyodideCompat` (`src/skills/pyodide-compat.ts`) loops `pypiHasPureWheel(name, version)` per declared spec; each is a separate `fetch` to PyPI's JSON API. A manifest declaring 8-12 deps fires that many round-trips serially at register time. PyPI's JSON API doesn't support batch queries, but `Promise.all` would parallelise the round-trips (still N requests, but ~N/concurrency wall-clock). Cheap follow-up once register latency becomes user-visible. Flagged on holistic deps-stack review.
- [ ] `p3` Add `COGMO_INSTANCE_ID` and use it as the deps-cache volume suffix when `COGMO_SKILLS_DEPS_VOLUME` is unset on Local-Docker. Today's bare default `cogmo-skills-deps-cache` is shared across every Cogmo on a host; a compromised skill on either side could pre-stage malicious `<hash>/.ready` entries the other consumes. Daytona is out of scope here — backends advertising `depsCacheSharing: 'per-sandbox'` never mount a shared volume and the cross-instance poisoning vector doesn't exist there. Shape: new `COGMO_INSTANCE_ID` env var (operator-set, opaque string), wizard generates a random suffix at first setup. When `COGMO_SKILLS_DEPS_VOLUME` isn't set, default becomes `cogmo-skills-deps-cache-${COGMO_INSTANCE_ID}`. Mirrors Helm's `release-name` convention. Rejected alternative: derive the suffix from `COGMO_MASTER_KEY` via HKDF -- works mechanically but couples a public identifier to a secret-bearing one, has no precedent in the ecosystem, and silently rotates the volume name on master-key rotation. Reviewer flagged on PR #324 as personal-scale-acceptable; promote when a second Cogmo instance ships on the same Local-Docker host.
- [ ] `p3` Make populated venv directories read-only after publish (`chmod -R a-w /skill-venvs/<hash>` after the atomic `mv -T`). At today's single-UID skills-runtime model this is theatrical -- any compromised skill in worker A runs as `cogmo` and could `chmod` back -- but it raises the bar for accidental cross-skill corruption and becomes a real defence once per-skill UIDs land. Cheap (~5 lines in `POPULATE_SCRIPT` after the rename), no behaviour change for read-only consumers.
- [ ] `p3` Per-skill process identity (UID per skill) so a compromised skill in worker A can't write to `/skill-venvs/` to corrupt other skills' venvs. Today the whole skills runtime runs as `cogmo:cogmo` and the shared `/skill-venvs/` volume is writable for every venv. Sandbox/userns boundary still protects host kernel/syscalls; this is the cross-skill mutual-trust upgrade. Marked `[research]` in `design/skills.md` -> Security; ship when multi-author/community skills become routine.
- [ ] `p3` Private pull-through PyPI mirror (Devpi / Bandersnatch) for multi-tenant deployments -- quarantine windows on new package versions catch flash-malicious publishes that the lockfile-hash + sandbox-compile path can't (the compile itself fetches from upstream PyPI). Pair with PEP 740 / Sigstore attestation verification via `pypi-attestations` once it stabilises. Marked `[research]` in `design/skills.md` -> Security.
- [ ] `p3` Revisit Daytona cross-sandbox dep dedup if pool sizing changes or empirical populate cost becomes painful. Today Daytona advertises `depsCacheSharing: 'per-sandbox'` and each pool worker pays a cold uv populate per `(lockfile-hash, py-abi)` it sees over its lifetime (seconds for typical skill deps, amortised across thousands of invokes — fine at `max=3, recycleAfterTasks=500`). The two non-default paths worth considering when this stops being fine: (a) snapshot-per-lockfile bake — register triggers a baker sandbox, populate writes the venv to local disk, then `sandbox.snapshot()` saves it; subsequent workers boot from the per-hash snapshot. Requires reshaping the pool to be per-hash (each worker dedicated to one skill's venv) or a base snapshot containing every reachable hash (re-bake on every skill change). (b) HTTP-fronted wheel cache — devpi/serverless-pypi/etc., uv talks to it via `[[tool.uv.index]]`; cache server holds the POSIX state, sandboxes stay POSIX-naive. Modal-style image-layer caching is the architectural sibling. Trigger: empirical wall-clock pain, pool resizing, or multi-author skill volume.
- [ ] `p3` Shell-script race-recovery regression test for `POPULATE_SCRIPT` — spawn two `sh -c "$POPULATE_SCRIPT"` instances concurrently against a shared empty tmpdir, assert both exit 0 with exactly one body of work and no `.tmp-*` orphan. Locks down the `.ready` marker + `mv -T` atomicity contract against refactors. Test costs ~20s wall-clock (cold uv resolve), but a single contract test that proves the loser of the race doesn't blow up is the bridge between unit-tested branches and the e2e test above. Land alongside any future change that touches the populate script body.
- [ ] `p3` Preserve original error `cause` when wrapping in `LockfileCompileError` / `VenvPopulateError` — both currently re-throw a fresh `Error` with a synthesized message, swallowing the underlying `ExecError` / stderr blob. Use `new LockfileCompileError(msg, { cause: originalErr })` and surface `cause` in the log emit. Improves operator debugging when uv panics on a malformed requirement vs when the sandbox itself fails to spawn. Gemini-bot flagged on PR #322, deferred as not blocking.
- [ ] `p3` Explicit `handle.dispose()` in success path of `makeSandboxLockfileCompiler` for resource-cleanup symmetry. Today the sandbox session is deleted in `finally` but the exec handle isn't disposed; the GC eventually reaps it but the pattern leaves a tiny window where the streaming buffers are pinned. Best-practice only; no observed leak.
- [ ] `p3` Tighten `SkillDependencySchema` version regex — current `[a-zA-Z0-9.+!-]+` accepts garbage like `pkg==-`, `pkg==..`, or `pkg==1!a` that uv rejects at compile time. Stricter PEP 440 modelling (epoch optional, release segment required, pre/post/dev segments well-formed) would catch errors at manifest parse rather than first-register. Cost: a small parser or a pinned regex from `packaging`'s reference grammar. Reviewer flagged on PR #320, accepted as deferred — uv at register time is the second line and the error surface there is fine.
- [ ] `p3` `cacheEntryFor(manifest, body, inputsValidator, lockfile)` helper to dry up the three `...(lockfile && { lockfile: buildLockfileCacheValue(lockfile) })` warm-cache spreads in `runner.ts` (register / approve / rollback). Three call sites is the line where extraction starts to pay; defer until a fourth appears (next dep-cache touch point will likely be the auto-reload-on-rollback path).
- [ ] `p3` Reconsider `string | null` (DB) vs `string | undefined` (cache) representation for "absent lockfile_hash". The DB column is `text NULL` (matches the nullable `lockfile_hash` invariant), but the in-memory cache uses `lockfile?: { hash; contents; specs }` for "no lockfile". Pick one boundary representation; today's mismatch forces a small adapter in `buildLockfileCacheValue`. Minor smell — only fix if a third representation creeps in.
- [ ] `p3` Granularity of `worker-reusable=false` on `populate_failed` — today a populate failure (lockfile-hash mismatch, uv resolve error, disk full) blanket-poisons the worker via `worker-reusable=false`. Defensible at single-user scale per the PR #323 reviewer note ("err on the side of replacing the worker"). Could relax to "reusable iff `mv` never ran AND the failure is deterministic (hash mismatch, malformed lockfile)" — leaves the worker intact when a transient PyPI 503 caused the failure. Wait until pool churn shows up as real cost.
- [ ] `p3` Document the `COGMO_SKILLS_DEPS_VOLUME` multi-tenant footgun in `DEPLOYMENT.md` — today the warning lives only in the env-var docstring + `design/skills.md` → Security. Operators planning multi-instance deployments need to see it on the runtime-requirements path, not buried in design.

### Setup, secrets & config

- [ ] `p3` Tighten wizard atomicity — `stepConfigureTelegram` / `stepConfigureOptionalTools` (Tavily) / `stepConfigureGitHubIdentity` / `stepConfigureClaudeCodeAuth` / `stepConfigureDaytona` / `stepConfigureProvider` each split `putSecret` + `markValidated` (and sometimes a follow-up `createChannel` / `addProvider`) across multiple `deps.runInTx` calls. A crash between calls leaves orphan rows that are harmless individually (re-running setup converges) but diverge from CLAUDE.md's compose-into-one-tx store pattern, and from `stepConfigureVoice` which was tightened in PR #245. Lift each step's sequence into a single `runInTx(async (tx) => { ... })`. Tests survive unchanged — the existing `runInTx` mocks pass the same tx token through to all inner calls. Pure refactor, no behavior change. Defer the LLM-provider path (it composes `validateProvider` + `putSecret` + `addProvider` + `discoverModels` round-trip — moving the network call inside the tx holds connections open longer than worth it).
- [ ] `p3` Lifecycle-contract tests for `stepConfigureVoice` — `src/setup/wizard.test.ts` covers the two highest-value branches (probe-success and probe-fail+save-anyway). Add four more to lock the lifecycle in: (a) existing config + `action=keep` → no `putSecret`, no `upsertVoiceConfig`, no `deleteVoiceConfig`; (b) existing config + `action=remove` → `deleteVoiceConfig` fires, `putSecret` does not; (c) existing config + `action=replace` → falls through to the normal paste/probe/upsert flow; (d) no existing config + initial-confirm declined → zero store writes. The first prompt mock chains are short (`p.select.mockResolvedValueOnce("keep")` etc.); each test is ~20 lines. Land alongside any future change to the keep/replace/remove menu so it doesn't drift silently.
- [ ] `p3` Operator-supplied SSH signing key import (`COGMO_GITHUB_SSH_PRIVATE_KEY`) — non-interactive setup currently rejects the var with "importing pre-generated OpenSSH keys isn't supported yet" and falls back to generating a fresh keypair. To support, parse the OpenSSH private blob (Ed25519-only is fine for slice 4 — the wizard generates Ed25519, and ssh-keygen format conversions across types are unnecessary scope), derive the public key, validate the keypair self-consistency (public matches what we'd derive), and persist into the bundle instead of regenerating. Useful for operators who want a stable signing key that survives setup re-runs (CI, GitOps, repeatable deploys); without import, every `cogmo setup` rotates the signing key and the operator has to install the new public key on github.com. Non-blocking for personal use — the wizard's print-the-public-key flow works fine when re-runs are rare. `micro-key-producer/ssh.js` doesn't expose a parse helper today but the OpenSSH wire format is documented; alternative is `sshpk` (~30 KB).
- [ ] `p3` Provider resolver hot-reload after `cogmo setup` — the per-turn `LlmProviderResolver` (`src/llm/resolver.ts`) memoizes by model for the process lifetime, so `model_providers` / `llm_providers` / `secrets` writes through the wizard or `/model` admin path don't take effect until restart. Two paths once it bites: (a) re-run wizard `cogmo setup` against a live process — print "Restart cogmo (`systemctl restart cogmo` / `docker restart cogmo`) for changes to take effect" at the end of the wizard's success screen, including non-interactive mode (`src/setup/wizard.ts` + `src/setup/non-interactive.ts`); cheap, ~5 lines, ships now. (b) Real hot-reload — wrap the resolver in a versioned cache that invalidates when a `setup-completed` Inngest event fires (or simpler, watch a single `config_version` row), reconstructing adapters lazily on the next `resolveProvider(model)` after a bump. Per `design/providers.md`'s "If hot-swap is needed later, a lazy-loading wrapper can reconstruct on DB change." Defer (b) until the messaging in (a) is observed to be insufficient — single-user deployments restart often enough that a setup-time reminder is probably plenty. (Same hot-reload shape as the voice config entry under *Skills, voice & transport*.)
- [ ] `p3` Dedicated `openai` image_provider_type (using `@ai-sdk/openai`) for gpt-image-* — `@ai-sdk/openai-compatible` (our current `openai_compatible` type) sends `response_format` on every image request, which OpenAI's gpt-image-1 / gpt-image-1.5 reject with HTTP 400 ("Unknown parameter"). The strip-for-gpt-image-* logic lives only in `@ai-sdk/openai` (prefix match via `hasDefaultResponseFormat`; PR vercel/ai#11573). For pure OpenAI image gen the right adapter is `@ai-sdk/openai`'s `openai.image(...)`. Land as a third `image_provider_type` enum value (`openai`) with its own branch in `buildImageProvider`; reuses the same row shape (`base_url` null, secret FK). Until this exists, operators who configure `openai_compatible` against `api.openai.com` are restricted to dall-e-3 — note in the wizard CLI help / design doc. The recorded integration test (`src/test/openai-image-gen.integration.test.ts`) pins dall-e-3 wire shape deliberately; once the `openai` type ships, add a parallel gpt-image-1 fixture.
- [ ] `p2` Recorded fal kontext wire-shape integration test — the image-to-image path landed in PR #223 forwards `prompt: { text, images: [Buffer] }` to fal/flux-kontext. Unit tests cover the mocked `generateImage` call args; no replay test pins the actual fal HTTP request body or response shape. The kontext input shape is a fal-provider extension (`prompt.images`) the AI SDK's public type doesn't declare — a silent regression on the next `@ai-sdk/fal` bump or fal API rev would only surface when a user tries to edit an image. Cost: ~50 lines of test plus a recorded fixture (~5 KB if we stub the output PNG to 1×1 like the openai-image-gen fixture). Use the existing fal-mock fetch override (`src/test/fal-mock.ts`) + `RECORD=1 FAL_API_KEY=... pnpm test:integration` to capture. Pair with a fresh kontext-specific stub PNG so re-records stay small.
- [ ] `p2` Add `--prompt-key` mode (or stdin-piped key) to `cogmo image-provider add` and `cogmo provider add` so the API key isn't passed positionally — today the key shows up in `ps`, shell history, and any process listing on the host. The wizard already uses `p.password()` for the same input, so the masked-input path exists; the CLIs just don't have it. Shape: when `--prompt-key` is passed (or stdin is not a TTY), read the key from stdin with no echo (`@clack/prompts` `password` or node-native `read-line` with `process.stdin.setRawMode`) instead of accepting a positional `<api-key>`. Touches `src/cli/image-provider.ts` (`addProviderCmd`) and `src/cli/provider.ts` (`addProviderCmd`, the LLM provider variant — same shape). Affects all provider types, not venice-specifically; deferred from PR #265.
- [ ] `p3` Extract a shared `addImageProvider` domain function under `src/agent/image-provider/` so the CLI (`src/cli/image-provider.ts`) and the wizard (`src/setup/wizard.ts`) converge instead of each building the same `{ name: <type>_api_key, plaintext, description }` secret + `createImageProvider` payload + JSONB-empty-vs-populated branch on `imageGenerationDefaults`. Mirrors the existing `src/agent/llm-provider/add-provider.ts` (or wherever `addProvider` for the LLM case lives — `stepConfigureProvider` already calls it) — same shape, same naming. Drift risk today is low (~30 LoC duplicated; both call sites are tested) but adding the next provider-creation surface (`cogmo setup --non-interactive` already speaks both paths, but new flows like a `cogmo migrate-secrets` will too) is the trigger. Deferred from PR #265.
- [ ] `p3` Per-model capability metadata from the Models API, replacing guesswork about what a model's request surface accepts. Today `AnthropicProvider` handles the divergence by sending the intersection — no `thinking`, no sampling params — which is correct for everything 4.6 and later but silently wrong at the edges: a pre-4.6 model an operator registers for a cheap `summarization_model` (`claude-haiku-4-5` is the realistic one) needs `budget_tokens` to think at all and rejects `output_config.effort`, and we'd have no way to know. The wrong fix is a hardcoded model→capability table — that is exactly the artefact that went stale and produced the `budget_tokens` 400 in the first place. The right source is upstream: `GET /v1/models/{id}` now returns `capabilities`, `max_input_tokens`, and `max_tokens`, and `discoverModels` (`src/agent/provider/discover-models.ts`) already calls `/v1/models` and discards everything except `id` on the Anthropic branch. Shape: capture capabilities at add-time and persist alongside the existing `context_window` / `max_output_tokens` overrides on `model_providers` — per CLAUDE.md's JSONB rule that's one `capabilities JSONB` with a Zod schema, not more nullable columns. Data-model change, so discuss before building. Trigger: someone registers a pre-4.6 or non-Anthropic model whose request surface we get wrong, or `effort` (below) needs to know whether the target model supports it.
- [ ] `p3` Add `effort` to `ChatParams` when something needs explicit reasoning-depth control. `output_config.effort` (`low`/`medium`/`high`/`xhigh`/`max`) is the parameter that replaced per-model thinking budgets; the adapter currently sends no `thinking` parameter at all and lets each model apply its own default, which is adaptive thinking on the 5 series. That's the right default and nothing today wants to override it, so the knob isn't plumbed — adding it now would be an abstraction with no consumer. Land it when a concrete caller appears: plausible candidates are a cheap `low` for the degraded-reply synthesis and sub-agent calls, or `high`/`xhigh` on the pipeline compile step. Pairs with the capability metadata above, since `effort` errors on Sonnet 4.5 / Haiku 4.5 and lacks `xhigh` before Opus 4.7.
- [ ] `p2` Close the MCP-image gap so third-party MCP servers that return image content flow through the same `AttachmentStore` + inline-photo path as in-tree image gen. Today only the in-tree `generate_image` tool's `{path, mediaType, model}` payload is recognised by the orchestrator's mid-stream extractor (`parseGeneratedImagePayload` in `src/agent/image-tools.ts`, `extractGeneratedImages` in `handle-message`) — an MCP tool returning MCP `image` content (`{ type: "image", data: <base64>, mimeType }` per the [MCP spec](https://modelcontextprotocol.io/specification)) is dropped on the floor for delivery purposes, even though the LLM sees the bytes. Without this, the policy "third-party providers go via MCP" (see `design/integrations.md` once the policy lands) has a UX cliff: third-party MCP tools don't get the Telegram `sendPhoto` mid-stream experience first-party providers do. Shape of the fix: extend the MCP client adapter (`src/mcp/`) to detect `image` content in tool results, upload the decoded bytes via `AttachmentStore`, and rewrite the tool_result block to carry the same `{path, mediaType, model}` payload extractor consumes. `model` is the MCP tool's display name. ~80 LoC + a unit test asserting "MCP tool returning image content lands in attachments and surfaces inline." Triggers: someone wants to plug in a community MCP image-gen server, OR we publish the extension-policy doc and want the third-party path to feel native.

### Scheduling

- [ ] `p2` Queue undelivered scheduled fires — when the dispatcher returns `no_reachable_channel` (no engaged conversation and no reachable channel for `(userId, profileId)`), persist to a `pending_fires` table; drain on next session open as synthetic inbounds. Needs a design pass on TTL ("call mom at 3pm" delivered at midnight is worse than dropped), per-task coalesce rules ("if the user comes back online to 12 missed reminders, collapse"), and drain idempotency. Unblocks Web-UI-only users and any future channel without proactive delivery. See changelog `2026-05-17-engagement-reachability-split.md`.
- [ ] `p3` Web UI `conversation/created` push for scheduled fires — when the fire-handler rotates onto a fresh conversation (idle path), a live Web UI tab on the prior conversation doesn't auto-navigate to the new one. A client-side notification ("a scheduled task created a new conversation") or auto-navigate would close the UX gap. Telegram already works fine because the bot can DM any time; this only matters once Web UI is the primary surface.

### Observability & operations

- [ ] `p1` Key the Inngest deployment, or put it behind something that authenticates. `INNGEST_EVENT_KEY` / `INNGEST_SIGNING_KEY` are documented as "Required if your Inngest deployment is keyed" — optional, and a self-hosted Inngest is commonly unkeyed, so anything that can reach `INNGEST_BASE_URL` can post events. That endpoint is a control plane rather than a store, which is what makes it worse than the Hindsight gap filed under *Memory & evolution*: reading memories is bad, driving the agent is worse. Two events carry it. `adapter/direct/inbound` takes `{platformAddress, text, platformTs}` — arbitrary text straight into a turn, no channel session and no identity check, which is message injection rather than replay. `coding/task/plan-approved` takes `{taskId, approvedAt}` and is the human checkpoint in `design/coding-delegation.md`; posting it approves a plan nobody read, and the CLI then executes, pushes and opens a PR. `agent/scheduled-task.fire` and `skills/cron.fire` similarly trigger work on demand, and `inbound/arrived` replays an existing message. Protection today is positional — the port is internal — which is the assumption `ctx.http` broke for Hindsight and would break here identically; the address guard in `ctx-handler.ts` covers both, but it is one check on one code path and it races DNS rebinding. **Verify what a self-hosted Inngest actually enforces before writing code**: setting both env vars is necessary but the server has to reject unsigned events for it to mean anything, and a key the server ignores is worse than none for being trusted. If the self-hosted build cannot enforce it, the answer is network policy or a fronting proxy rather than env vars.
- [ ] `p3` Observer `durationMs` telemetry is wrong under replay — `startedAt` is captured in the bare body of `runObserver` (`observer.ts`), so it resets on every re-invocation and the `persist-evolution-event` step records only the final invocation's bare-body time (~0ms), not the run's duration. The comment claims the opposite. Fix: capture `startedAt` inside the first step and thread it through, or derive duration from the first step's stored timestamp.
- [ ] `p3` Debounce `debounceWaitMs` histogram double-records — the `.record()` calls in `debounce.ts` (native router, idle, maxwait) sit in the bare body upstream of a `step.sendEvent` / after a `step.sleep`, so each fire records twice (and the second sample reads a later `Date.now()`). Move the record after the final step, or accept and halve in the dashboard — either way document it.
- [ ] `p3` `agentIterations.record()` in `buildResult`/`buildDegradedResult` (`loop.ts`) is bare-body code — it re-records the same turn's iteration count once per post-loop boundary invocation (~3-6 samples per clean turn), inflating the `cogmo.agent.iterations` histogram. Same class as the debounce entry above and now contradicts `.claude/rules/inngest.md`'s record-inside-steps rule. Fix shape: record once from inside the `persist-new-messages` step body (result is in scope, exactly-once), or drop the metric into the turn-complete log line and derive the histogram downstream.
- [ ] `p2` Stuck-conversation watchdog cron — daily Inngest scheduled function scans `messages` + Inngest run state for conversations whose last `handle-message` invocation has been failing for >N minutes OR whose last assistant row trails the last user row by >N hours with no in-flight run. Runs the history invariant validator (`src/agent/history-invariants.ts`) and re-emits `inbound/ready`, or marks the conversation `errored` (PR2 status guard) when irrecoverable. Catches both retry-storm and silent-stall cases. Build only after telemetry from PR2's OTel taxonomy shows a real tail of stuck conversations — premature at single-user scale.
- [ ] `p3` LiteLLM snapshot refresh automation — `data/litellm-models.json` is regenerated by hand via `pnpm tsx scripts/refresh-litellm-models.ts`. Two natural follow-ups: (a) weekly GitHub Actions cron that runs the script and opens a PR with the diff so we don't drift more than a week behind upstream; (b) runtime refresh that swaps the bundled snapshot for a lazy fetch + on-disk cache with a TTL, so picking up a new model id doesn't require a cogmo release. Start with (a); only build (b) once we see operators waiting on a release to add a model. Refresh script lives in `scripts/refresh-litellm-models.ts`; the loader (`src/llm/litellm-data.ts`) already lazy-loads, so (b) is a one-file swap.
- [ ] `p3` Add pino `redact` paths for the LLM failure-logging wrapper. `withFailureLogging` (`src/llm/logging-fetch.ts`) currently emits the entire request/response body on failure — fine at single-user scale where the only PII is the user's own messages, but logs become unsharable when collaborating or troubleshooting with someone else. Pino's `redact` supports field-level paths with wildcards (`body.messages[*].content`, `body.system`, `responseBody.choices[*].message.content`). Declare the paths globally on the logger in `src/logger.ts` or pass per-call via a child logger; both flow through `fast-redact` so cost is negligible. Trigger this when log-sharing becomes a need, not pre-emptively. Reference: PR for the failure-logging wrapper.

### Tools & web

- [ ] `p3` fetch_url SSRF hardening — dns.resolve() + IP check before fetch (current check is string-level only)
- [ ] `p3` Verify Perplexity Sonar citations structure via real OpenRouter API response — may need to adjust parsing

### Code style & refactors

- [ ] `p2` Audit and migrate remaining raw `sql\`\`` usages in production store code to typed operators. The rule (`.claude/rules/code-style.md`) reserves `sql\`\`` for fragments Drizzle's typed API can't express (`now()`, JSONB `||`, partial-index predicates, DDL); audit anywhere a query body interpolates a JS value next to a comparator and convert to `lt` / `gt` / `lte` / `gte` / `eq` / `inArray` so postgres-js receives a mapper-converted parameter. Search shape: `grep -rn 'sql\`[^\`]*\${' src/` and triage each hit — JSONB merge / partial-index / function-call uses stay, comparator+JS-value uses migrate. Cheap mechanical sweep; the value is closing the class so the next regression doesn't ship through the PGLite-unit-tier hole that lets non-string parameters look fine until postgres-js binds them in production.
- [ ] `p3` Coalesce per-row UPDATEs in both cron tickers into a single bulk statement. `scheduled-task-ticker` (`src/agent/scheduling/ticker.ts:131-136`) and `skill-cron-ticker` (`src/skills/cron-ticker.ts:117-123`) both loop `await store.advance*(tx, row.id, ...)` per locked row. Bounded at `TICK_BATCH_SIZE=100`; realistic N at single-user scale is 1-10 scheduled rows, wall-clock cost ~3ms total against localhost PG, so deferred. Becomes meaningful when scheduled-task / scheduled-skill counts cross ~30, or when PG goes non-local (each round-trip 5-20ms; 100 rows × 10ms = 1s pure wall-clock per tick). Fix shape: build a `(rowId, lastFiredAt|lastRunAt, nextRunAt)[]` array inside the lock-and-advance loop, then issue one `UPDATE ... SET last_fired_at = data.last_fired_at, next_run_at = data.next_run_at FROM (VALUES (...)) AS data(id, last_fired_at, next_run_at) WHERE t.id = data.id::uuid`. Change both tickers together to avoid asymmetric perf characteristics. Flagged on PR #301 (Gemini bot) and accepted as deferred — sibling to the `failTaskIfNonTerminal` SQL-coalescing item under *Coding delegation & sandbox*.
- [ ] `p3` Consider bundling per-call deps into a `RequestContext` object instead of threading `runInTx: Transactor` + N store/services as separate fields on every `*Deps` interface. Today each use-case-shaped function takes `deps: { runInTx, agentStore, transportStore, ... }` and bootstrap wires the same fields into ~30 deps interfaces. A single `RequestContext` (or extending the existing per-conversation `Service` at `src/agent/service.ts`) would drop the boilerplate to one parameter per call. Industry research (PR #169 thread): trigger.dev / documenso / dub.co keep deps explicit, matching what we do; NestJS shops bundle via `@Injectable()` constructors. The case for: less ceremony when adding a new use case, single-source-of-truth for what's request-scoped. The case against: changes the boundary between "what's globally available" and "what's a function arg", risks `RequestContext` becoming a god-object. Defer until the friction actually bites — at ~30 interfaces today, threading is fine.
- [ ] `p3` Consolidate `load-turn-snapshot` + `assemble-prompt`'s profile read in `handle-message.ts`. `load-turn-snapshot` (durable step) loads the profile to extract `model` + `summarizationModel`; `assemble-prompt` runs `loadConversationContext` which loads the full profile again; voice-mode resolution (line ~377) reads the profile a third time. Folding them into a single durable `load-conversation-context` step would drop two redundant reads per turn and let the voice-mode resolver reuse the loaded profile. Deferred because the Inngest step boundary is part of the crash-recovery contract (`design/crash-recovery.md`) — combining changes the durable-replay shape and the recovery test contract. Re-do the durability map and the replay test in lockstep with the consolidation. (PR #169 already deleted the dead `resolve-channel-types` step that was the third leg.)
- [ ] `p3` Tighten `CodingOrchestratorDeps.secretsStore` to required (drop the `?`). Production wiring (`src/index.ts:270`) always passes it; verify-orchestrator already declares it required. The optional only exists for tests that don't exercise teardown — a thin reason. Tightening would: eliminate the conditional `...(deps.secretsStore !== undefined && { secretsStore: deps.secretsStore })` spreads at the 4 `safeTeardownWorktree` call sites in `orchestrator.ts`, normalise the call-shape asymmetry between `orchestrator.ts` and `verify-orchestrator.ts` (PR #169 review), and let TS catch any test that forgets to wire teardown. Update the affected tests' `mockDeps` to provide a fake secretsStore (a `mock<SecretsStore>()` from vitest-mock-extended is a one-liner).
- [ ] `p3` Refactor `CodingProgressSubscriber` from a static-only class to a plain function — `src/agent/coding/progress-subscriber.ts:55` is `class CodingProgressSubscriber { static start(args) { ... } }`. No instance state, no inheritance, no `this` — biome's `lint/complexity/noStaticOnlyClass` flags it because it's a class used as a namespace. Replace with `export function startCodingProgressSubscriber(args)` and update the ~3 call sites (telegram adapter mostly). Pre-existing — landed in the original Telegram coding-progress slice and never cleaned up. Mechanical 5-min change.
- [ ] `p3` Replace the bare `as any` on the OpenRouter `cache_control` extension in `buildMessages` at `src/llm/openai-compat.ts:307` — `content: [{ ..., cache_control: { type: "ephemeral" } } as any]`. `cache_control` is an OpenRouter pass-through extension that OpenAI's official TS types don't model; the original author cast with `as any` plus an inline English comment, but biome's `lint/suspicious/noExplicitAny` still flags it (only `// biome-ignore lint/suspicious/noExplicitAny: ...` suppression actually authorises the cast). Three options, preferred first: (a) declare a local `CachedTextPart extends OpenAI.Chat.ChatCompletionContentPartText` interface (with `cache_control: { type: "ephemeral" }` and an optional `ttl?: "5m" | "1h"` for forward compat with OpenRouter's 1h cache variant) and use a `satisfies` cast — no `as` at all, ecosystem-standard pattern (Qwen, wave-agent, et al.) using local interface extension and width subtyping; (b) `as unknown as OpenAI.ChatCompletionMessageParam["content"][0] & { cache_control: { type: "ephemeral" } }` — real type narrowing instead of `any`; (c) keep `as any` but add a proper `// biome-ignore lint/suspicious/noExplicitAny:` line with the library-gap rationale (CLAUDE.md's standard form). Pre-existing — landed when prompt caching first shipped on the OpenAI-compat adapter.
- [ ] `p3` Replace top-level `process.exit(await dispatch(command))` in `src/main.ts` with `process.exitCode = await dispatch(command)` so buffered stdout/stderr drains naturally before the process exits. Blocked on per-dispatch graceful shutdown: `serve` already cleans up via the `finally` after `connection.closed`, but `skills`/`setup`/`seed` call paths that open DB pools, LLM SDK clients, and (for `skills`) the sandbox supervisor — those keep the event loop alive and would hang the process if `process.exit` is removed without explicit teardown. Fix needs a `shutdown()` hook on each entrypoint that closes its handles, then exitCode-driven exit becomes safe.
- [ ] `p3` Rename `DEPLOYMENT.md` → `deploy.md` and `PROGRESS.md` → `progress.md` for consistency with the project's lowercase-hyphenated naming convention. Update inbound links across the repo (`README.md`, `CLAUDE.md`, design docs, CI workflows, `todo.md`). Keep `README.md`/`CONTRIBUTING.md`/`CLAUDE.md` as-is — universal or tool-enforced conventions.
- [ ] `p3` Evaluate DI library (ditox/awilix) when adapter count exceeds 5
- [ ] `p3` Consider ISO 8601 PT durations for time-valued config — instead of `minTimeoutMs: 1000`, `SESSION_IDLE_TIMEOUT_MINUTES=60`, `DEBOUNCE_IDLE_SECONDS=3` etc, use `"PT1S"`, `"PT1H"`, `"PT3S"` parsed by `date-fns` or a tiny helper. Eliminates the unit-suffix bikeshed and prevents seconds-vs-milliseconds bugs at API boundaries. Would touch `RetryOptions`, `env.ts`, and any caller passing durations. Defer until we add a third unit (anything other than ms/s/min) or until the unit-suffix maintenance becomes annoying.
- [ ] `p3` Replace `(b as { text: string }).text` casts with type-guard predicate filters. Current pattern is `.filter((b) => b.type === "text").map((b) => (b as { text: string }).text)` — TypeScript doesn't narrow through `.filter()` because the predicate signature is `(value: T) => boolean`, not `(value: T) => value is U`. Switching to `.filter((b): b is TextBlock => b.type === "text")` lets `.map((b) => b.text)` work without the cast. Grep for `as { text: string }` to find call sites (handle-message summarize callback, llm provider adapters likely). Cosmetic, pre-existing pattern.
- [ ] `p3` Narrow `ImageProviderRow` into a discriminated union so consumers don't need defensive `if (row.baseUrl === null)` checks. Today `image_providers.base_url` is `text` nullable and a CHECK enforces "null iff type=fal" — but TypeScript infers `baseUrl: string | null` for every row, so `buildImageProvider` (`src/llm/image-providers.ts:74-77` and `:94-97`) carries dead defensive throws "the CHECK guarantees this is unreachable, but typing surfaces nullable on every read." That comment is the smell. Refactor: `type ImageProviderRow = (BaseRow & { type: "fal"; baseUrl: null }) | (BaseRow & { type: "openai_compatible" | "venice"; baseUrl: string })`, with the store's `getImageProvider` / `listImageProviders` / `findImageProviderByName` narrowing at the boundary (switch on `type`, throw `InvariantError` if the DB row violates — same posture as JSONB Zod parsing per CLAUDE.md's "validation at the store boundary"). Touches the type definition + ~3 store read functions + ~6 consumer sites (`buildImageProvider`, CLI list, image-tools, schema test, store test). ~80–120 LoC, pure type refinement, no runtime behaviour change. **Trigger:** the planned `openai` provider type (line 61 in this section) is the natural moment — adding a 4th type with its own baseUrl-required-or-not story makes the discriminated-union load-bearing rather than aesthetic, and doing it then forces the right design decision for `openai` instead of carrying a third defensive branch.
- [ ] `p3` Apply the JSONB-blob pattern to multi-field clusters on `coding_tasks` as later slices add them, instead of growing more nullable columns. Today's nullable lifecycle columns (`session_id`, `container_id`, `plan`, `plan_approved_at`, `pr_url`, `failure_reason`) are all single fields where blob = text-with-overhead — don't convert. Slice 2+ likely candidates: a `pr` blob (`pr_url` + `pr_number` + `branch_sha` + `opened_at`) when slice 4 ships verify+push+PR; a `failure` blob (`failure_reason` + `failure_step` + `failed_at`) for richer error reporting; possibly a `cli_session` blob (`session_id` + `cli_version` + `model`) if per-turn CLI metadata becomes useful. The `worktree_assignment` JSONB pattern (atomicity by Zod schema, single null check, all-or-none invariant enforced at the store boundary) is the model to copy.
- [ ] `p3` Group all-or-none coupled deps structurally in `createTransport` / `RegistryDeps` — both `skillRunner`+`skillStore` and `codingStore`+`codingStreamingRegistry` are independently optional in the type but must be passed together (Transport / per-channel functions check `if (!a || !b)` at runtime). One PR refactor: collapse each pair into `skills?: { runner, store }` / `coding?: { store, streamingRegistry }` so the invariant is structural and TypeScript rejects half-set states at the call site. Anti-pattern will keep biting whenever a third coupled-pair gets added. Touches `transport.ts`, `transport/registry.ts`, `transport/adapter-module.ts`, `index.ts` bootstrap, and adapter `setup()` functions.
- [ ] `p3` Typed extractor for Inngest function internals in tests — every `*.test.ts` that drives a `createX(...)` Inngest function reaches in via `(fn as any).fn(...)` (main handler) or `(fn as any).opts.onFailure(...)` (failure handler). Replace with a single helper, e.g. `function inngestInternals<T>(fn: T) { return fn as unknown as { fn: Handler; opts: { onFailure?: Handler } } }`, and route every test through it. Isolates the type hatch in one place, removes ~50 scattered `as any` casts. Touches handle-message.test.ts, observer.test.ts, debounce.test.ts, coding/orchestrator.test.ts, etc. Solo refactor — should land as its own PR, not bundled with feature work.
- [ ] `p3` Convert `createService` to an options object. Today's positional signature is 11 args with 3 trailing optionals (`coding`, `skills`, `scheduling`) — every new sub-namespace appends another positional. Per the PR #246 review, swap to `createService({ memory, bankId, memoryScope, speakerClass, restrictedClassNames, files, coreMemory, stageRetain, coding?, skills?, scheduling? })`. Touches the factory + ~3 call sites (`handle-message.ts` two, plus test fixtures) and removes the asymmetric ordering between required and optional sub-namespaces. Solo mechanical refactor; do before the next sub-namespace lands so the call-site change isn't bundled with feature work.

### CLI & developer experience

- [ ] `p2` Replace the hand-rolled CLI flag parsing with `parseArgs` from `node:util`. `src/cli/model.ts`, `image-model.ts` and `subagent.ts` each carry their own `parseFlags` + `takeValue` pair — a positional scan, a guard against `--provider --context` eating the next flag as a value, and per-flag integer coercion. `parseArgs` has been stable since Node 20 and the workspace pins `^24.15.0 || >=26.0.0`, so this is a built-in we are reimplementing, against CLAUDE.md's "check if a maintained library or built-in feature covers the use case". It also handles `--flag=value`, which the copies disagree about: `subagent.ts` reimplements it (splitting on `indexOf("=")`, covered at `subagent.test.ts:124,138`) while `model.ts` and `image-model.ts` reject it, so the same syntax works or fails depending on the subcommand. The duplication has already cost something: `image-model.ts` throws on an unknown flag (its comment explains that a dropped `--ratio` silently registers a fixed-size model) while `model.ts` swallowed them until this was noticed, so a typo'd `--max-outputs` registered no override and still printed a success line. `strict: true` makes that the default rather than a lesson each copy has to learn. Numeric bounds stay ours — `parseArgs` yields strings — so keep `parseIntAtLeast` and its per-flag minimums. Do the three files in one pass so the behaviour converges; each has its own tests to keep green.

- [ ] `p3` `pnpm console` exits before the assistant reply lands when stdin is piped — readline's `close` event fires the moment EOF is seen, racing the in-flight `waitForResponse` poll loop and printing `Bye.` mid-wait. Fine for interactive use but breaks one-shot CLI usage like `echo "msg" | pnpm console`. Fix: defer the `close` handler until any pending `waitForResponse` settles (or delivery times out). Workaround today: `(sleep 1 && echo msg && sleep 35) | pnpm console`.
- [ ] `p3` Consider dropping Inngest serve mode — only connect mode is used (tests, production, local dev)

### Testing, fixtures & CI

- [ ] `p1` Stop `skill-authoring.integration.test.ts` reaching the live internet. Re-enabling it flipped this suite from never-running to running on every `pnpm test:integration`, and the skill it exercises calls `https://api.coingecko.com/...` for real: tier-1 skills execute in-process through `runOnWorker`, `ctx.http` goes to `DefaultCtxHandler` and out through the host's own `fetch`, and nothing in the harness intercepts it — `test/integration-setup.ts` and `test/llmock-setup.ts` patch no globals, and `src/test/fal-mock.ts` deliberately avoids `globalThis.fetch`. So the tier's one hermetic guarantee does not hold for this file: it fails offline, and CoinGecko rate-limits datacenter IPs, so a 429 makes the skill raise and `expect(run.status).toBe("success")` fail for reasons unrelated to any change. The URL is not baked into the Daytona cassette in the way it first appears: on replay the stub Octokit's `POST /pulls` materializes the branch from `test/fixtures/skill-authoring-branch/`, checked in and hand-maintained per the test file's own header, so changing the authored skill's target is a fixture edit rather than a re-record. What blocks the obvious fix is elsewhere. A local stub server listens on loopback, and `ctx.http` refuses loopback, link-local and private destinations by design — the allowlist cannot name it either, since single-label hosts do not parse and `127.0.0.1` is refused on the resolved address regardless. So the honest-and-hermetic shape needs a deliberate seam rather than just a different URL: either inject `fetch` into `DefaultCtxHandler` the way `resolveHost` already is and point it at a stub, or give the address guard a test-only opt-out that production construction cannot reach. Prefer injecting `fetch` — a guard with a bypass is a guard with a bypass, and the injected seam keeps the refusal absolute in every path that ships. Either way the cassette itself stays valid, so no Daytona or Anthropic spend is required.
- [ ] `p3` `loopResult(overrides?: Partial<AgentLoopResult>)` factory in `src/test/factories.ts` (defaulting `streamed: { text: "", toolUseIds: [] }`, minimal usage/model/iterations) — adding the `streamed` field meant hand-editing 18 inline result literals across `handle-message.test.ts` (15) and `handle-message.replay.test.ts` (3), all inside untyped `vi.fn().mockResolvedValue` calls where the next missed site is a runtime TypeError, not a compile error. Sweep the sites onto the factory. Companion cleanups from the same review: dedupe the two Inngest-cache stand-ins in `loop.test.ts`, derive `StreamedOutput` as `AgentLoopResult["streamed"]`, and drop the `as { type: string }` casts in the replay tests that defeat `StreamEvent` narrowing. Mechanical, disjoint from feature work — own PR.

- [ ] `p3` Carry `redacted_thinking` blocks through the canonical `ContentBlock` union instead of dropping them. `fromAnthropicBlock`'s `default` arm returns `[]` for any unnamed block type and the streaming accumulator only tracks `thinking`, so a redacted block is lost in both paths — producing the failure this PR fixed from the other direction: an assistant turn reaching the API missing a block the model emitted. Live now that the 5 series thinks by default, though unverified whether it emits them at all: the migration guide says Fable 5 / Mythos 5 return regular blocks "not encrypted blobs or `redacted_thinking`" and is silent on Sonnet 5, so confirm first. Needs a new variant on the `ContentBlock` union, the `MessageContentSchema` gating the JSONB column, and the OpenAI-compat thinking filter — which is why it isn't bundled with the adapter fixes. The `default` arm logs each dropped type once in the meantime.

- [ ] `p2` Replace MinIO in the dev/test stack — `minio/minio` is **archived** (marked unmaintained 2026-02-12, archived for good 2026-04-25) and the company has moved to the commercial AIStor product. Newest release is `RELEASE.2025-10-15T17-29-55Z`; upstream promises no features, no compatibility updates, and **no guaranteed security patches**. `dev/containers.ts` runs `cgr.dev/chainguard/minio:latest`, so we're on an unpinned floating tag over a dead upstream — the worst of both (silent drift now, silent freeze later). Blast radius is small: it's the S3 stand-in for the `files` bucket in dev + integration only (`ensureFilesBucket`, attachments and workspace files), production points `S3_ENDPOINT` at a real bucket. Candidates were benchmarked 2026-08-15 against the exact flow `ensureFilesBucket` + the file tools use (CreateBucket / Put / Get+verify / ListObjectsV2 with prefix / Delete / re-CreateBucket), driven by `@aws-sdk/client-s3` with `forcePathStyle: true`:

| candidate | image | ready | S3 ops | health path | change needed |
|-|-|-|-|-|-|
| `rustfs/rustfs` | 82 MB | 704 ms | 6/6 | `/minio/health/live` | image ref only |
| `chrislusf/seaweedfs` (`server -s3`) | 101 MB | 3.8 s | 6/6 | `/status` | port 8333, command, health, anonymous auth |
| `adobe/s3mock` | 100 MB | 8.4 s | 6/6 | `/` | port 9090, health, and in-memory only (breaks dev's `withReuse`) |

(SeaweedFS and S3Mock "fail" re-CreateBucket with `BucketAlreadyOwnedByYou`, which is correct S3 behaviour and is already swallowed by `ensureFilesBucket`. RustFS returns success instead. Both are fine.)

RustFS is the strongest technical fit — it answers `/minio/health/live`, reads `MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD`, takes `server /data` and listens on 9000, so `minio()` would change by exactly one line and the `Wait.forHttp` / env / command blocks stay as they are. It is Apache-2.0 and the only candidate that validates credentials rather than running open. **But the choice of replacement is governed by the Blocked "Monitor RustFS" item, not by this entry** — that item holds at Chainguard until RustFS reaches a *stable* 1.0, and `1.0.0-rc.2` does not clear that bar. Don't act on the benchmark alone. If the swap has to happen before RustFS stabilises, SeaweedFS is the pre-1.0-safe pick at the cost of a slower boot and real config changes.

**What is actionable now, and is the whole of this entry:** pin the floating tag by digest. `cgr.dev/chainguard/minio:latest` over an archived upstream drifts silently today and freezes silently later, and that is true regardless of which replacement eventually wins. Everything above is measurement to hand the Blocked item when its trigger fires; tested RustFS digest for that day: `rustfs/rustfs@sha256:41fe89380f4120a337790c02af192c3fe7bb55c3edc2e6e9357b487b47c6ab21`. A replacement, when it comes, touches `minio()` + `ensureFilesBucket()` in `dev/containers.ts` and `scripts/dev-infra.ts` (rename the helper too); `@aws-sdk/client-s3` call sites need no change.
- [ ] `p3` Route sub-second debounce to the native path, and constrain `DEBOUNCE_IDLE_SECONDS`. Inngest 4.18 clamps any duration under 1s up to 1s inside `timeStr`, so the legacy state machine — which `debounce.ts`'s header says exists *specifically* to serve sub-second debounce — no longer delivers it. A sub-second config now pays a full second on the racy legacy path while the race-free native path already floors to `"1s"` via `msToSeconds`, so native is strictly better for exactly the configs currently routed away from it. Two parts: (a) flip the routing predicate so sub-second goes native; (b) `env.ts:145` declares `z.coerce.number().default(3)` with no `.int()` or `.min()`, so `0.5` is accepted and silently becomes 1s — decide whether to reject it at parse time or document the floor. Both are config/behaviour choices rather than bug fixes, which is why the 2026-08 sweep only restored the record-vs-sleep invariant (`durableSleepMs`) without changing routing.
- [ ] `p1` Investigate parallel-fork flake on `pipeline.mcp.integration.test.ts` bootstrap. Observed once locally on 2026-05-18 during a full `pnpm test:integration` run: the file failed at `bootstrap()` → `setup/seed.ts:235:22` → `DrizzleSecretsStore.putSecret` with "Failed query: insert into secrets ... ON CONFLICT (name) DO UPDATE ..." for `fal_api_key`. The error surface has no underlying PG code, only the query and the "ErrorResponse" stack frame in postgres-js. Did NOT reproduce when running the file in isolation, did NOT reproduce when running just the pair (`pipeline.integration.test.ts` + `pipeline.mcp.integration.test.ts`), and did NOT reproduce on the targeted parallel pair with my PR #267 changes stashed — only manifested under the full ~22-file integration suite's parallel-fork load. Hypothesis: multiple integration files all calling `bootstrap()` race on the `fal_api_key` upsert; the ON CONFLICT path is supposed to be idempotent but under heavy concurrency the Postgres client surfaces something pre-conflict-resolution as the failure. Likely candidates for the root cause: (a) postgres-js connection-pool exhaustion (the integration suite spins 20+ forks each holding their own pool); (b) a unique-constraint violation surfacing before ON CONFLICT kicks in under very specific timing; (c) an Inngest connect-mode collision on the dev server's WebSocket registry leaking into the bootstrap probe path. CLAUDE.md → Testing → "Integration tests pass in isolation, ship in parallel" documents this hazard class for `bootstrap`-calling tests. Concrete next steps: (1) add a tighter try/catch around `ensureFalImageDefaults` so the error wraps the underlying PG error code; (2) repro by running the suite N=10× in a loop and recording the failure rate; (3) if the rate is non-trivial, route `secrets` upserts behind a `pg_advisory_xact_lock` keyed on `hashtext(name)` (cheap; the secrets table is small and rarely-written) so concurrent boots serialise on the conflicting row.
- [ ] `p3` Speed up the unit-test tier (currently ~4 min on CI / ~100s wall-clock locally; ~635s cumulative across workers). Two structural costs dominate: **Pyodide WASM bootstrap** in `skills/runner.test.ts` (~101s for 24 tests) and `skills/runner.register.test.ts` (~86s for 28 tests) — every test invocation spins a fresh worker and pays ~5s WASM init; **PGlite `pushSchema`** in every `*/store/store.test.ts` `beforeAll` (8 files × ~2-7s each under contention; bumped `hookTimeout` to 30s in `vitest.config.ts` to absorb worst case). Plus the CI multipliers: `pnpm test --coverage` adds V8 coverage instrumentation overhead (~30-50s); GHA standard runners are 2 vCPU vs typical dev 8-12, so vitest worker parallelism scales down ~6×. Three concrete options, ordered by ROI / risk: (a) share Pyodide workers across tests in a file via `beforeAll` instead of per-test boot — saves ~50s on `skills/runner.*`; risk is hairy Python state across tests (sys.modules cache, asyncio loop, globals) — needs a worker-reset hook or a fresh-`pyodide.runPython("__name__ = '__main__'")` between tests; (b) move `--coverage` to a nightly / main-only job so PRs run plain `pnpm test`; (c) pool a single PGlite instance across store-test files via vitest `globalSetup` with per-test `truncateAll` — saves ~150s cumulative across the eight store files; loses a small amount of test isolation (one test crashing the DB blasts the others) but `truncateAll` already runs in `afterEach` so isolation is mostly already at the row level. Net: ~3min wall-clock should drop to ~1.5min on CI without sacrificing coverage signal.
- [ ] `p3` Shrink fal fixture image — current replay fixture is 125 KB (recorded at fal's default 1024x768). A smaller size parameter or post-record downscaling would keep the repo lean. Not urgent — Git LFS is overkill, 125 KB is acceptable.
- [ ] `p3` Migrate binary test fixtures to Git LFS — only when any of: (a) a single fixture exceeds 1 MB, (b) fixtures churn in most PRs (would happen with visual regression testing on FE screenshots), (c) total binary growth > ~50 MB/year, (d) clone time becomes annoying (>1 min), or (e) GitHub shows the large-files warning. Today all fixtures are <200 KB with low churn (re-recorded only when prompts/models change), so direct commit is correct. One-shot migration when symptoms appear: `git lfs track "test/fixtures/fal/*.jpg"` + `git lfs migrate import --include="test/fixtures/fal/*.jpg"` rewrites history. See `.gitattributes` for the current binary markers.
- [ ] `p3` Stale llmock fixture cleanup — detect unused fixtures after test run, auto-delete
- [ ] `p3` File upstream PR against `@copilotkit/aimock` for a `responseTransform` hook (parallel to the existing `requestTransform`). Today aimock's `RecordConfig` accepts no post-response transform, so when we record an image-gen fixture via `RECORD=1 OPENAI_API_KEY=... pnpm test:integration src/test/openai-image-gen.integration.test.ts` the recorder writes the full ~2 MB base64 PNG straight to disk. Our committed fixture has been hand-stubbed to a 1×1 transparent PNG (~390 bytes) — every re-record blows that away. A `responseTransform: (resp, providerKey, path) => MaybeRewrittenResp` hook in `@copilotkit/aimock/dist/recorder.cjs`'s `proxyAndRecord` would let us register a transform in `test/llmock-setup.ts` that detects `path === "/v1/images/generations"` (or any `response.image.b64Json`) and swaps the bytes for the canonical stub before the fixture is written. Interim workaround: an `afterAll` hook in the test that walks `test/fixtures/recorded/` and rewrites any oversized image fixture to the stub. Upstream PR is the clean fix — the workaround clutters the test file with fs/filesystem code and only handles the one fixture path we know about.
- [ ] `p3` Bootstrap N+1 transaction for image-provider secret decrypt (`src/index.ts:733` build loop + `buildImageProvider` at `src/llm/image-providers.ts:55`). Each iteration opens a fresh `runInTx` to call `secretsStore.getSecretById` for one row — N image_providers = N transactions = N PG round-trips at boot. Negligible today at N=1–3 with localhost PG (~3 ms total); flagged because the pattern is the canonical N+1 query shape and would matter the moment image_providers crosses ~10 or PG goes non-local (deploy with managed Postgres → ~5–20 ms per round trip). Fix shape when it bites: (a) add `SecretsStore.getSecretsByIds(tx, ids: readonly string[]): Promise<Map<string, string>>` doing a single `SELECT … WHERE id = ANY($1::uuid[])` decrypt-fold; (b) bootstrap opens one `runInTx`, batch-decrypts every provider's secret, and hands a `(secretId → apiKey)` map to a sync `buildImageProvider(row, { apiKey, fetchOverrides })` variant. ~40 LoC refactor. Sibling pattern exists in `agent/handle-message.ts` where the snapshot loader already batches reads in one tx. Flagged on PR #222 review; deferred per reviewer's own note that it's "negligible".
- [ ] `p3` Composite index on `steering_rules(profile_id, channel_type)` — premature at personal scale (~30 rules), add when rule count or query latency grows
- [ ] `p3` Consider Renovate over Dependabot — Renovate has a built-in `node-lts` preset that genuinely tracks the Node LTS schedule, which Dependabot lacks. Currently using a workaround in `.github/dependabot.yml` (ignore all Node major bumps, manual yearly upgrade). Switch only if the manual cadence becomes annoying or if other LTS-tracking gaps appear.
- [ ] `p3` Cross-tier integration test for orphan `tool_use` repair — load a conversation with an existing orphan row (assistant `tool_use` followed by user text, no `tool_result`), fire `inbound/ready`, assert no Anthropic 400 and that the LLM request body llmock saw contains a synthesized `tool_result` block in the right position. Unit tests cover `validateHistory` and the agent loop's call into it independently, but not the stitch `agentStore.getHistory → loaded Message[] → runStreamingAgentLoop → repaired payload`. Best landed alongside the *Heal-on-persist* work in *Agent loop, context & conversations* above — that change adds a fixture-builder for messages-table seeds *and* makes the cross-tier behavior (load orphan → repair → persist clean → next turn loads clean) observable, so the integration test becomes both cheap and meaningful. Without heal-on-persist this is just a re-shape of the unit tests.
- [ ] `p3` Switch to a managed pull-through cache (AWS ECR PTC or Google Artifact Registry remote repository) only if `mirror.gcr.io` starts evicting the org-namespaced images Cogmo pulls. Today every image — `library/{alpine,redis,node,python}`, `pgvector/pgvector:pg18`, `inngest/inngest:v1.41.1` — routes through `mirror.gcr.io` (free, no auth, no maintenance), and Google's docs warn there's "no guarantee that a particular image will remain cached for an extended period of time" but in practice the mirror has held the popular org images we've probed. Trigger to act: a CI run failing with a 404 from `mirror.gcr.io` on pgvector/inngest. Cost shape if we move: AWS ECR PTC ≈ pennies/month for personal CI, requires a Secrets Manager secret with Docker Hub creds + a pull-through cache rule pointing at `docker-hub`. Don't pre-emptively migrate.
- [ ] `p3` Unit tests for `transport/registry.ts` `startChannels` — only `resolveCredentialSecrets` is unit-tested today (39% file coverage). `startChannels` (lines ~72–151) is a wiring function: read `transportStore.getAllChannels`, look up each `adaptersByType[channel.type]`, build a per-channel `Transport`, call `mod.start({ transport, ... })`, accumulate `{ functions, adapters, adapterMap }`. Drive it with `mockTransportStore` returning a fixture set of channels (one happy adapter, one unknown type to cover the warn-and-skip branch, zero channels for the empty-list case), and a mocked `AdapterModule` whose `start()` returns a sentinel adapter + functions array. Assertions: returned function array is the concatenation of per-channel returns; adapterMap is keyed by channel id; unknown type logs `unknown channel type` (spy on `logger.warn`); empty input returns `{ functions: [], adapters: [], adapterMap: new Map() }`. The harder-to-mock part is the `secretsStore`/`runInTx` plumbing — reuse `FAKE_TX` + `fakeRunInTx` from the existing test file. Currently deferred to integration where real adapters start; this would close most of the unit gap.
- [ ] `p3` Unit tests for `transport/attachment-store.ts` `createAttachmentStore` — file shows 14% coverage because no test imports the constructor; only the pure helper `mediaTypeToExt` gets covered transitively. Worth ~6 cases: upload writes a `${prefix}/${uuid}.${ext}` key with the `mediaType` as `ContentType`, default prefix is `inbound`, custom prefix is honored, unknown mediaType falls back to `bin` via `mediaTypeToExt`; download returns `Buffer` from `transformToByteArray`, `NoSuchKey` is rewrapped to `Attachment not found: ${path}`, empty `response.Body` throws `Empty response for: ${path}`. Use `vi.fn()` on `S3Client.send` switching by command instance: `cmd instanceof PutObjectCommand` records the args, `cmd instanceof GetObjectCommand` returns a fake `Body` with `.transformToByteArray`. Thin wrapper around AWS SDK so the marginal test value is small, but the file is presently a coverage hole and the tests would catch the kinds of regressions integration tests don't (path scheme drift, error rewrap shape, default-prefix change).
- [ ] `p3` Backfill missing unit tests for `transport/transport.ts` namespaces — file is 1469 lines / 75 tests / 65% line coverage. Existing `transport.test.ts` thoroughly covers `inbound.*`, `conversations.{getCurrent, switchTo, rename, recover}`, `profiles.{switch}`, but several methods have no test: `uploadAttachment` (single-line delegate to `attachments.upload`), `conversations.list`, `profiles.list`, `profiles.create` happy path + `model_unavailable` + `profile_name_taken` branches, plus the smaller methods around lines 642–664 / 845–875 / 999 / 1086–1110 (run `awk '/^SF:src\/transport\/transport\.ts$/,/^end_of_record$/' coverage/lcov.info | grep '^DA:' | awk -F'[:,]' '$3==0 {print $2}'` to refresh the line list before starting). Pattern matches the existing tests — `mockTransportDeep` for store overrides, the `mockSecretsStore` / `mockSkillRunner` pattern from existing cases, identity_rejected as the universal first branch. Probably a one-PR sweep — group by namespace (`conversations.list`, `profiles.list`, `profiles.create`, `uploadAttachment` = 4 sections, ~12–15 cases). The integration tier would also exercise these, but the cost of unit coverage is low and they fall in the "delegates to a single store call after identity check" shape that's been stable since transport.ts was written.
- [ ] `p3` `/repo add` HTTPS clone e2e against the production image — drive `transport.repos.cloneAndAdd` from inside the built runtime container against a TLS-fronted Gitea fixture (Caddy or nginx terminating HTTPS with a self-signed cert; existing `dev/containers.ts:154` gitea container reused behind it), asserting the registry row lands and the on-disk clone exists at `/var/lib/cogmo/repos/<name>`. Catches the package-surface bug class fixed in PR #211 (missing `ca-certificates` → `server certificate verification failed. CAfile: none`) *and* exercises path/permissions/error-mapping (`repo_clone_failed`, `github_identity_unavailable`) end-to-end. Existing Gitea integration runs git from the host over plain HTTP, so it can't catch either — the only signal that would have surfaced PR #211's bug is in-container TLS. Cost: ~100 lines of fixture (generate self-signed CA, mount as trusted root in the image at test time, point Gitea at it). Cheaper interim guard if appetite is low: a `runtime.e2e.test.ts` running `docker run $IMG sh -c 'which ssh && git ls-remote https://github.com/<stable-public-repo>.git HEAD'` — ~15 lines, network-dependent, covers the package-surface class but not the registry write. Land the smoke first, then promote to the full `cloneAndAdd` test if registry-shape regressions surface.
- [ ] `p3` `/repo add` SSH clone e2e against the production image — drive `transport.repos.cloneAndAdd` against a Gitea-on-SSH fixture (Gitea exposes SSH on port 22 by default) with a pre-seeded keypair, a `known_hosts` entry for the container, and the askpass/agent path the production code uses. Catches the SSH-binary surface (PR #211 fixed `cannot run ssh: No such file or directory`) plus regression coverage on the SSH-key wiring once it actually ships through `/repo add`. **Blocked on the SSH-key path being routed through `cloneAndAdd` first** — at the time of PR #211 the dialog hit `cannot run ssh` *before* any auth code ran, meaning the existing askpass/key code (`/var/lib/cogmo/askpass`, used today by the coding/skills pipelines) isn't yet plumbed into the `/repo add` clone. Until then a test would either skip auth (proves nothing real) or duplicate fixture (not production coverage). Promote when `cloneAndAdd` actually invokes the SSH-key path to clone.
- [ ] `p3` Worker-wasm host lifecycle failures (`src/skills/worker-wasm/host.ts:79-90, 153-175`) — worker emits `{type: "fatal"}` before ready (reject with "worker init failed"), worker exits before ready (reject with "worker exited before ready (code N)"), grace-period termination when the soft timeout fires before `taskPromise` resolves. Deferred from the L115 batch because these need a mocked `node:worker_threads.Worker` (drive `'exit'` / `'error'` / custom `'message'` events on a fake `MessagePort`), which the existing host.test.ts doesn't have — it runs end-to-end against real Pyodide. The lifecycle branches are real risk in production (Pyodide WASM ship failure → user sees a useful error message vs. an opaque timeout) but the mock setup is ~50 lines of fixture for ~3 tests. Promote when the host's failure-mode contract changes or when a real worker init crash slips through.
- [ ] `p3` Drop the auto-register subscriber's 180s wall-clock cap once `SkillRunner.register` accepts an AbortSignal. Today's `Promise.race` in `auto-register-skill.ts` is a bandage — it makes the Inngest function exit, but the underlying call leaks past it. With the exec-pty abort signal now in place (PTY exits within `timeoutMs`/`idleTimeoutMs`), the leak surface is smaller, but plumbing an AbortSignal through `runner.register` -> `makeSandboxLockfileCompiler` -> `execStreaming` is the structural fix. Drop the wall-clock cap + delete the "KNOWN LEAK" comment + delete the wall-clock-cap regression test when the signal lands.
- [ ] `p3` Refresh `src/agent/coding/claude-cli.integration.test.ts` fixtures next time they're touched anyway. The current execute-mode fixture captured claude denial-spiraling on permissions before `--permission-mode bypassPermissions` shipped — replay still passes (assertion is only `toolCalls.length > 0`; body matching is loose enough that the added flag doesn't break the order), but the cassette is misleading: it shows a broken conversation shape. No urgency — re-record opportunistically when a prompt-shape change or SDK bump forces it.
- [ ] `p3` Move `SKILLS_REPO_CONVENTION` (the file-layout + manifest + lockfile guidance in `src/agent/coding/prompt.ts`) out of the prompt builder and into a `CLAUDE.md` shipped on `main` in the bare cogmo-skills repo. Claude Code natively loads `/workspace/CLAUDE.md` from its memory tiers, so the prompt scaffolding wouldn't need to inject ~60 lines on every plan + execute call. The in-prompt block is a tactical stopgap until `bootstrapSkillsRepo` (or the wizard) seeds the bare repo's main commit with the file. Once the seed lands, drop the `repoConvention(repo)` call and the `SKILLS_REPO_CONVENTION` const.
- [ ] `p3` Move the skill-authoring auto-register from `coding/task/pr-opened` to a merge-gated path. Today's subscriber fires on PR-open and immediately calls register, which fast-forwards the local bare repo's main + mirror-pushes to origin. That collapses the "PR is the artifact" lifecycle — the PR exists but has nothing left to merge once register runs. Cleaner long-term: keep PR-open as a preview-only signal, gate the actual register on PR-merge (poll-merged subscriber every 2-5 min, dedup via a `processed_pr_ids` table). Opt-in `automerge: true` flag per skill mirrors Renovate's `packageRules` for the chat-authored flow. Promote when a real human-review tier surfaces.
- [ ] `p3` Typecheck `apps/server/scripts/` in CI. `apps/server/tsconfig.json` has `include: ["src"]`, so `pnpm typecheck` (CI, `.github/workflows/ci.yml`) skips `scripts/` entirely. Several scripts import from `../src/...` (e.g. `dev-infra.ts` → `src/secrets/encryption.js` + `src/web/auth/login-token.js`); a rename/move on the `src` side keeps CI green but breaks the script at the next dev's first run as a tsx runtime error. Fix shape: add `apps/server/tsconfig.scripts.json` extending the base with `rootDir: "."` + `noEmit: true` and `include: ["src", "scripts"]`, then wire `tsc -p tsconfig.scripts.json` into the package's `typecheck` script so `pnpm -r typecheck` covers it. **Blocker:** three pre-existing strict-mode errors must be fixed first or the new step fails — `scripts/tokei.ts:14` (`tokens[0]` is `string | undefined` under `noUncheckedIndexedAccess`; guard before `return [tokens[0]]`), `scripts/simplify-fixtures.ts:22` (regex capture `textMatch[1]` possibly undefined before `.trim()`), and `scripts/simplify-fixtures.ts:48` (`JSON.parse` returns `unknown` via ts-reset; `data.fixtures` needs a typed shape — define a small `FixtureFile` interface). Deferred from the `pnpm dev`-web-UI PR (#377) to keep that PR scoped — fixing two unrelated maintenance scripts didn't belong there.

## Later

### User-defined pipelines (Phase 8, slices 2-4)

- [ ] `p1` Pipelines slice 2 — run engine MVP: `pipeline_runs` table, generic `pipeline-stage-runner` Inngest function (transition tx in `step.run`, next-stage emit in separate `step.sendEvent`), `agentic` stages as agent-loop turns under per-stage tool allowlists via `Service`, `gate` stages via `step.waitForEvent` + Telegram inline keyboard (reuse coding-delegation approval machinery), `text`/`json` stage outputs, `start_pipeline` command trigger. Linear pipelines only. Inherits the known "real Inngest in integration tier" test-infra gap (PROGRESS Phase 6 test section).
- [ ] `p2` Pipelines slice 3 — loops, parked waits, cron, admission: back-edge execution with iteration reset (and extend the run store's `advanceStage`/`#recordAndMove` idempotency guard to key on `(stage, iteration)`, not stage alone — see the `TODO(slice 3)` in `src/agent/pipeline/store/index.ts`; today the guard is sufficient because iteration is invariantly 0) + `until` LLM evaluator (structured `{done, reason}`, cannot extend `maxIterations`); `wait` stages with `wait_key` partial-index resume + ticker-fired `TimeoutAction`; cron triggers riding `scheduled_tasks`; `queued` status + `max_concurrent_runs` + per-definition daily quotas. New runtime dep (agreed): `@marcbachmann/cel-js` for wait filters — same CEL dialect as Inngest `waitForEvent` `if` expressions, short waits pass filters through verbatim.
- [ ] `p2` Pipelines slice 4a — coding delegation as a stage: `agentic` stage instructions resolving to `Service.coding.delegate`, `plan` / `pr_metadata` artifact kinds flowing through `stage_outputs`, resume-same-session semantics for review loops (draft PR as the durable checkpoint).
- [ ] `p3` Pipelines run store — integration-tier concurrency test for the race-safety invariants. `DrizzlePipelineRunStore`'s `transitionStatus` / `#recordAndMove` / `#terminate` rely on `FOR UPDATE` + REPEATABLE-READ-40001 + the transactor's one-shot retry to serialize concurrent terminations and persists. The unit tests (PGlite, single-connection) only assert the *sequential* outcomes — true concurrency can't be exercised in-process. A `*.integration.test.ts` against Docker PG with two parallel `runInTx` calls racing on the same run would actually prove "two concurrent terminations can't both win" and the advance/terminate row-lock serialization. Acceptable to defer at single-user scale (the mechanism is a documented Postgres guarantee), but the claim is currently unverified by test. Pairs with the broader "real Inngest in integration tier" gap.
- [ ] `p3` Pipelines slice 4b — first external event source: GitHub PR review events (`pull_request_review` / `pull_request_review_comment` / `issue_comment` / `pull_request` synchronize+closed) via named Cloudflare Tunnel webhook with `X-Hub-Signature-256` check + ETag reconciliation poller backstop, or pure polling at personal scale; normalize onto the event bus as `github/pr.*` for `wait` stages and `event` triggers. See design/pipelines.md → Triggers for the parked research.

### Phase 2 — memory automation & ingestion

- [ ] `p2` Wizard recurring-tasks step (Phase 2) — opt-in flow that writes a single `scheduled_tasks` row per recurring action (morning briefing is the canonical example, not a special-cased function). Re-runnable, removable via `/schedules`. Calls `SchedulingService.create` directly (same validation path the agent tools use).
- [ ] `p2` Memory consolidation — daily `reflect()` via static Inngest cron (Phase 2) — genuinely system-wide, not user-defined; stays as a hard-coded cron.
- [ ] `p2` First ingestion agent: Gmail via MCP (Phase 2) — depends on MCP client Phase D below; polling cadence is itself a `scheduled_tasks` row, not a hard-coded cron.
- [ ] `p2` First ingestion agent: Google Calendar via MCP (Phase 2) — same shape as Gmail.
- [ ] `p2` Dual-mode monitoring for ingestion — embedding scan first, LLM only when relevant (Phase 2).
- [ ] `p3` `platformTs` ordering invariant under stale-fire downstream — integration test that drives a scheduled-task fire whose `next_run_at` is days in the past (post-outage catchup), persists the synthetic inbound with `platform_ts = scheduledFor`, then asserts `handle-message` + history compaction + the assistant reply path treat the stale `platform_ts` consistently (no out-of-order ordering in compaction, no "this is a duplicate" guard mis-firing). Belongs in the integration tier alongside the real-Inngest items in PROGRESS.md — fire-handler unit tests pin the inbound-side invariant (`platformTs === scheduledFor`), this catches what downstream layers do with that. Promote when integration-tier Inngest is in place.

### MCP client phases B–D

- [ ] `p2` MCP client Phase B — sysbox runner for `untrusted` servers (read-only rootfs, no host mounts, per-server egress allowlist), trust allowlist in code (`SHIPPED_TRUSTED_SERVERS`), full approval UX. Gates: every third-party server runs sandboxed.
- [ ] `p3` MCP client Phase D — OAuth 2.1 with PKCE + Dynamic Client Registration, encrypted refresh-token persistence, proactive refresh before tool dispatch (gemini-cli #23296 cautionary tale). Unblocks Google Workspace servers (Gmail, Calendar) — replaces the Gmail/Calendar entries above.
- [ ] `p3` MCP HTTP transport abort signal — `requestInit.signal` wired into an `AbortController` exposed up to the dispatcher so a hung connect / response wait can be cancelled deterministically from the call-timeout path, not just by the SDK's internal stream-only abort. Same gap exists for stdio (slow-starting subprocess hangs `client.connect`), so the work is a small abstraction (`Runner.spawn` returns `{ connection, abort }`) rather than HTTP-only plumbing. Defer until a real timeout incident or before Phase D, whichever comes first.
- [ ] `p3` Make `mcpRegistry` strictly required across `RegistryDeps` / `Transport`-deps, OR introduce a real `MCP_ENABLED` env flag that gates bootstrap construction. Today the field is `?:` but production bootstrap always constructs it, so the `mcp_disabled` code path (and its `TransportError` variant) only fires in tests — "rep exposure: a state the type permits but the runtime never produces" (cubic on PR #128 review). Two options: (a) drop the `?` and have tests wire a no-op registry mock — `mockTransport` already does it, just lifts the friction to other test setups; (b) add `MCP_ENABLED` defaulting to `true`, skip construction when false, give `mcp_disabled` real semantic weight. Phase B (sysbox sandboxing) might re-justify the optional path on its own ("MCP unavailable because sysbox missing") — fold this work into that PR if the timing aligns, otherwise it's a small standalone cleanup.
- [ ] `p3` Cache `McpRegistry.resolveTools` per-conversation when measured. Currently every conversation turn does `listEnabledServers()` + per-server `getApprovedToolPins()` — ~6 simple Postgres queries per turn at typical scale, free in practice. Becomes hot when (a) tool count grows past ~30 servers, (b) a real conversation-rate workload appears, or (c) profiling shows it. Cheap to invalidate: any `addServer` / `removeServer` / `approveServer` / `approveTool` / `rejectTool` write through the registry already runs in the admin path; just bump a per-server-set version on those writes and key the cache off `(profile.toolSet hash, version)`.

### Skills follow-ons

- [ ] `p2` Skills P3.1 foundation — skills store schema (`skills`, `skill_deploys`, `skill_runs`, `skill_context_calls`), `SkillManifestSchema`, local bare skills repo bootstrap at `$COGMO_SKILLS_PATH` with pre-receive hook (see [design/skills.md](design/skills.md) and PROGRESS.md P3.1)
- [ ] `p2` Skills P3.4 invocation (remaining) — failure handling (retry → notify → auto-disable after 3 consecutive failures → `/enable` / `/rollback` recovery), tier-2 progressive-disclosure (swap full SKILL.md body into tool description on selection). Cron scheduling shipped in 2026-05-19-skill-cron-dispatch.md — `skill-cron-ticker` + `skill-cron-fire` mirror the agent-scheduled-prompt machinery without sharing tables. (PROGRESS.md P3.4)
- [ ] `p2` Skills P3.5 cost accounting (remaining) — cost accounting via `ctx.llm` token metering + declared `cost_per_call_usd` summed per run + dispatcher daily/monthly budget enforcement from `SKILL.md.budget`. Blocked on `ctx.llm.complete` (P3.2 remainder). `skill_runs` resource tracking (`resource_usage` JSONB with wall-clock + peak memory) and `skill_context_calls` audit both shipped — see changelog `2026-05-19-skill-runs-resource-usage.md`. (PROGRESS.md P3.5)
- [ ] `p3` Bring `SkillStore.startOrRecoverRun` onto the `ON CONFLICT DO UPDATE` shape the other two keyed inserts use. Its `DO NOTHING` + `FOR UPDATE` re-select cannot resolve a concurrent loser under the project's REPEATABLE READ default — the winner's row is outside the loser's snapshot, `FOR UPDATE` doesn't change that, and `DO NOTHING` raises no `40001` for the transactor to retry, so `single([])` throws. Unreachable today: the agent-loop path keys on `(turn, iteration, position, args)` so two concurrent calls can't share a key, and skill tools aren't `parallelSafe`. Fix when the recovery-point machine is next touched — it also needs the `xmax = 0` discriminator to keep telling `new` from `recovered`, which is more delicate there than in the coding store because the caller then UPDATEs the row it locked. See `coding/store/index.ts` → `insertOrRecoverTask` for the shape.
- [ ] `p3` Pending-approval skill branch cleanup — `runner.register` only calls `deleteRef(branch)` when the deploy goes live (auto / notify tier). Approve-tier deploys leave the original feature branch behind on the bare repo even after `approveDeploy` advances main; long-running deployments accumulate stale `refs/heads/skill/*` refs. Cleanup needs the original branch name on `skill_deploys` (extra TEXT column or JSONB `pending_metadata`) so `executeApprove`'s `applyFilesystem` can also `deleteRef` it. Personal-scale tolerates manual cleanup until the ref count starts mattering; promote when there are 20+ unmerged feature branches or a register starts failing on ref-name collisions.
- [ ] `p3` Auto-extract skills from successful agent runs. When a turn solves a non-trivial, repeatable computer task (heuristic: >= 2 agent rounds or >= 2 tool calls), run a conservative LLM distillation that returns a structured skill (title / problem / solution / 3-7 steps / tags / confidence) or bails with the bare word null -- drop anything below ~0.6 confidence, dedup by title. Feeds the skills library without the user hand-authoring every skill. Fits the self-evolution ladder: extracted skills are *candidates*, gated through the existing approve/deploy path, not auto-deployed. cf. Odysseus `services/memory/skill_extractor.py` (prompt insists on null for one-offs / off-computer / pure-Q&A / failed runs). Gated on the skills runtime being routine + a staging surface for candidate skills to land in for review.
- [ ] `p3` Skills P3.6 deferred — `ctx.skills.invoke` composition, agent-led repair loop, search_skills retrieval layer. Move to Next individually when the trigger fires (skill count crosses ~50, tool-selection accuracy drops).
- [ ] `p3` Vend short-lived credentials where the underlying secret supports exchange. Stage 4 of `design/skills.md` → *Secret exposure and egress control*. Substitution (stage 2) protects a long-lived secret; minting avoids having one to protect. For OAuth-backed entries — `gmail_oauth` in the manifest example is exactly the shape — the binding can exchange the refresh token for a task-scoped access token with a minutes-long lifetime and hand the skill that, so a leak expires on its own. Bot tokens and plain API keys have nothing to exchange, so this narrows the blast radius for one secret class rather than replacing stage 2. Trigger: the first OAuth-backed secret a skill actually declares. Cheap to add per-secret-class once the binding machinery exists; pointless before it.
- [ ] `p3` Egress proxy for tier-2 raw sockets. Stage 5 of `design/skills.md` → *Secret exposure and egress control*, and the only case stages 1–4 leave open: a tier-2 skill using a bound secret over raw `httpx` instead of `ctx.http`. The mitmproxy-sidecar shape is well-trodden ([agent-sandbox](https://github.com/mattolson/agent-sandbox), Onyx) but costs a CA lifecycle, a run-correlation channel so the proxy knows whose bindings apply, a Python sidecar in the deployment, and `iptables` to stop the workload routing around `HTTP_PROXY` — unenforceable inside a sandbox we do not own, so it would work on the local-Docker backend and nowhere else, per `design/sandbox.md`'s "policy lives where it can be enforced". Note it is **additive to stage 2, not an alternative**: a proxy can only substitute for a placeholder the host already issued, so the `secrets.get` work is a precondition either way. Trigger: a skill needing a bound credential over a protocol `ctx.http` does not carry. Until then it buys coverage for a case worth refusing.
- [ ] `p3` `ctx.files` POSIX shim (stage + reconcile) — mount the workspace at `/files` in all three runtime substrates so Python libraries that take a path (`pandas.read_csv`, `pathlib.Path.read_text`, stdlib `open()`, `os.remove`) work unchanged. Spec landed in PR #204 (`design/skills.md` § File workspace, marker `[confirmed]`). Implementation work: (1) Define `TierFs` interface (`stage(spec) → Promise<StagedSet>`, `reconcile(spec, stagedSet) → Promise<ReconcileReport>`) with one host-side orchestration loop that calls `Service["files"].list/read/write/delete` and a SHA-256 `path → hash` map for the diff. (2) Add `Service["files"].delete(path)` to the interface + S3 backend in `src/agent/files.ts` (currently only `read/write/list`); add `ctx.files.delete` to the RPC dispatcher and `_Files` Python class. (3) Three substrate adapters: WASM (Pyodide MEMFS via `pyodide.FS.writeFile/readFile`); sysbox (host tmpfs bind-mounted at `/files` via `node:fs` + Docker mount config); Daytona (Daytona Volume `get-or-create` + per-user `subpath` mount via `sandbox.fs.uploadFile/listFiles/downloadFile`). (4) Wire stage call into worker host startup (`src/skills/worker-wasm/host.ts`, `src/skills/worker-sysbox/host.ts`, Daytona equivalent) and reconcile into the corresponding teardown path. Defer until a real skill needs `open()` ergonomics or library compatibility forces the issue — RPC `ctx.files.{read,write,list,delete}` covers programmatic access without it.
- [ ] `p3` Revisit PEP 734 subinterpreters for tier-2 isolation when the ecosystem catches up. P3.2.B.2 shipped a hand-rolled pre-fork supervisor (`os.fork()` per task, ~10-30ms COW from a parent that pre-imports `asyncio` / `json` / `traceback` / `uuid`) which is what subinterpreters were marketed as a faster successor to (~50ms per-interp vs ~10-30ms fork). Re-evaluate when (a) NumPy lands `Py_mod_multiple_interpreters` (open: numpy/numpy#24755, maintainer estimate ~1 year of rewrite); (b) at least one notable project ships subinterpreters in production; (c) async-aware `interpreters.Queue` API lands in stdlib. Likely 3.16+ territory — late 2027 at earliest. Until then the fork supervisor wins on simplicity, ecosystem compat (numpy/pandas/cryptography work), and battle-tested-ness; subinterpreters would only buy ~20ms per task. See `design/skills.md` "Why not subinterpreters" + the research notes in the PR.

### Web UI

- [ ] `p2` Web UI phase 2b — reconnect durability. Assign a per-turn monotonic event sequence at persist time and put it on the SSE `id:` field so `Last-Event-ID` replay is correct and idempotent — the `StreamEvent` `tool_result` variant carries no id, and on Inngest retry the streaming section re-runs (new tool ids), so replay must reconstruct from the persisted message, not the live stream. Decide the cursor at build time: lean (b) reuse the UUIDv7 `messages.id` (verify `pk()` mints process-monotonic ids so batch inserts stay ordered, + that a `(conversation_id, id)` index exists), else (a) a new `seq` column. Then add the in-flight-turn buffer + `Last-Event-ID` DB replay path in `WebUiAdapter`/the SSE route so a mid-stream refresh resumes seamlessly. Acceptance: a refresh during streaming continues without losing events. (Today 2a closes the tab session on disconnect, so abandoned tabs don't accumulate; revisit the GC story if reconnect needs the session to survive a blip.) Also: `use-chat`'s `touched` guard currently *suppresses* the history seed once an early live delta arrives on a mid-stream refresh, so prior history is dropped for that session until the next reload — history (DB UUID ids) and live messages (localId) are disjoint, so prepend history instead of suppressing it as part of this work.
- [ ] `p2` Web UI — AGENT/SYSTEM editing surface. 3b shipped the read-only screens (profiles / models / MCP / skills / scheduling / evolution audit) over the contract-first oRPC read surface; this adds the mutations the design doc calls for: profiles editor (base prompt / model / toolset / memory scope / voice / streaming knobs), MCP add → approve-server → approve/reject-tool, skills enable / approve deploys / view code, scheduled-task enable-disable. Each mutation appends a procedure to `webContract` (`packages/contracts`), its handler to the server `implement`, and its form/action to the screen with inline `Result`-error rows. The `evolution.getEvent` procedure already exists in the contract (the audit drawer currently renders the in-list payload directly) — wire it for a deep-linkable event view if/when events get their own route.
- [ ] `p2` Web UI phase 4 — hero screens. Memory browser (Hindsight search + trust filters + detail drawer), coding-task view (plan approval + worktree/PR status + live SSE log tail via the oRPC event-iterator), evolution audit feed + diff.
- [ ] `p3` Web UI phase 5 — responsive + polish. Container-query mobile pass (drawer nav, bottom-sheet inspector, stacked-card table fallbacks), motion pass, and a code-split pass (the SPA ships one ~511 KB / 156 KB-gzip JS chunk over Vite's 500 KB warning — assistant-ui + TanStack Router + cmdk; lazy-load routes / vendor-chunk to bring it under).
- [ ] `p3` Web UI — cover the remaining DOM-dependent gaps in the `apps/web` Browser-Mode component/hook suite (the harness, the screens, `screen-kit`, `use-resource`, `use-theme`, `CommandPalette`, and the `App` auth boundary already ship): the `use-chat` SSE event dispatch (ready / turn-end / turn-abort / stream-event, the touched-guard history merge); `App.tsx`'s `login`/`logout` transport-failure catches; `use-theme`'s localStorage `setItem` catch (private-mode); `CommandPalette`'s `run` action-failure catch (`console.error`); and `ChatIndex`'s list/create error state.
- [ ] `p3` Web UI — `/chat` index can orphan an empty conversation. `ChatIndex`'s effect creates a conversation when the user has none, but if they navigate away while `createConversation` is in flight, the row is created and the redirect is skipped — the `cancelled` guard can't un-create it, and the client can't reliably abort the server-side commit (aborting the fetch doesn't guarantee the server didn't persist). Only bites a brand-new user leaving during first-conversation creation; empty rows are invisible until the Phase 4 conversation list. Proper fix is server-side: lazy creation (don't persist until the first message lands) or an orphan-empty-conversation sweep.
- [ ] `p3` Web UI trusted-identity-header auth — add a header strategy to the fail-closed gate, configurable. Cloudflare Access: verify the signed `Cf-Access-Jwt-Assertion` JWT against Cloudflare's JWKS + the configured AUD. Otherwise accept `Tailscale-User-Login` / `X-Forwarded-User` only from a configured trusted source-IP range or with a proxy-injected shared secret; never plaintext-header trust on a publicly-reachable bind.
- [ ] `p3` Web UI rate-limit `POST /api/session` — bound login attempts. Acceptable to defer today (failed logins return before any DB write, the token is 256-bit + constant-time-compared, the body is capped at 64 KB, and the publish/proxy boundary is the real control); revisit if the exposure model changes (public bind, no fronting gateway), alongside the trusted-identity-header work.
- [ ] `p3` Web UI voice — mic-in (STT) + audio-out (TTS) in the chat cockpit, reusing the existing voice provider. Deferred from v1 scope.
- [ ] `p3` Web UI programmatic API channel — stabilize and document the oRPC OpenAPI surface as a real API channel for scripting the agent over HTTP. Internal-only until then.

### Future infrastructure

- [ ] `p3` Graceful shutdown handle for deferred boot work. `scheduleReconcileCrashedInstances` (and any future fire-and-forget at boot — Daytona snapshot pre-creation, MCP eager-connect, etc.) detaches its promise via `void`, so on rapid `SIGTERM`/`SIGKILL` the warn/error log callbacks may not fire before the event loop exits. Today's observable cost is one missing warn log per restart in a narrow timing window — no state corruption, no missed work (periodic reaper picks up orphans on its next tick). Fix when a real graceful-shutdown abstraction lands for other reasons (in-flight LLM calls, Inngest connections): the in-flight promise becomes a one-line `await` in the shutdown drain. Don't build the abstraction just for this. See PR #217 review residue and `design/infrastructure.md` → Boot-Time Blocking Policy.
- [ ] `p3` Local-model tool-calling fallback (dual dispatch). Gated on relaxing the cloud-only constraint (`design/overview.md` -> Constraints: "Cloud only for now -- revisit when local hardware justifies it"). When/if Cogmo adds local inference (Ollama / llama.cpp / vLLM), the OpenAI-compat adapter + agent loop need a text-fence tool path alongside native function-calling: many small/open models (DeepSeek especially) don't reliably emit `tool_use` blocks -- or the local endpoint doesn't expose the tools param at all -- so detect per-endpoint capability, and when native is unavailable, instruct the model to write a fenced tool block in its prose and regex-parse the call back out of the text. Native stays the default for capable cloud models; the fence path is the degraded fallback. Purely future-facing -- cloud models make this unnecessary today. cf. Odysseus `src/agent_loop.py` (`parse_tool_blocks` / `strip_tool_blocks`, returns `(tool_blocks, used_native)` per round).
- [ ] `p3` Artifact renderer — standalone web server for rich content (charts, tables, interactive views)
- [ ] `p3` All-in-one Docker image — single container bundling Postgres+pgvector, Redis, Inngest, Hindsight, Cogmo, supervised by systemd (or s6-overlay) inside. Base on `ubi9-init` or `jrei/systemd-debian`; one named volume for all persistent state; first-boot oneshot unit runs setup wizard. Targets the "simple user" one-liner path (`docker run -v cogmo-data:/var/lib/cogmo ghcr.io/.../cogmo-aio`). Decide vs. a `docker-compose.yml` at implementation time — compose is cheaper (~50 lines YAML) but keeps multi-container UX; AIO is ~1 week packaging work but yields true one-liner. Known hard problems: Postgres major-version upgrades across image tags (Home Assistant hits this; mitigate with `pg_upgrade` migration unit), backup story requires `pg_dump` via `docker exec` (not volume snapshot). Precedents: GitLab Omnibus (runit), Home Assistant OS.

### Future evolution & agent

- [ ] `p3` Prompt optimization — evaluation rubrics, bootstrapped few-shot (Phase 4)
- [ ] `p3` HyDE for auto-recall — generate a short hypothetical answer with a small/fast LLM, embed that instead of the raw user message. Improves recall quality on long/multi-turn turns where a mean-pooled embedding of raw chatter dilutes intent. Adds an LLM call on the hot path, so gate behind a profile setting and only consider after bumping `HINDSIGHT_API_RECALL_MAX_QUERY_TOKENS` + truncation (issue #120) is in place and measured insufficient.
- [ ] `p3` Sub-agents v1 follow-ons — the v0 (agents-as-tools) ships CLI-only CRUD and text-in/text-out delegation. Two extensions when warranted: (1) richer CRUD surfaces — a setup-wizard step, a web UI panel (oRPC `webContract` mutation + `sub_agents` list/create/delete), and a Telegram `/subagent` admin command, mirroring how `model_providers` / `image_models` grew their surfaces; (2) nested-loop sub-agents — a sub-agent that runs its own `runAgentLoop()` with a scoped (read-only?) `Service` and/or its own toolset, which re-opens the orchestrator-holds-secrets boundary and nested-durability (`step.run` IDs) questions deferred in v0. Start with read-only memory recall before any write/tool capability. (3) a pre-send context-window guard in the tool handler — today an oversized `task` + `context` surfaces as a provider error → `isError` tool_result (degraded, not a crash); a token check before the `provider.chat` call would fail faster with a clearer message.

### Future testing & code style

- [ ] `p2` Add a GritQL biome plugin banning `await import(...)` outside an explicit allowlist (`main.ts` CLI lazy-load, `worker-wasm/boot.mjs`). Pattern mirrors `biome-plugins/no-unsafe-cast.grit`; suppression via `// biome-ignore lint/plugin/no-await-import: <reason>` for the rare legit case. Built-in alternatives ruled out — Biome 2.x ships no `noDynamicImport` rule, and ESLint's `no-restricted-syntax` with `ImportExpression` would mean adopting ESLint just for one rule. Landing blocked on cleaning up ~20 existing violations across `scripts/`, `test/e2e-setup.ts`, `test/integration-setup.ts`, and several `*.test.ts` files: convert to static imports where the lazy form is unjustified, biome-ignore the rest. Single follow-up PR.
- [ ] `p3` Evaluate `@inngest/test` `InngestTestEngine` for our Inngest function tests. Today we mock `step` with `vi.fn()` and reach into `(fn as any).fn(...)` for handler invocation; the engine runs the real Inngest dispatcher (step memoization, replay, `step.waitForEvent`) and could catch a class of bugs the hand-rolled mocks miss. Trade-offs to weigh: execution-engine overhead vs hand-rolled speed, network side effects of real `step.sendEvent` calls, what (if anything) it offers for asserting function *config* (debounce/concurrency/triggers — currently `(fn as any).opts`). Candidates if we adopt: `handle-message.test.ts` (durable + non-durable boundary, resume policy), coding orchestrator tests (plan → execute → verify with `step.waitForEvent`), skills runner tests with the register/approve flow.
- [ ] `p3` First setup-wizard tests — `src/setup/wizard.ts` has no `wizard.test.ts` today. Every step is dependency-injected and testable in principle; the Keep/Replace branches and Save-anyway-on-validation-failure paths are state machines that benefit from regression coverage. Establishing the first wizard test (e.g. `stepConfigureDaytona` cycle: configure → keep on rerun → replace → save anyway) sets the pattern for the rest (provider, telegram, optional tools, GitHub, Claude OAuth).
- [ ] `p3` Replace `(err as Error).message` casts in `src/setup/validate.ts` with `err instanceof Error ? err.message : String(err)`. 8 occurrences (lines 37/55/74/92/132/157/201/212) — every validator catch arm. CLAUDE.md → "Code Style" forbids `as`; the file predates the rule. File-wide pass rather than touching one arm at a time.
- [ ] `p3` Replace `ConstructorParameters<typeof Daytona>[0]` with the SDK-exported `DaytonaConfig` type at both call sites: `src/sandbox/daytona/client.ts:76` and `src/setup/validate.ts:179`. The indexed-tuple inference reads opaquely; `DaytonaConfig` is exported from `@daytona/sdk`. Trivial swap, do both at once for consistency.

## Blocked

- [ ] `p3` Switch Hindsight LLM to gpt-5-nano — blocked on Hindsight emitting `max_completion_tokens` for GPT-5 models. Currently on gpt-4o-mini (~$10/mo vs ~$6/mo target). See `design/memory.md` → Known Gaps.
- [ ] `p3` Bump GHA sysbox e2e job from `ubuntu-24.04` to `ubuntu-26.04` once both ecosystems catch up. Status checked 2026-05-08: (a) `actions/runner-images` PR [#14002](https://github.com/actions/runner-images/pull/14002) merged the 26.04 image-generation pipeline 2026-05-05 but `Ubuntu2604-Readme.md` doesn't exist yet, no beta image release tag has been published, and tracking issue [#13964](https://github.com/actions/runner-images/issues/13964) is still OPEN — beta runner expected ~mid-May, `ubuntu-latest` flip ~Sep-Oct 2026; (b) upstream Nestybox sysbox ships its amd64 `.deb` via `downloads.nestybox.com`, and [#1005](https://github.com/nestybox/sysbox/issues/1005) reports sysbox **broken on 26.04** because AppArmor's `fusermount3` profile blocks the FUSE mount (only workaround is `aa-complain`, which defeats the point of using sysbox). Re-checked 2026-08-14 against v0.7.1 (2026-07-31): its changelog covers containerd 2.x on kernel 6.8+, sysbox-fs deadlock/nil-deref fixes and a grpc bump — no AppArmor or 26.04 entry, so the blocker stands. Re-evaluate when Nestybox cuts a release acknowledging 26.04 in `distro-compat.md`, OR #1005 closes with a profile fix. Sysbox is the binding constraint — runner image is the smaller blocker.
- [ ] `p3` Monitor RustFS as a long-term MinIO replacement — `cgr.dev/chainguard/minio:latest` (Chainguard's rebuild of upstream AGPL MinIO) is the current test container and the right call today, but MinIO Inc. archived `minio/minio` in April 2026 and any ongoing maintenance is via small community forks (`pgsty/minio`). RustFS (`github.com/rustfs/rustfs`, Apache 2.0, written in Rust, marketed as a drop-in S3-compatible MinIO replacement) is the most active "from-scratch" successor. As of 2026-08-15 it is at `1.0.0-rc.2` with no GitHub release objects — advanced from alpha, still short of the stable 1.0 this item waits on. Re-evaluate when that lands. The comparison against SeaweedFS and Garage this item calls for has since been measured against our actual `ensureFilesBucket` flow — see the `p2` "Replace MinIO" entry under Next for the table; RustFS wins on every axis except maturity, which is precisely the axis this item is blocked on. Keep Chainguard until at least one Apache-2.0 alternative is production-grade. The one thing that does not wait is pinning the floating `:latest` by digest, which the Next entry owns.
