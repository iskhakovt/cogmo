failure-threshold: warning

# DL3008 — pin apt package versions. Base images in this repo are pinned
# by digest, which fixes the OS package versions transitively, and the
# Debian/Ubuntu archives roll forward security patches under the same
# major version. Pinning every package fights `apt-get update` and creates
# rebase churn without buying additional reproducibility.
ignored:
  - DL3008
