Expand the "comments describe the current state, not migration history" rule in `.claude/rules/code-style.md` to name four drift categories with examples: direct contrast, implicit contrast (`without` / `now` / `no longer` / `back to`), PR/commit/date references, and incident framing.

Extend the same rule to `changelog.d/README.md` so per-PR fragments describe what the change IS, not the path that led there. The Conventional Commit + PR description own that history.
