LLM capability — `ThinkingBlock` in ContentBlock union with `thinking` config on `ChatParams`. Anthropic: native extended thinking, streaming accumulation, history replay. `clearOldThinking()` pre-pass in agent loop replaces old thinking content with empty (preserves block structure, frees tokens). `ResponseFormat` on `ChatParams` for structured JSON output — Anthropic via tool_use trick (synthetic tool + `tool_choice`), OpenAI via native `response_format`. Mutually exclusive with `tools`. `chatTyped<T>()` utility in `src/llm/typed.ts` — Zod schema → `responseFormat`, parse + validate response, retry with error feedback injection (default 2 retries). 284 unit tests.
