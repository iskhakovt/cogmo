Phase 6 P1 slice 2 — plan approval + execute (`awaiting_approval` → `pending_verify`). New `pending_verify` enum value (migration 0011) between `executing` and `verifying` — naming convention recorded: `awaiting_X` = human gate, `pending_X` = automatic transient. `ClaudeCodeBackend.execute(ctx, sessionId)` wraps `claude -p --resume <sid> --permission-mode acceptEdits` with the same JSONL parser + new `tool_use` / `tool_result` extraction from consolidated assistant/user messages (deduped against repeated blocks). New `CodingStreamingRegistry` — per-task in-process EventEmitter pub/sub bridging the durable orchestrator and turn-bound delivery; matches industry split (durable bus for state transitions, side-channel for token streams; Inngest's own Realtime is Redis-backed for the same reason). `Service.coding.delegate` becomes the **submit** path (insert task, emit `coding/task/start`, return `{taskId, status: "queued"}` immediately) — real LLM-facing API change vs slice 1; `DELEGATE_CODING_GUIDANCE` rewritten to tell the model to ack briefly and not speculate. Admission check (`repo.maxConcurrentTasks` + `countActiveTasksForRepo`) lands at the boundary now that multiple conversations could trigger concurrent tasks on the same repo. Plan-approval inline keyboard (Approve / Revise / Cancel) on the plan message, Telegram callback handler with **strict identity check** (resolves `callback.from.id` via `transportStore.resolveUser` and rejects strangers in the same chat with `identity_rejected`); atomic `approvePlanIfPending` / `cancelTaskIfActive` store methods make double-tap idempotent. Conversational revise (matches Cursor / Devin / Claude Code): cancels the task and posts a follow-up asking what should change; agent's next turn re-delegates. New `coding-task-execute` Inngest function triggered by `coding/task/plan-approved` reuses-or-recreates the task container (`sandbox.listContainersForTask` + `inspectContainer`; `--resume <sid>` rehydrates from the per-task home volume if the reaper got it during a long approval). Per-task progress message in Telegram (`CodingProgressSubscriber`): single message edited in place across plan + execute, header (phase emoji + truncated goal), status line (token counter, failure reason on terminal phases), last activity (executing only — `Read…`, `Edit ✓`), throttled at 500 ms; `plan_finalized` always force-edits to attach the keyboard without a stale interim post racing it; per-channel `telegram-coding-progress-<channelId>` Inngest function on `coding/task/start` resolves the chat via `transportStore.getActiveSessionsForConversation`. Token counts surface at `execute_complete` via the result event; `resource_usage` schema gains `cost_usd`; orchestrator translates camelCase `BackendUsage` → snake_case `ResourceUsage` at the persistence boundary. New events: `coding/task/plan-approved`. New `Transport.coding.{approvePlan, cancelTask}` namespace; new `TransportError` codes (`task_not_found`, `task_already_approved`, `task_not_pending_approval`, `task_already_terminal`). Decisions recorded: in-process EventEmitter beats Redis/Inngest for token streams (single-node deployment); retries=0 stays on both orchestrator functions (file edits + CLI session non-replayable mid-stream); `pending_verify` chosen over `cli_done`/`executed`/`awaiting_verify` for naming-convention symmetry with `awaiting_approval`. Design markers upgraded: `coding-delegation.md` → Backend Interface (plan + execute) / Container Lifecycle / Resume procedure / Plan gate all `[confirmed]`.
