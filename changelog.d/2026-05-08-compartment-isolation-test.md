Compartment-isolation integration test — exercises the `tag_groups` ACL end-to-end against a real Hindsight server. The new "compartment isolation via tag_groups recall" case in `src/test/memory.integration.test.ts` retains four memories via `retainBatch` (two `compartment:work + trust:first-party`, two `compartment:personal + trust:first-party`), then recalls with a profile-style AND-of-leaves filter (`buildScopeLeaves`-shaped: each dimension a leaf with `match: "any_strict"` so untagged rows can't leak in) scoped to `compartment:work`. Asserts at least one work memory surfaces (engineer / Acme / payments-service substrings) and no personal-compartment substrings (`pepper`, `hiking`, `kreuzberg`, `berlin`, `apartment`) appear in the result. Unit tests in `src/agent/service.test.ts` already cover that the right `tag_groups` shape gets constructed; this test catches mismatches between our `TagGroup` shape and Hindsight's server-side interpretation. The original blocker (sync `retainBatch` hung ~5 min inside Hindsight in replay mode while a `per_tag` consolidation prompt waited on a missing fixture) cleared in two steps: Hindsight #1375 was fixed in 0.6.0 (PR #1382), and the subsequent flip to `async: true` moved consolidation off the response path so the test no longer observes the wait. Remaining replay-mode flake — fixture-key drift on the bank-ID prefix `test-compartments-...` — fixed by extending `test/llmock-setup.ts`'s `\btest-\d{10,}\b → test-[ID]` normalizer to accept an optional `[a-z]+-` infix, so per-suite descriptive bank IDs normalize identically across record and replay. Test runs in ~1.1s in replay, ~5s in record. New fixtures: 4 OpenAI chat-completion + embedding fixtures captured for the work/personal retention prompts.
