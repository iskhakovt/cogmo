`WorktreeAssignment` is now a discriminated union — `{type:"host-path";branch;worktreePath}` for the bind-mount flow, `{type:"git-remote";branch}` for the future Daytona path that clones inside the sandbox. Local-Docker still gets `type:"host-path"` everywhere; the orchestrator + verify-orchestrator + teardown helpers narrow on the discriminator and throw a clear error if a transport they don't yet support shows up. The coding orchestrator's deps drop the `SandboxClient<LocalDockerSessionState>` shim and accept the wide `SandboxClient` — a new `isLocalDockerSessionState` guard gates the `setTaskContainerId` write so only the local-docker path persists a FK into the `containers` table. New `src/agent/coding/git-as-transport.ts` adds the host-side helpers Phase 3b.2.B will compose: `runBranchFor(taskId)` namespaces under `cogmo/run/<task-id>` (full UUID — the slice-4 `cogmo/<idShort>` feature branch can never collide), `pushTaskBranchToRemote` refreshes the local mirror's `origin/<defaultBranch>` and force-pushes it to the run-ref under one askpass helper, `fetchFeatureBranch` mirrors the sandbox's commit graph back to the host post-PR, and `loadIdentity` decrypts `github_identity:<name>` with the human-readable error from `describeResolveIdentityError`. Foundation only — orchestrator wiring + cleanup cron land in Phase 3b.2.B.
