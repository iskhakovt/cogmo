Dependency cleanup — removed `@testcontainers/ollama` (all test/dev setups moved to llmock via `hindsightSlim()`; `ollama()`/`pullModel()`/`OLLAMA_MODEL` factories and the `"ollama"` branch of `hindsight()` were dead code). `hindsight()` signature narrowed: drops `llmProvider` arg, now takes `{ apiKey, baseUrl? }` directly. Also dropped five never-imported packages from `package.json`: `superjson`, `ts-pattern`, `p-limit`, `msw`, `telegram-test-api` — all were aspirational entries that never got wired up (`design/tooling.md` still documents them as the approved pick when the need arises).
