Stage 1 evolution — Observer Inngest function (`src/agent/evolution/observer.ts`) triggered by `conversation/idle`. Extracts behavioral corrections from conversation transcripts via `chatTyped()` with Zod-validated structured output. Corrections persisted to `steeringRules` table with graduation model: `observationCount >= 2` promotes from learning (inactive) to rule (active, injected into system prompt). LLM-based semantic dedup compares new corrections against existing rules (new/reinforce/contradiction). Consolidation at 30+ active rules merges similar entries. Formatted readable transcript (tool calls as `[Tool: name(input)] → result`, images/thinking stripped). Global scope (industry standard). Safety: contradictions logged but not applied, safety-category excluded from extraction. New store methods: `getCorrections`, `upsertCorrection`, `countActiveRules`, `replaceRules`. 33 new unit tests across 4 test files.
