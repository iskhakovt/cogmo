Fix `openai-compat` provider sending `{role:"assistant", content: null}` with no `tool_calls` when an older assistant turn carries only thinking blocks. OpenAI-compatible backends (OpenRouter, Venice direct) reject this as a 400 — observed as "list object has no element 0" in production. The thinking-only state arises naturally when `clearOldThinking` zeros historic thinking content or when a profile switches from Anthropic to an OpenAI-compatible provider mid-conversation. `buildMessages` now drops the malformed turn at the wire boundary.
