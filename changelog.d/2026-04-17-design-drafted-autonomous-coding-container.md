Design drafted: autonomous coding + container sandbox. Two new design docs — [design/sandbox.md](design/sandbox.md) (container sandbox infrastructure — `sysbox-runc` as default runtime on Ubuntu 22.04+, Docker socket proxy pattern for lineage + policy at the Docker API level, `containers` + `cogmo_instances` + `networks` + `volumes` tables with `parent_id` / `root_task_id` / `depth` denormalized at insert, per-task systemd slice as `CgroupParent` for kernel-enforced hierarchical resource ceiling, reaper cron at 30s for TTL + orphan + stale-row passes, raw bidirectional socket pipe for hijacked endpoints + buildx `/session` so transparent buildx works in P1 without the moby/buildkit SDK, cache volume scoping split by integrity story — global for npm/cargo/go/apt, per-repo for pip + build caches; P2 sidecar extraction via single-image / two-subcommand topology with tRPC `SandboxRouter` control plane, transport deployer-configurable between unix socket and TCP) and [design/coding-delegation.md](design/coding-delegation.md) (Claude Code / Codex CLI subprocess delegation using the user's Max/Pro subscription — no API keys, no Agent SDK; `coding_tasks` + `coding_repos` tables; one container per task with worktree bind-mounted at `/workspace`, per-task home volume `cogmo-task-home-<task-id>` for CLI session files so resume works after idle teardown; worktree persistence via git remote — clean remove on success, `refs/cogmo-wip/<task-id>` push for dirty teardown, no S3 / tarballs default; three autonomy gates — plan (user-only, automated triggers skip), tool (stream-json bidirectional, not PreToolUse hooks — hook timeouts can't cover user-away gaps), merge (draft PR only, never merges); Prompt Construction section with self-verify clause retiring the Cogmo-side retry loop in favour of CLI-internal iteration + budget caps + single post-hoc verify; Concurrency section per-repo-serial default with install-lock relaxation as P3; Admission & Rate Limiting section for automated `trigger_source` — global cap, per-source daily quota, failure backoff with exponential pause, user-priority FIFO scheduler on Inngest cron; Inngest step boundaries table mirroring `handle-message` durable-around-streaming pattern; P1 disk-based credential delivery via `GIT_ASKPASS` with short-lived PATs on a bot account, SSH commit signing via `git config gpg.format ssh`, vault socket + GitHub App installation tokens deferred to P2; three-tier Implementation Phases P1/P2/P3). CLAUDE.md design-doc index updated with both entries. No code changes — design only.
