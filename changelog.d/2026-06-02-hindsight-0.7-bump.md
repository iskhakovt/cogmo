Bumped Hindsight to 0.7.2 — npm client, dev/test image pins (full + slim), and `cogmo.hindsightCompat` (now `>=0.7.0 <0.8.0`). Added a boot check asserting the bundled client's `CLIENT_VERSION` sits within that range, so a future dependency↔pin drift fails fast at startup instead of mid-turn.
