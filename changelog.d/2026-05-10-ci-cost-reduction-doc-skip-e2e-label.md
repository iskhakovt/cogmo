CI: doc-only PR detection via `dorny/paths-filter` (negation patterns + `predicate-quantifier: every`), `e2e` label gate on PR runs (auto-triggered by `pull_request: types: [labeled]`, label auto-removed after a green e2e run so the next push doesn't re-run e2e until you opt in again — failing runs keep the label sticky), single `Required Checks` aggregator job via `re-actors/alls-green`, `--coverage` instrumentation gated to main-push only on Unit + Integration jobs.
