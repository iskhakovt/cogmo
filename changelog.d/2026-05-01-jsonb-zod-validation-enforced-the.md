JSONB Zod validation enforced at the Drizzle driver boundary via a single `jsonbZod(name, Schema)` helper in `src/db/helpers.ts` (`customType` wrapper that runs `Schema.parse()` in `toDriver` + `fromDriver`, with `JSON.stringify` on the write side because `customType` doesn't auto-encode the way Drizzle's built-in `jsonb()` does). Three new shape schemas land alongside the existing ones: `ProviderAttrsSchema` (`{promptCaching?, headers?}` for `llm_providers.attrs`), `ToolSetSchema` (`string[]` for `profiles.tool_set`), `InboundContentSchema` (`string \| InboundBlock[]` for `inbound_messages.content`, replacing the `JsonValue` TODO in `transport/content.ts`). Every other JSONB column moves through the same helper: `messages.content` (`MessageContentSchema`), `containers.{labels,resource_limits}`, `networks.labels`, `volumes.labels`, `skills.{effects,inputs,outputs}` (with `inputs` upgraded to the stricter `SkillInputsSchema` to match what writers always validated), `skill_deploys.classifier_log`, `skill_runs.{inputs,output}`, `coding_tasks.{devcontainer,worktree_assignment,pr_metadata,resource_usage}`. `channels.credentials` stays raw `jsonb()` with an explicit OPAQUE marker comment per CLAUDE.md's exception (encrypted ciphertext the adapter hands back to the channel SDK — Cogmo never inspects it). Drizzle row inference is now the validated TS shape, so per-method `parseFooRow` wrappers and `JsonValue` casts are gone from sandbox/skills/coding/agent stores; `Transport.emit` and `transport.persistInbound` propagate the typed `InboundContent` through to adapters; the only remaining `JsonValue` uses are intentional (`channels.credentials` opaque, `Transport.deliver` arbitrary outbound). CLAUDE.md JSONB rule rewritten to point at the helper as the canonical pattern. Tests for `contentToBlocks` rewritten for the now-strict `string \| InboundBlock[]` contract — the speculative null/raw-object cases were testing implementation details no longer in scope. 1814 unit tests pass. Closes the standalone "Define InboundContent schema" `p2` todo as part of the same change.
