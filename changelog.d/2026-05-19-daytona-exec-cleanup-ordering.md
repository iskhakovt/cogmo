Fix Daytona `executeSessionCommand` failing with `DaytonaNotFoundError: session not found` on every natural exit. `getSessionCommand` 404s once the session is deleted, so cleanup must run after the exit-code fetch on the success path. The `wsPromise.then(...)` handler now wraps the success branch in `try/finally` — `cleanupSession()` runs exactly once on every settle path (`timedOut`, `disposed`, success, fetch-error) and always after the fetch. The catch branch also maps to `DisposedError` when `dispose()` raced the fetch and surfaced the 404, so consumers branching on outcome see the documented sentinel instead of the raw SDK error.

Strengthen the `Process` test double in `exec-streaming.test.ts`: `getSessionCommand(sid, _)` throws `DaytonaNotFoundError` after `deleteSession(sid)` has been called for that session, mirroring live Daytona. New tests pin (a) the relative order `getSessionCommand → deleteSession` on the success path, (b) the fake's own contract, and (c) the dispose-during-fetch race mapping back to `DisposedError`.
