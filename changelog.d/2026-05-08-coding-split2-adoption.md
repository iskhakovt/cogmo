Adopt `split2` for line framing in coding-delegation. Two sites converted: `src/agent/coding/jsonl.ts` (was `node:readline`'s `createInterface`) and `src/agent/coding/claude.ts` `drainStderr` (was a manual `indexOf("\n")` accumulator loop). Both now hand the framing to `split2` (Matteo Collina, ISC, zero deps — the line splitter pino is built on); the malformed-line tolerance and JSON.parse-with-warn pattern stay where they were. `split2` was also adopted earlier in `src/skills/worker-sysbox/transport.ts` for the tier-2 sysbox NDJSON RPC pipe. 429 coding-suite tests pass unchanged. Other line-buffered candidates considered and rejected: full-text subprocess-output buffering (`secrets/git-askpass.ts`, `agent/coding/verify.ts`) needs binary-safe `TextDecoder` capture, not line semantics; the in-process `worker-sysbox/host.ts` stderr drain is a passthrough to the logger with no parsing.
