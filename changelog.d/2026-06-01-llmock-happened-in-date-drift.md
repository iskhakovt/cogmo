Date-normalize the `(happened in <Month> <Year>)` temporal suffix that Hindsight stamps on each fact (from the current date) before embedding it — collapsing it to a stable `(happened in [WHEN])` token across both the chat (`normalizeContent`) and embedding (`embeddingInput`) llmock match paths, with the one affected fixture updated to match. The Hindsight evolution integration tests (`backfillProfileClass`, `migrateUntaggedMemories`) now match their `/v1/embeddings` fixtures regardless of which month they run in, instead of going stale on a month boundary.
