pnpm `overrides` entry pinning `ip-address >= 10.1.1` to clear Dependabot alert #38 (GHSA-v2v4-37r5-5v8g / CVE-2026-42338) — XSS in `Address6.group()`/`link()`/`spanAll()` and `AddressError.parseMessage`. Pulled in transitively via `@modelcontextprotocol/sdk → express-rate-limit@8.4.1 → ip-address@10.1.0`. Not exploitable here: `express-rate-limit` only calls `new Address6(ip)` for subnet matching and never renders HTML; we have no direct uses. Lockfile resolves to `10.2.0`.
