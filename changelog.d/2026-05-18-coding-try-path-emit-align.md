- Try-path failure emits in `runCodingTask`, `runCodingExecute`, and `runCodingVerify` now flow through `step.sendEvent` with idempotency `id: "task-failed-${taskId}"` — matching the catch-path shape introduced for in-worker durability. All four `coding/task/failed` emission sites across the three orchestrators now share one shape, so bus-level dedup is uniform whether the failure is a recoverable in-loop branch (try) or a thrown error (catch). Dropped the dead `inngest` field from `runCodingTask`'s `RunParams` — the function reaches the bus exclusively through `stepSendEvent` after this change.
