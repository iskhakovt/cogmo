Flip `HindsightMemoryProvider.retainBatch` back to `async: true` now that [Hindsight #1375](https://github.com/vectorize-io/hindsight/issues/1375) is fixed (by upstream PR #1382, shipped in 0.6.0). Multi-item batches no longer silently collapse into a single `document_id`, so we recover background processing for `conversation/idle` retain — extraction + embedding + consolidation move off the response-latency path and the Inngest step returns immediately rather than blocking on N sequential pipelines. Per-item UUIDv4 `document_id` stays in place. Removes the matching workaround section from `design/memory.md` (only the default `types` filter override remains under "Hindsight Adapter Workarounds"). Test in `src/memory/hindsight.test.ts` updated to assert `async: true`.
