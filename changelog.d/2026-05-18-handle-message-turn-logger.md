Extend `turnLogger` (a child logger bound with `runId` + `conversationId`) to the remaining `handle-message.ts` orchestrator emissions: `onFailure` notify-user error, "agent loop complete" info, auto-recall failure warn, voice over-cap info + notify-warn, batch-delivery image/document download errors. Closes the coverage gap from PR #257, which scoped the migration to in-loop emissions inside `runStreamingAgentLoop`. The evolution failure-reflector's planned join between structured logs and `conversation/degraded` / `conversation/errored` events now covers the orchestrator surface, not just the loop body. `onFailure` constructs its own child since it runs outside the main handler's scope, mirroring the same `runId` + `conversationId` binding.
