Telegram document attachments — both directions. **Inbound:** new `bot.on("message:document")` handler downloads via Bot API, uploads to `AttachmentStore`, emits a new inbound `document` block (`{path, mediaType, name}`); orchestrator resolves `document_ref` to a base64 `DocumentBlock` alongside the existing `image_ref` path. The handler also routes `image/*` MIME types — Telegram's "Send as file" path for PNGs/full-res JPEGs — to an `image` inbound block instead of `document`, so they reach the LLM's vision pipeline (Anthropic's `document` block doesn't accept image media types). New canonical `DocumentBlock` on `ContentBlockSchema` (`source: base64|url`, `data`, `mediaType`, optional `name`); Anthropic adapter transcodes the entire text family — `text/*` plus `application/{json,xml,yaml,x-yaml}` — through the API's `text` source variant (Anthropic only labels the wire `text/plain` but Claude reads structured text fine; the original filename rides on `title` so the model still knows what kind of text it was), forwards `application/pdf` as a base64 source, fails fast with a clear "unsupported mediaType" error pre-flight on anything else (e.g. `application/zip`, `application/octet-stream`) rather than burning a 400 round-trip; OpenAI-compatible adapter inlines `text/*` documents into a text block and stubs binary docs (Chat Completions doesn't have a portable document part). **Outbound:** new `send_document(filename, content, mediaType?)` agent tool — uploads to `generated/`, returns `{path, mediaType, name}` JSON; `RenderedMessage.documents` carries `OutboundDocument[]` ({data, mediaType, name}) parallel to `images`. `handle-message.ts` batch path resolves both image and document refs via `Promise.allSettled`; Telegram adapter sends docs via `sendDocument` (`InputFile(bytes, name)`) in both `deliver` and the streaming handle (`runId+path` dedup matching the image flow). `extractGeneratedImages` and `extractGeneratedDocuments` collapsed onto a shared `extractToolResultsByName(messages, toolName, parser)` helper — same two-pass scoping (build `tool_use` id→name map, then filter `tool_result` content) factored once. 2079 unit tests pass.
