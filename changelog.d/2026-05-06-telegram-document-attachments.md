Telegram document attachments — both directions. **Inbound:** new `bot.on("message:document")` handler downloads via Bot API, uploads to `AttachmentStore`, emits a new inbound `document` block (`{path, mediaType, name}`); orchestrator resolves `document_ref` to a base64 `DocumentBlock` alongside the existing `image_ref` path. New canonical `DocumentBlock` on `ContentBlockSchema` (`source: base64|url`, `data`, `mediaType`, optional `name`); Anthropic adapter sends Anthropic `document` content blocks (PDF base64/url + `text/plain` via `text` source); OpenAI-compatible adapter inlines `text/*` documents into a text block and stubs binary docs (Chat Completions doesn't have a portable document part). **Outbound:** new `send_document(filename, content, mediaType?)` agent tool — uploads to `generated/`, returns `{path, mediaType, name}` JSON; `RenderedMessage.documents` carries `OutboundDocument[]` ({data, mediaType, name}) parallel to `images`. `handle-message.ts` batch path resolves both image and document refs via `Promise.allSettled`; Telegram adapter sends docs via `sendDocument` (`InputFile(bytes, name)`) in both `deliver` and the streaming handle (`runId+path` dedup matching the image flow). 2020 unit tests pass.
