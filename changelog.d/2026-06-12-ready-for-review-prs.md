**Coding tasks now open ready-for-review PRs instead of drafts.** PR creation is already gated on the verify step passing, and the merge is the human-approval gate — at single-user scale the draft state only added a manual "ready for review" click, suppressed review-request notification routing, and doesn't exist on some GitHub plans. `runOpenPr` (renamed from `runOpenDraftPr`, `draft-pr.ts` → `open-pr.ts`) omits the `draft` flag on `pulls.create`; design docs updated to record the ready-for-review decision under coding-delegation's Merge gate.
