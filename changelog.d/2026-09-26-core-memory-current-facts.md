Core memory holds current facts only. The core-memory guidance and the `core_memory_update` description tell the agent that a change replaces the old value without mentioning it, a finished project leaves `active_projects`, and blocks carry no relative time words ("recently", "last month"), which go stale in a block every later prompt shows. What used to be true and details about a family member go to long-term memory, and the memory guidance and `memory_retain` list them. The routing and multi-turn live evals check blocks for relative time words, from a word list in `src/test/eval-checks.ts`. On Sonnet 5, the multi-turn eval (two samples per scenario) removes the old value in 6 of 6 changes, where two runs on the previous wording kept it as history in all 12, and no final block uses a relative time word (14 of 14, against 8 of 14); every fact reaches core memory in its turn and no rewrite loses an established fact. The routing eval (three samples per case) drops the finished project in 3 of 3, against 0 of 3, and core recall holds at 36 of 36 with blocks and 27 of 33 without, against 24 of 33. With no blocks, 6 of 27 onboarding writes still carry a relative time and 5 of 27 pick an unexpected block; design/memory.md records the rates.
