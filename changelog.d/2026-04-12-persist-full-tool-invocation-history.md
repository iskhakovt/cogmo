Persist full tool invocation history — `messages.content` now stores `ContentBlock[]` (text, tool_use, tool_result, image, thinking) instead of collapsing to final text. `AgentLoopResult.newMessages` exposes all messages produced per invocation (intermediate tool turns + final assistant). Orchestrator persists all of them in a single Inngest step. Pair-aware compaction (`snapToPairBoundary`) ensures summarize/truncate never orphan a tool_use/tool_result pair. Store interface tightened: `insertMessage` accepts `string \| ContentBlock[]`, `getHistory` returns `ReadonlyArray<Message>`. Prerequisite for Stage 1 correction extraction (needs to see tool choices, not just text).
