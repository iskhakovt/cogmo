Telegram response formatting + channel-scoped steering rules. `marked` v18 + custom HTML post-processor (`renderTelegramHtml`) converts agent markdown to Telegram's HTML subset. OpenClaw-informed edge cases: table→`<pre>` wrapping, emoji+bold preprocessing, native `<blockquote>`. `renderOutput` hook on `AdapterModule` — adapter-level pure function, DeliveryRouter calls per-session. Streaming: plain text during stream, HTML on final edit, 400 fallback. `steering_rules.channel_type` nullable column for channel-scoped behavioral rules. `PromptSource.assemble` takes context object `{ profileId, channelTypes }`. `getActiveChannelTypes` on TransportStore (joins sessions→channels). Default Telegram rules seeded at channel setup. `RenderedMessage` type, `AdapterEntry` in DeliveryRouter. Telegram adapter restructured from single file to directory module.
