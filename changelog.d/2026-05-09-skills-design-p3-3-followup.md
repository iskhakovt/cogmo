Catch up `design/skills.md` with the P3.3 implementation that landed in PR #190 (`/disable` + `/enable` + `/skills` Telegram commands) and PR #191 (AST-based risk classifier). Previous P3.x feature PRs (#156, #159, #181, #188) all bundled a design-doc update; this is the missing follow-up so future readers don't have to reverse-engineer the implementation from `git log`.

Updates: bump "Risk tiering & auto-apply" from `[proposed]` to `[confirmed]` (it's now real); replace the design-only classifier section with implementation specifics (tree-sitter via `web-tree-sitter` + WASM, vendored grammar, rule tables in `ast-rules.ts`); add the explicit threat-model framing — UX gate not security boundary, sysbox + secret allowlists carry security, `getattr(__import__("os"), "system")` is a documented known false negative; document the AST → stub fallback path with `classifier_version` shifting in the audit log so degraded runs are visible. RPC signature gets `enable` (new), the typed `DeregisterResult` / `EnableResult` discriminated unions (replaces thrown errors), and `listAll` (operator-facing inventory). Recovery section adds `/enable` and `/skills` alongside the existing `/disable`, plus the approval-gate guard rationale (`/enable` refuses skills whose current sha was never live, closing the denied-on-first-deploy → re-enable bypass loop).

No code changes — pure documentation refresh.
