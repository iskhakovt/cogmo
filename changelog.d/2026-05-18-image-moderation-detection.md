- Add `detectImageFailure` in `src/agent/image-moderation.ts` and wire it into the `generate_image` tool: on a successful `generateImage` response, two cheap signals run before the AttachmentStore upload — fal's normalised per-image `nsfw` flag from `providerMetadata.fal.images[i].nsfw` (with concept names when fal returns them), plus a cross-provider byte-length canary (default 2048 bytes) that catches solid-color / blurred placeholder fallbacks.
- On detection, the tool returns `Error: ${reason}` to the LLM (same soft-error pattern as the existing unknown-model / unsupported-ratio paths) so the model can rephrase, switch model, or report back — and a `logger.warn` records the event for operator visibility. No placeholder bytes are uploaded; no Telegram delivery is attempted.
- Scope: fal providerMetadata + size canary only. Venice's `x-venice-is-*` response-header signals are handled by a sibling work that ships its own detection alongside the new `venice` provider type; a follow-up PR will unify both paths behind one error type. Pixel-level analysis remains a non-goal — deferred until production traffic shows a real case the cheap signals miss.
