Unit test coverage of the codebase expanded from 86% to 91% lines and from 78% to 83% branches at the merged-flag baseline (unit + integration). The setup wizard is the largest single contributor — eight private step functions (`stepConfigureClaudeCodeAuth`, `stepConfigureDaytona`, `stepValidateHindsight`, `stepSummary`, `stepConfigureOptionalTools`, `stepConfigureTelegram`, `stepConfigureGitHubIdentity`, `stepConfigureSkillsRemote`, `stepConfigureProvider`, `stepConfigureImageProviders`) are now exported and tested against mocked `@clack/prompts`, taking `setup/wizard.ts` from 14% → 71% line coverage. Long-tail coverage added across `skills/configure-remote-prompts.ts` (1.9% → 97%), `agent/evolution/migrations-cli.ts` (25% → 90%+), CLI subcommands (`cli/{image-provider,model,image-model}.ts`, `skills/cli.ts`), Telegram `commands.ts` `/profile stream` and `/profile class` error branches, the `profile-dialog`/`repo-dialog` error paths, `transport/transport.ts` evolution namespace, and `setup/validate.ts` error branches. No production behaviour changes; the only source edit is widening eight wizard-step `function` declarations to `export`.
