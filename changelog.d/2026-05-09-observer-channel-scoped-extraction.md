Observer channel-scoped extraction. The post-conversation correction extractor now scopes new steering rules to the channel they came from instead of always landing them as global. `runObserver` adds a `load-active-channel-types` step that calls `transportStore.getActiveChannelTypes(conversationId)` and threads the result into `extractCorrections`; the extraction prompt renders the active channel set so the LLM has the context to decide whether a correction is medium-specific ("avoid markdown headings here" on Telegram) or general ("be concise").

The discriminated-union Zod schema gains a `channelType: string | null` field on the `new` variant only — `reinforce` / `contradiction` inherit scope from the matched id. Existing rules in the prompt now render with their `(category, channel:foo | all channels)` scope so the LLM can correctly distinguish a global "be concise" from a Telegram-specific variant and avoid false reinforcement. `AgentStore.getCorrections` returns `channelType` per row (`text` column already existed via the Phase-1 manual-rule path, so no migration). `applyCorrection` validates the LLM's choice against the active channel set before persistence: anything outside the set falls back to `null` with a structured warning, so a hallucinated `slack` value can never land in `steering_rules.channel_type` when no Slack session was active.

`consolidateRules` excludes channel-scoped rows from its merge candidates because `replaceRules` always emits the merged row as global — silently merging a Telegram-only rule into a global one would drop `channel_type` without the operator noticing. Channel-scoped rules stay un-consolidated until consolidation grows real channel-aware merge semantics; this is fine in practice because consolidation only fires above 30 active rules and personal scale sits well below that.

Wires `transportStore: Pick<TransportStore, "getActiveChannelTypes">` into `ObserverDeps` and bootstrap. Test additions: 4 new unit cases in `extract-corrections.test.ts` (active-channel pass-through, hallucinated-channel coercion, prompt rendering of existing-rule channel scope, no-active-channels fallback message), 4 new schema/prompt cases in `extraction-schema.test.ts`, a new "excludes channel-scoped rules" guard in `consolidate-rules.test.ts`, an updated `getCorrections` store assertion that pins the new column round-trips, and a real-PG integration case in `observer.integration.test.ts` that seeds an active Telegram session, stubs the LLM to return a `channelType: "telegram"` correction, and verifies `steering_rules.channel_type = 'telegram'` lands on the inserted row. Per-test cleanup widened to `channel_sessions`, correction/evolution-sourced `steering_rules`, and channels marked `_test: true` in their credentials JSONB.

Behaviorally compatible with existing data: rules with `channel_type IS NULL` keep behaving as global, and `getActiveRules` (the runtime read path that filters by active channels) was already channel-aware from Phase 1. 2810 unit tests pass; typecheck + lint clean.
