Anthropic-deprecated model migration. Migration `0019_replace_deprecated_models.sql` rewrites `profiles.{model,summarization_model,extraction_model}` and `model_providers.model` for `claude-sonnet-4-20250514` → `claude-sonnet-4-6` and `claude-opus-4-20250514` → `claude-opus-4-7` (Anthropic retiring both on 2026-06-15). `model_providers` rewrite uses dedup-then-rename plus a position-bump pre-pass to satisfy `unique(model, provider_id)` and `unique(model, position)` if an operator registered both old and new ids. `messages.model` deliberately untouched (audit trail). `MODEL_REGISTRY` (`src/llm/models.ts`) overhauled: dropped `claude-sonnet-4-20250514`, `claude-opus-4-20250514`, `claude-haiku-3-5-20241022` (the retired Haiku 3.5); added current-tier `claude-opus-4-7`, `claude-opus-4-5-20251101`, `claude-opus-4-1-20250805`, `claude-sonnet-4-5-20250929`; fixed `claude-sonnet-4-6` / `anthropic/claude-sonnet-4.6` `maxOutputTokens` 128k → 64k (Anthropic docs confirm 64k synchronous output for Sonnet 4.6); added direct-OpenAI flagships `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini`; added new OpenRouter routes `x-ai/grok-4-fast` and `google/gemini-2.5-flash-lite`; corrected pre-existing `google/gemini-2.5-flash` `maxOutputTokens` 65_536 → 65_535 to match the OpenRouter API. Defaults updated: `src/setup/seed.ts` default profile, `src/agent/evolution/observer.ts` `DEFAULT_EXTRACTION_MODEL`, and `src/transport/adapters/telegram/profile-dialog.ts` example string all switched to `claude-sonnet-4-6`. Test files (`src/test/factories.ts`, `src/llm/models.test.ts`, plus 9 `.test.ts` files using the deprecated id as opaque mock value) bulk-renamed; `models.test.ts` assertions updated to match Sonnet 4.6 limits (1M context, 64k output, budget 926k). New `migration-0019.test.ts` (PGlite) seeds each `model_providers` conflict shape (rename-only, same-provider dedup, position-only bump, both-conflicts together, multi-row position fan-out, profiles three-column rewrite, untouched-when-unrelated) and asserts post-migration state — pins the position-bump SQL against future regressions. `getModelLimits` carries an inline invariant comment: only ever called with the current turn's model resolved from `profiles.model` (via `snapshot.model` in `handle-message.ts`); historical `messages.model` rows are write-only, may reference retired ids, and must never be routed through the registry — verified by grep (`getHistory` / `getMessage` / `getLastAssistantMessage` all project everything except `model`). New `p3` todo: warn at startup when a `profiles.model` / `model_providers.model` row points at an id missing from `MODEL_REGISTRY` — surfaces retired Anthropic ids and any non-Anthropic deprecations to the operator instead of silently substituting a guess. Out-of-scope: Grok 4.3 (max output not exposed), Gemini 3.x previews (not stable), `gpt-5.4-nano` (limits unknown).
