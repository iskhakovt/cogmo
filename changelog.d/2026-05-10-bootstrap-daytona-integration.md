Bootstrap-level integration test for the Daytona sandbox arm (`src/test/bootstrap-daytona.integration.test.ts`). Adds `BootstrapOptions.sandboxClientOverride` so tests can wire `FakeDaytonaSandboxClient` (shipped 3c.1) into `bootstrapSandbox` without `SANDBOX_BACKEND=daytona` env or a `daytona_api_key` secret — skips env-driven backend selection, treats the override as coding-capable (`sandbox` AND `codingSandbox` resolve to it), generates a `randomUUID()` instance id, and runs `reconcileCrashedInstances` on the override. Production wiring is unchanged; the override field is the same shape as `providerOverride` / `falFetchOverride` / `voiceFetchOverride`.

The test wires stages individually (`bootstrapCore` → `bootstrapSandbox` with override → `bootstrapSkillRunner(core, NO_SANDBOX)` → `bootstrapRuntime`) so the tier-2 skill worker pool, which needs a stdin-capable sandbox the host-side fake doesn't provide, stays out of scope. Asserts the four things 3c.2 was carved out to prove: capability shape (`workingTreeTransport: "git-remote"`, `hostBindMount: false`, `volumes: "managed"`), `healthCheck` resolves, the coding-orchestrator + verify + cleanup-run-branch + orphan-sweep cron + per-repo functions all register, and the local-docker `sandbox-reaper` does NOT (gated on `sandboxDocker`, which the override path leaves null because the reaper queries the Docker daemon directly).

Also fixes a stale comment + return value in `bootstrapSandbox`'s daytona branch: 3b.2.B made daytona coding-capable via git-as-transport, but the branch still returned `codingSandbox: null` per a Phase 3a comment. Updated to `codingSandbox: sandbox` so the orchestrator + cleanup functions register on real Daytona deployments too — orchestrators branch on `capabilities.workingTreeTransport`, not backend identity.

Phase 3c.2 — first slice after 3c.1 (the host-side fake). Unblocks 3c.4 (reaper / orphan reconcile) and 3c.5 (cost dashboarding) — both want to register additional Inngest functions in the daytona arm and want this same bootstrap-level smoke as their integration coverage.
