**Reconcile `/.cogmo-askpass/` doc/comment/test-fixture drift to match production `/tmp/cogmo-askpass`.** `CONTAINER_ASKPASS_DIR = "/tmp/cogmo-askpass"` (`src/sandbox/askpass.ts:41`) is the bind-mount target both backends actually use — but several places described the path as `/.cogmo-askpass/` instead: the file-header docstring on `src/sandbox/askpass.ts` itself (contradicting the constant declared 30 lines below), `src/sandbox/daytona/askpass-upload.ts` docstring, `verify-orchestrator.ts` comment, `DEPLOYMENT.md` operator-facing description, and `containerDir` fixtures in `src/sandbox/supervisor.test.ts` + `src/sandbox/daytona/client.test.ts` (the supervisor parameterizes `containerDir`, so the fixtures didn't break — just diverged from what `provisionAskpass()` actually returns). Mechanical replacement; no behavior change. The `<sandboxRoot>/.cogmo-askpass` paths in `src/test/daytona-sandbox-fake.ts` are left alone — that's a legitimate host-side mirror subdir created at `src/test/daytona-sandbox-fake.ts:265`, not the canonical container path.
