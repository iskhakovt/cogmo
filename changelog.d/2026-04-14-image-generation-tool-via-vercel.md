Image generation — `generate_image` tool via Vercel AI SDK (`ai` 6.x + `@ai-sdk/fal`), fal.ai as first provider. Curated `MODEL_CATALOG` enum (Flux Dev/Schnell/Pro/Pro-Ultra/Kontext), LLM picks per-call from tool description guidance. Graceful degradation when `FAL_API_KEY` missing. 4xx classification via `APICallError.isRetryable` (SDK-structured, not substring). `AttachmentStore.upload()` gains optional `prefix` param — generated images stored under `generated/`. Mid-stream delivery: Telegram stream handle parses `tool_result` for `generate_image`, downloads bytes, `sendPhoto` during the loop (dedup on `runId + path`, marked only after successful send so transient failures don't block retries); placeholder stripped after delivery. Batch path for non-streaming channels: `RenderedMessage.images`, `DeliveryHandle.deliverBatch(content, images?)` + `hasBatchTargets()` gate to skip S3 downloads on streaming-only setups, pure `extractGeneratedImages()` helper scoped by `toolUseId → toolName` to avoid false positives, wrapped in `step.run("batch-delivery")` for exactly-once on retry + Inngest observability (small `{delivered, failed}` return; image bytes flow through body in memory only). `Promise.allSettled` per-image resilience. `AdapterDeps` gains `attachments`. `isRenderedMessage` type guard shared by Telegram + Direct adapters. Direct adapter `directOutbound` event extended with base64 images. Wizard prompts for `fal_api_key` (no live validation — no cheap ping endpoint). `generate_image` added to default `tool_set`. Design doc: [design/image-generation.md](design/image-generation.md) with rationale for AI SDK (images) vs raw SDK (text LLMs, blocked on `countTokens` PR #12176 + thinking signature bug #11602). `design/crash-recovery.md` durability map updated. 34 new unit tests across extract-images, image-tools (incl. abort classification), telegram stream handle (incl. failed-sendPhoto retry + placeholder stripping), delivery-router (incl. `hasBatchTargets`), orchestrator.
