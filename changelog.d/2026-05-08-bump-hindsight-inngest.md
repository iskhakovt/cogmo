Bump Hindsight to 0.6.0 and Inngest to 4.3.0. `@vectorize-io/hindsight-client` moves `^0.5.6 → ^0.6.0` and the test container `ghcr.io/vectorize-io/hindsight-api` moves `0.5.1-slim → 0.6.0-slim`, picking up the TypeScript client's `AbortSignal` support across all methods (#1198), exposed missing recall/reflect params (#1362), and server-side fixes including OpenAI-compatible JSON hardening (#1368), embedding `allowed_openai_params` for dimensions (#1320), `observation_sources` dedupe with ON CONFLICT (#1340), atomic batch-retain commit (#1343), retain-all-documents in async multi-document batches (#1382), and per-document graph counts including observations (#1374). `inngest` moves `^4.2.6 → ^4.3.0` and the dev container `inngest/inngest` moves `v1.9.1 → v1.19.2` to keep the dev server roughly in step with the SDK. Cogmo's call sites in `src/memory/hindsight.ts` (HindsightClient + raw sdk for recall/reflect) need no changes — both bumps are additive at the surfaces we use. Integration fixtures in `test/fixtures/recorded/` were re-recorded against 0.6.0-slim (additively — wiping all timestamped fixtures broke the voice round-trip test, which pairs LLM and TTS fixtures by exact reply text and can't tolerate a fresh non-deterministic Anthropic reply without re-recording the TTS side at the same time). All 2298 unit tests and 97 integration tests pass.
