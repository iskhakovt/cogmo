MCP integration — design doc + Phase A foundation (PR 1 of 3). New canonical [design/integrations/mcp.md](design/integrations/mcp.md) splits MCP out of `integrations.md` (now a pointer + non-MCP target table); covers goals/non-goals, module layout, data model, interfaces, lifecycle, trust + sandboxing, auth, failure handling, Hindsight-stays-on-native-client rationale, testing, captured decisions; phased `[proposed]` markers for A (core) → B (sysbox sandbox) → C (HTTP) → D (OAuth) → E (resources/prompts). New `src/mcp/` module owns its store: `mcpServers` (config blob validated via `McpServerConfigSchema` — Zod discriminated union over `transport: "stdio"\|"http"\|"sse"`, no separate transport column; `enabled`, `approval_status pending\|approved\|needs_reapproval`, `last_connected_at`, `last_error`, name `/^[a-z][a-z0-9_]*$/`) and `mcpServerTools` (`schema_hash` + `schema_snapshot` JSONB validated by `ToolSchemaSnapshotSchema`, per-tool `approval_status pending\|approved\|rejected`, `unique(server_id, tool_name)`, `ON DELETE CASCADE` from servers). `McpStore` interface + `DrizzleMcpStore` implementation: `addServer`, `removeServer`, `getServerById`/`ByName`, `listServers`, `listEnabledServers` (hot path), `listServerStatuses` (admin view with tool counts via grouped left-join), `setEnabled`, `setServerApprovalStatus`, `recordLastConnected`/`recordLastError`, `upsertToolPin` (on-conflict update by `(server_id, tool_name)`), `getToolPins`/`getApprovedToolPins`, `setToolApproval`, `deleteToolPin`. `McpValueSource` discriminated union (`literal`/`secret`) lets `env`/`headers` mix constants with encrypted-secret refs in one map. Tool naming locked at `mcp__<server>__<tool>` (Claude Code convention) with `composeMcpToolName` + server-name regex enforcement at the store boundary. `compileToolMatchers` wraps **picomatch 4.0.4** (new direct dep, plus `@modelcontextprotocol/sdk@1.29.0` ahead of PR 2) for profile `toolSet` glob filtering — supports exact names, wildcards, and brace expansion case-sensitively. Migration `0018_skinny_master_mold.sql` adds the two enums + tables + FK; `src/db/schemas.ts` barrel and `src/test/pglite.ts` schema bootstrap updated. 52 unit tests across `config.test.ts` (33: value sources, transport variants, snapshot schema, server name validation, tool-name composition, glob matchers across exact/wildcard/brace/case-sensitive cases) and `store/index.test.ts` (19: server CRUD round-trip, duplicate-name + invalid-name + malformed-config rejection at the JSONB boundary, approval transitions, error-vs-connected-timestamp interaction, pin upsert on conflict, approved-only filter, single-pin delete, cascade on server delete, status view with mixed counts). `data-model.md` deferred-table list updated; CLAUDE.md design-doc index split. Typecheck clean; remaining biome warnings are pre-existing (`biome.json:2:14 deserialize`, `agent/coding/*` test suppressions).
