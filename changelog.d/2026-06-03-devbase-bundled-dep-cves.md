`docker-bake.hcl` now owns the task images' toolchain version pins (uv, pnpm, npm, Claude Code) and passes them as build args, so a bump is one line with no drift between `cogmo-devbase` and `cogmo-skills`. `cogmo-devbase` installs pnpm via npm (11.5.1, matching the workspace `packageManager`) instead of `corepack prepare` and upgrades npm to 11.16.0, clearing the corepack- and npm-bundled-dependency CVEs (tar, picomatch, minimatch, brace-expansion, ip-address) the Trivy scan flagged. The workspace pnpm bumps to 11.5.1, and the transitive `tmp` dependency is pinned `>=0.2.6` via a pnpm override.
