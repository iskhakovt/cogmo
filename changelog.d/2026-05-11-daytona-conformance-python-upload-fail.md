Second conformance scenario for the daytona suite. `python-upload-fail` uploads an inline Python script via `fs.uploadFiles`, runs it through `executeSessionCommand`, and asserts that stderr lands in the `onStderr` callback (separate from `onStdout`) and that `getSessionCommand` returns exit code 2. Closes three WS-side gaps the existing `create-exec-delete` fixture doesn't reach: the multipart `/files/bulk-upload` REST endpoint, stderr WS demux in `getSessionCommandLogs`, and non-zero exit-code retrieval.

Conformance test refactored to support multiple scenarios cleanly: `scenarioState()` + `createScenarioMock()` helpers factor out the record-vs-replay boilerplate so each describe block owns its own mock + fixture lifecycle without sharing module-level state. The fixture itself stays scenario-scoped — one JSON per scenario under `test/fixtures/daytona/`.

Fixed one real mock bug found while recording: `#prepareUpstreamHeaders` was forwarding `Transfer-Encoding: chunked` to upstream. Node's `fetch()` (undici) sets its own transfer encoding based on the body Buffer and throws `InvalidArgumentError: invalid transfer-encoding header` if it sees one on the input — multipart uploads from form-data set chunked encoding, so the bug only surfaced on uploads, not on the JSON-bodied paths the first scenario exercises. Stripped the full hop-by-hop list now (`transfer-encoding`, `keep-alive`, `proxy-*`, `te`, `trailers`, `upgrade`) for safety.

Gaps still deferred: `git.clone`, `sandbox.refreshActivity`, `sandbox.start` (resume from stopped), each one HTTP endpoint. Low marginal value until production drift surfaces; can be one-shot recordings later.
