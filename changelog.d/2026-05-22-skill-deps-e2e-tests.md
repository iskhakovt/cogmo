End-to-end coverage for the skills tier-2 deps stack. `runner.sysbox.integration.test.ts` gains a test that registers a skill declaring `idna==3.10` with a committed `requirements.lock`, invokes it against a real `cogmo-skills:test` sandbox, and asserts the populator landed the venv under `/skill-venvs/<hash>/`, the activation path put it on `sys.path`, and the skill's `import idna` resolves at the pinned version. Catches regressions in the populator script, the shared-volume mount, the activation `sys.path` prepend, and the venv permission set-up that unit tests can't see. The Daytona conformance suite gains a `wrapper-volume-mount` scenario that drives `DaytonaSandboxClient.create({ depsCacheVolume })` against the mock-or-real Daytona API to pin the `daytona.volume.get` + `volumes: [{ volumeId, mountPath }]` wire shape — fails replay loudly if a future SDK rev renames the field. `__registerForTests` now accepts `lockfileContents` so e2e tests can seed skills with deps without bootstrapping a real git repo for the register-time compile.
