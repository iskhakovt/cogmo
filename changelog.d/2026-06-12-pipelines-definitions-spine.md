**User-defined pipelines, slice 1 — definitions spine.** New `src/agent/pipeline/` module: a user's free-text workflow description compiles into a typed, versioned `PipelineDefinition` (Zod envelope — trigger, stages, gates with terminating timeout actions, disjoint loop scopes, per-stage tool allowlists) via `chatTyped` plus a deterministic validation pass whose issues feed back to the compiler LLM. Definitions land inactive in the new `pipeline_definitions` table (immutable except `active`; partial unique index enforces one active version per name); the agent surfaces `define_pipeline` / `activate_pipeline` / `list_pipelines` through a new `Service.pipelines` namespace, with preview-then-explicit-confirm as the activation gate. Ten-case compiler eval set runs in the record/replay integration tier. No execution yet — the run engine is slice 2 (PROGRESS Phase 8).
