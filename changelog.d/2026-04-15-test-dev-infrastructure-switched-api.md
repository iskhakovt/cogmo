Test + dev infrastructure switched to `hindsight-api` / `hindsight-api-slim` (API-only variants). `hindsightSlim()` pinned to `ghcr.io/vectorize-io/hindsight-api:0.5.1-slim` (closest-patch replacement for the retired `hindsight:0.5.0-slim`); `hindsight()` uses `ghcr.io/vectorize-io/hindsight-api:latest`. Drops the Control Plane web UI that Cogmo never talks to (~100 MB saving on slim, one fewer service per test run). Verified against Hindsight's `docker/standalone` Dockerfile + `start-all.sh`: API and Control Plane are independent processes, Alembic migrations run in the API, so CP absence is safe. Production guidance in DEPLOYMENT.md is intentionally unchanged — end users may still want CP for memory inspection.
