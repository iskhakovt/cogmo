Remove the runtime per-tool-call permission gate from coding delegation; the sandbox container is now the security boundary, with `git push` / `gh` writes bounded by per-repo PAT scope. Re-routes `profiles.coding_autoapprove_mode` to the plan-approval gate — when `on`, the plan orchestrator auto-stamps `plan_approved_at` and emits `coding/task/plan-approved` once the plan text persists, skipping the Telegram round trip.
