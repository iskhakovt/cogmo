Testing retrospective gap-fills: JSONB read-side Zod parsers reject corrupted rows (raw-SQL bypass); `ClaudeCodeBackend` stream-json parser permissive-input contract pinned; `CodingStreamingRegistry` concurrency invariants — listener-set snapshot, self-unsubscribe, re-entrant publish; reaper TTL boundary (strict `<`) + stale-DB-pass skipped on `docker.listContainers` failure; `/repo add` FSM dialog surfaces `repo_local_path_exists` and transport-side `repo_invalid_input` and clears state.
